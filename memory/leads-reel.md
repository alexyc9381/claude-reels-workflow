---
name: leads-reel
description: "Reel 20 — The Free Lead Machine (Claude Code drives a free scraper to pull local leads w/ emails + write cold emails); CTA LEADS; Nick-Saraev money-receipts + real GitHub repo"
metadata:
  node_type: memory
  type: project
  originSessionId: 18338ca5-bbdb-4b71-8530-25a9d4d4baa5
---

Reel 20 = "**The Free Lead Machine**" (CTA **LEADS**) — Nick-Saraev money-receipts lane: Claude Code drives the free open-source scraper **github.com/gosom/google-maps-scraper** (VERIFIED 4.6k★, MIT, v1.16.1 Jun-2026, extracts name/phone/**email**, ships a claude-skill) to pull a niche+city local-lead list WITH emails, then writes a personalized cold email per row. Villain = "stop paying $2,000 for a lead list" / "no Clay ($500/mo), no Apollo ($99/mo)". Topic chosen after Alex REJECTED two earlier picks as boring: (a) a "fork the open-saas SaaS boilerplate" reel (too slow-burn), (b) generic. The win = money-with-receipts + a villain + a HERO OUTPUT that films itself (a leads table filling live). See [[nick-saraev-style-reference]] (his cold-email-in-Claude-Code lane is the peg; he did NOT name this repo, so attribute the TECHNIQUE to the lane + the repo to itself; no fabricated $ or fake Nick quote on screen).

**⛔ HOOK LOCK (Alex's exact words, do not "zero"-ify):** opening is "**Stop paying $2,000 for a lead list. This free tool builds it for free, and Claude Code runs the whole thing.**" (He explicitly rejected "builds one for zero".)

Built from VO `Untitled - 7:1:26, 2.01 AM.m4a` (Python-glob the ` ` narrow-space + colons). 3 "cut cut" retakes spliced (aselect [15.02,17.86]+[29.90,31.55]+[33.25,38.40]): a "name f-" false start, an "email-" false start, and the whole FIRST "no Clay/Apollo/monthly bill" line before "cut cut". Pipeline per [[video-editing-toolchain]]: 51.2s raw → cut → silenceremove → polish → **`vo_leads.wav` 30.85s, cuts=0**; re-transcribed to verify. Comp `ClaudeLeadsReel` in `video/src/` (**934f / 31.2s**), captions `src/data/words_leads.json` (line=scene 0-6, CTA line 6 hidden), music `seo_music.wav`. Caption fixes in the json: merge "$2"+",000"→$2,000; cloud→Claude, code→Code; "read dissent"→"ready"+"to send,"; six+-figure→six-figure; clay→Clay. **L = [0, 5.08, 9.56, 14.56, 18.74, 24.74, 29.00]**; scene-onset sync ±0.1s (RMS-verified). Delivered `20_Claude-leads.mp4` → Final + Drive/Claude Reels + Photos.

**⛔ v2 REDO (Alex 2026-07-01: v1 was "really really boring" throughout esp. the hook, the "table fills live" was "a big seizure", and — KEY — "replicate the NIGHT video: it's split in half, a clear distinction between the visuals vs the caption; apply that here" + "screen record the actual repo/tool Saraev-style with the highlighter left-to-right on key parts").** Fixes: (1) **SPLIT LAYOUT** = a constant dark navy "screen" `Stage` panel (x36-1044, y284-1104, deep-slate gradient + faint dot-grid + top sheen) holds EVERY hero in the upper zone; captions sit clearly separated below on cream at y1250 — the nightshift split Alex loves. Removed ALL per-scene text headers (captions carry the words); heroes float on the dark stage (light cards pop). (2) **Calm table** = rows fade+slide-up one-by-one (stagger 10f, eOut), smooth eased count-up, a soft green tint on the newest row; REMOVED the sweeping scan-line + glint + fast counter (those were the seizure). (3) **Saraev left-to-right `Marker`** (highlighter bar grows in WIDTH w*p + a leading pen-tip dot + a label chip; `below` prop so top-edge labels don't clip) sweeps over the REAL cropped repo About-panel (`repo_about.png`, ffmpeg-cropped 920×760 from repo.png since PIL isn't installed) — amber on "★ 4.6k · free", green on "email". (4) **Intriguing hook** = a premium white "500 local leads" card (real rows + blurred email dots + "+497 more") with a "$2,000" price ribbon that SLASHES → a green "FREE" ribbon stamps + Claude zap, bold light header "stop paying for a $2,000 lead list" on the dark stage. Stage fades to cream for the CTA. Same L/VO/sync (±0.1s). Re-delivered `20_Claude-leads.mp4` v2. **LESSON: the nightshift "split" (a distinct framed visual STAGE up top + captions clearly separated below) is a reusable layout Alex loves — apply it to real-screen reels too; + he wants the Saraev L-to-R highlighter marker on real repo/tool footage.**

**⛔ v3 (Alex 2026-07-01: "visuals too text-heavy AFTER 19s" + "for the GitHub repo I want to see MORE of it — the name etc, how it works — right now I just see the stars which isn't good").** (1) **Repo shows more:** re-captured the repo FULL PAGE (headless Chrome window 1400×5200 → repo_full.png 2800×10400) and ffmpeg-cropped TWO tall regions: `repo_head.png` (2800×1240 — the NAME gosom/google-maps-scraper + tabs + file tree + About/description + stars) and `repo_how.png` (2800×1480 — the README "Google Maps Scraper" heading + description + the "Goal → Start here" table incl the **"Ask an AI coding agent to run a scrape → AI Agent Skill"** row = how Claude runs it + the Web-UI screenshot). S2 now shows repo_head (markers: clay "free · open source" on the name, amber "4.6k stars") → crossfades to repo_how (markers: clay "how it works" on the heading, green "Claude runs it" on the AI-Agent-Skill row) → the leads table. So you SEE the name + how it works, not just stars. (2) **Post-19s made visual (less text):** PAYOFF = big green count-up "512" + a row of flying **`Envelope`** icons (emails included) + `PriceStrike` cards (Clay $500/mo, Apollo $99/mo) with an animated red **✕ slam** + a green "$0 you own it" — replaced the text subtitle stack. FOMO = a visual transform: a struck "six figures/yr" agency card → a Claude **zap beam** → a green "one chat" chat-bubble ($0, you own it) — replaced the two 3-bullet lists. Re-delivered `20_Claude-leads.mp4` v3. **LESSONS: (a) to "show more of a repo" grab the full-page screenshot + crop the header AND the README/how-it-works, don't just crop the About/stars; the google-maps-scraper README's "AI Agent Skill" row is the perfect Claude tie-in. (b) "text-heavy" fix = replace bullet-list cards with ONE animated visual metaphor (count-up + icons + struck price cards; agency-tag→chat-bubble transform).**

**7 real-screen Saraev-style scenes** (v1 layout; v2 recolors onto the dark Stage) (reuses the [[screen-reel-github-capture]] annotation kit — Browser/RepoShot/Chip/Cursor + ClaudeMark/Bloom/Glint/Captions/ProgressBar): (0) HOOK = a "$2,000 lead-list invoice" card, the price SLASHES + a green "FREE" stamp drops, "Claude Code runs it" badge, hook-header "stop paying for / lead lists"; (1) INPUT = a Claude Code terminal typing ONE line "get me every dentist in Austin — with their email" + niche/city chips; (2) SCRAPE (**hero**) = the REAL repo browser (repo.png, headless-Chrome 2x, focus 0.145 scale 1.62; chips "★4.6k·MIT·free" + "extracts name·phone·email") slides up → the **LeadsTable fills LIVE** (7 real-looking Austin dentist rows: business/phone/green-✓email streaming in + a "▲ NNN found" counter climbing 12→518 + a scan line); (3) EMAIL = a compose card typing a personalized cold email ("Hi Bright Smile team…", business+city highlighted); (4) PAYOFF = big green count-up "512 / leads · emails included / ready to send" + "NO MONTHLY BILL" with **Clay ($500/mo) + Apollo ($99/mo) wordmarks struck through** (favicons failed as HTML → recreated as styled text wordmarks); (5) FOMO = 2-card compare "a lead-gen agency: six figures/yr" (✕) vs "you: one chat" (✓ green); (6) CTA = ClaudeMark + Comment "LEADS" + "the exact free tool". Framing = standard IG safe zone ([[glm-swap-reel-and-setup]]): captions y1244, progress y200, heroes ~y856; ship-gate overlay passed.

**LEADS lead-magnet doc + caption NOT built yet** (see [[lead-magnet-docs]]) — the exact repo + claude-skill install + the one-line ICP prompt + the cold-email prompt. Runner-up backlog: **$0 APOLLO KILLER** (the official apollo-mcp-plugin: one-sentence ICP → ranked leads → enrich → sequence-load; natural Part 2, same CTA). Still scripted-not-built: COUNCIL reel.
