---
name: factory-reel
description: Reel 37 FACTORY / "The Assembly Line" (GPT-5.6 Sol/Terra/Luna as a 3-tier assembly line; Sol writes the spec, Terra drafts, Luna stamps 50, Sol QA-gates; CTA FACTORY) + build notes (cloned from GptSolReel)
metadata:
  node_type: memory
  type: project
  originSessionId: fd2a64f7-8d2a-4a40-a1dc-da7f726a2785
---

**Reel 37 FACTORY** (keyword FACTORY, ~46.4s, `src/ClaudeFactoryReel.tsx`, delivered `37_GPT-5.6-Sol-assembly-line.mp4` to Final + Drive). 2nd GPT-5.6 Sol reel (after reel 36 SOL). VO recorded by Alex 2026-07-08 (11.06 PM take). Concept from the "5 Sol workflow ideas" set (the ARENA-caliber named-mechanism ideas, NOT the money-brag ones Alex rejected).

## Concept (the reframe + walk-through)
Stop running everything on the expensive Sol. Build a 3-tier ASSEMBLY LINE where each model does the job it's priced for: **Sol** (architect) writes ONE master spec + the quality bar → **Terra** drafts → **Luna** (cheap fast, ~1/5 cost) stamps out all 50 units → **Sol** comes back as the QA inspector and kicks the weak ones back. Output = 50 units at flagship quality for pennies. Framed as JUST-LAUNCHED / get-ahead-of-it (Alex: "sol just released, don't say everyone, frame it as new"). Gated per [[gate-the-how-in-scripts]]: the reel shows the line + who does what, but BLURS the master-spec prompt + the QA rubric (🔒 "in the guide"); output shown sharp.

## Scenes (6 beats, L=[0, 11.93, 17.56, 29.11, 37.15, 44.72], CUT 46.3)
Hook (3 phases: torch-bill = Sol + burning $ counter / architect laying bricks = "the SLOW, $$$ way" / assembly-line blueprint reveal SOL>TERRA>LUNA>SOL) → Architect "SOL WRITES THE SPEC" (Sol drops blurred MASTER SPEC on belt + QUALITY BAR fills, "SOL: 1 JOB ONLY") → Line "LUNA STAMPS OUT 50" (Sol hands off, Terra T-drafts fan, Luna moon stamps, ×N counter to 50, COST bar tiny + "1/5 the cost") → QA "SOL INSPECTS THE BATCH" (Sol magnifier, units get ✓, one ✗ kicked back, blurred QA rubric) → Payoff "50 UNITS, PENNIES EACH" (50 units stack sharp + seal + "build once, runs forever" loop) → CTA comment FACTORY (THE FACTORY GUIDE card: master-spec prompt / Terra+Luna line / QA-gate rubric).

## Build notes (reusable)
Cloned `GptSolReel.tsx` (reused ALL infra: SolMascot/LunaMascot, Panel, ScreenHead, ProgressBar, ChapterBar, the 3-2-1 hook countdown, Captions, Sfx, CTA/SnackLane). Method for a big scene-body swap: author the 5 new bodies to a scratch file, PYTHON-SPLICE the line range `const HookBody`..`const Hook: React.FC` (replaced 935 lines->257), then targeted Edits for wrappers/CTA/ChapterBar CHAP/ProgressBar VIRT+marks+STARS+PELLETS/gift-wake range + a clean re-timed Sfx block (the clone carried DUPLICATE Sol+ARENA+OUTLAST sfx w/ dup keys — replace the whole block). Register a new Composition in Root.tsx (durationInFrames=1389). Headers name the models (raycfu [[reel-hook-header]]): SOL WRITES THE SPEC / LUNA STAMPS OUT 50 / SOL INSPECTS THE BATCH.

## Audio pipeline (this take)
58s raw m4a, single clean take (no retakes). Had a 4.96s DEAD GAP mid-recording (after "reach you.") + 1.14s lead — spliced both out via `aselect='between(t,a,b)+...'`, cut after "guide", minimal clean chain (highpass+alimiter+loudnorm, 48kHz), then atempo=1.08 tighten -> `public/vo_factory.wav` (46.36s). Captions: difflib-align canon->whisper-1x timings, scale /1.08 -> `src/data/words_factory.json` (186 words).

## ⛔ PREMIUM REBUILD v2 (2026-07-09) — the "much better iterative approach" Alex demanded
v1 graphics were too flat/sparse AND the hook opened on the SAME big centered Sol sun as reel 36 (Alex: "ppl will immediately scroll thinking it's the same video" + "the chatgpt logos need to be at the hook so ppl know"). The fix = a 3-workflow pipeline (the process to reuse whenever graphics need to jump a tier):
1. **HOOK-REDESIGN design panel** (5 lanes → judge): a NEW hook that is visually DISTINCT from the last reel + features the ChatGPT logo prominently. Winner = **THE BILL FURNACE**: an industrial money-incinerator MACHINE (NOT a sun) whose HOPPER is the big ~150px ChatGPT-knot badge (white circle + teal #10A37F rim, reuse the existing badge markup); $100 bills feed down a chute through the knot into a LOW RECTANGULAR grate fire (fire boxed/clipped so it never reads as a radial sun burst), a red digital meter climbs $1,240→$1,720, "running EVERYTHING on Sol = $$$" pill, a SMALL Sol cameo flicks a bill in at ~4s (Sol = culprit), then a greyed "CHEAPER LINE →" + a 3-dot SOL/TERRA/LUNA belt-stub teases the fix. STANDING LESSON: each new reel needs a NEW hero-silhouette hook; never reopen on the prior reel's hero (esp. the Sol sun) or viewers scroll thinking it's a repost.
2. **PREMIUM-REBUILD authoring workflow** (5 parallel agents, 1 per scene, a shared PREMIUM POLISH CONTRACT + factory bible): rebuilt all 5 scene bodies to the [[reel-progress-bar-reward]]/[[claude-ai-reel-workflow]] polish bar (5 depth layers: bloom→parallax→contact-shadow→sharp hero→glint; surface polish gradients+rim+sheen+stacked shadow on every object; hero fills 78-88%; no dull grey; escalate full duration; gating = blur the prompt/rubric cards w/ lock cue). Agents returned self-contained `(() => {...return <AbsoluteFill>...})()` JSX (HTML-entity-encoded → html.unescape); python-spliced the 5 bodies over the range `const HookBody`..`const Hook: React.FC`.
3. **ADVERSARIAL CRITIC gate** (3 lenses: overlap / flat-sparse / clarity+brand, on full-res frames → synth, drop false positives). Came back 1 real finding: the LINE scene's early right side was sparse (counter/units appeared too late) → fixed by adding a "THE BATCH" 50-cell tracker (fills teal-green as count climbs) + bringing the counter/cost pill in early, positioned right (x754) to clear the Luna station.
Result = reel 37 v2 (delivered, faststart, Final+Drive). Big quality jump; scenes now dense/dimensional.

## v3 (2026-07-09) — MESMERIZING hook + fast retention bars
Alex: the furnace hook v2 was "not interesting enough... not much going on... a lot of stuff overlapping... not sure what's going on"; wanted it "way way better"/mesmerizing, remove the bottom bar, add fast per-scene progress bars. Ran a mesmerize design panel → winner = **THE CASH WATERFALL**: a DENSE continuous column of ~14 $100 bills pouring straight DOWN a central stream (modulo-loop `y=colTop+((lf*speed+i*gap)%span)` so it never freezes + a faint green stream-glow behind) INTO the big 170px ChatGPT-knot badge (dead-center "mouth", teal rim flips brighter on each swallow, teal halo pulses), bills CHAR/ignite as they enter and vanish, a boxed fire grate burns below with COUNTER-FLOW embers rising UP (down-cash vs up-embers = the hypnosis), and a red "$ BURNED $X" ODOMETER climbs. 4 clean horizontal bands (header / waterfall / logo-mouth / burn) = ZERO overlap; one straight cause→effect (money→ChatGPT→fire) = instant mute read. Lesson: MESMERIZING = 5-6 PERIODIC/looping motions so something streams every frame; CLEAR = one vertical cause→effect in clean separated bands; and the ChatGPT logo works best as the single big central object DOING something (eating the money), not a small radiating detail. Removed the ChapterBar; added `StatusZip` fast retention bar to the QA scene (see [[reel-progress-bar-reward]]). Delivered v3 (46.4s, faststart, Final+Drive).

## v4 (2026-07-09) — CHARACTER hook + TerraMascot + fixes
Alex: the Cash-Waterfall hook was still boring ("simple one-thing graphic... just like that for way too long"); wanted the Sol/Luna mascots used + more beats, the ChatGPT logo less-zoomed. Rebuilt HookBody as a **3-BEAT CHARACTER STORY** (see the standing rule now in [[reel-hook-header]]): A) Sol the flagship burning money (bills spiral into Sol + "$ BURNED" meter + a clean ChatGPT badge at 86% = less zoomed) B) Sol alone in a hardhat grinding all 50 tasks by hand + a racing "$$$" meter + "0→50 done" C) Sol + Terra + Luna line up on a belt = "THE ASSEMBLY LINE". Also this session: **built `TerraMascot`** (a friendly green EARTH-globe creature: continents drift, atmosphere glow, tilted orbit ring, clay cheeks — the 3rd tier mascot alongside SolMascot/LunaMascot) and swapped it in for the plain "T" letter in the Line station; "EMAILS SHIPPED"→"TASKS SHIPPED"; fixed the QA MAGNIFIER handle (diagonal 45° bar + a collar joint so it seats into the lens); and **CUT THE VIDEO AT "FACTORY"** (re-cut vo_clean_1x at the 1x "factory" end 49.30s → atempo 1.08 → 45.66s, CUT=45.65, durationInFrames=1370, trimmed words_factory.json to end on "Factory" so no unsaid "for the free guide" caption flashes). Delivered v4 (45.7s, faststart, Final+Drive).

## v5 (2026-07-09) — COSMIC WHIRL pattern-interrupt hook
Alex wanted a real PATTERN INTERRUPT for the hook + Beat B polished: "the sun sucks up all the planets in a circle with sol and terra and other unnamed planets spun around and then flung outwards." Built exactly that as Beat A: **SolMascot (sun) at center + 5 orbiting PLANETS (Terra=green earth, Luna=moon, + unnamed Mars/Neptune/Saturn-with-ring, plain radial-gradient spheres) orbiting on an ellipse, spin ACCELERATES (`spin = lf*0.05 + over(lf,30,64)^2*10.5`), radius SUCKS IN (rMul 1→0.13 easeIn) → white FLASH + Sol flares → FLING OUTWARDS (rMul 0.13→4.6 easeOut + an expanding shockwave ring, planets fade)**. A genuinely kinetic scroll-stopper that opens on a concrete solar-system frame-0. Beat B (Sol grinds all 50 by hand) POLISHED (hot bloom, task-cards w/ clay header stripe + sheen + drop-shadow, a hammer + sweat drop, a card flying off w/ green ✓). Beat C (Sol+Terra+Luna line up) kept. STANDING lesson added to [[reel-hook-header]]: a strong hook pattern-interrupt = a big KINETIC physics move (orbit→vortex→burst) using the mascot cast, not a static tableau. Delivered v5 (45.7s, faststart, Final+Drive).

## v6-v7 (2026-07-09) — avalanche Beat B + planet stations + workflow consolidation
v6: rebuilt hook Beat B from scratch (no props on Sol; overwhelmed Sol buried under a cascading AVALANCHE of task-cards + "$ · and SLOW" meter — see the don't-glue-props rule in [[reel-hook-header]]). v7: the Architect scene's downstream stations were dim "T/L/S" LETTER nodes → swapped to the actual **TerraMascot / LunaMascot / SolMascot** graphics with labels (Alex: "there needs to be the terra luna sol planets here as well"). Then Alex: "make sure these adjustments are added to the reiterative workflow" → **consolidated ALL of this chat's reel-37 lessons into [[reel-ship-gate-pipeline]]** (new-hero-silhouette + multi-beat character hook + kinetic pattern-interrupt / no-props-on-mascots / use-mascots-not-letter-placeholders / premium-rebuild-if-subpar / fast per-scene StatusZip not a 2nd global bar / cut-on-keyword). Delivered v7 (45.7s, faststart, Final+Drive).

## v8 (2026-07-09) — "Sol beats Claude Mythos" flex in the hook
Alex: "show it's better than Claude Mythos." Added a CHAMPION REVEAL as the payoff of the cosmic whirl (fr100-138, reusing the fling's shockwave, zero added time): a gold CROWN (teal jewel = ChatGPT tell) drops onto Sol + a podium rises = Sol on a TALL glowing gold #1 step "SOL 91.9" vs a SHORTER, desaturated slate #2 step "CLAUDE MYTHOS 88.0" (0.64 opacity, no glow) + a teal "THE NEW #1 · TERMINAL-BENCH 2.1" line. Real numbers (Terminal-Bench 2.1: Sol 91.9 > Claude Mythos 5 88.0). Two mute-read signals (podium height + crown). Tightened the A→B hook cross-fade (pA fade 132-138, pB in 139-153) so the podium gets a clean beat before the avalanche. Delivered v8.

## Guide + caption — BUILT (2026-07-09)
`FACTORY - The Assembly Line.docx` in Final + Drive (Node `docx` build script `build_factory_doc.js`, same premium helper set + modelTable as the SOL doc). Delivers the 4 station prompts (the gated HOW): Station 1 = Sol's MASTER-SPEC prompt (write a spec + quality bar a cheap model can execute with zero guesswork), Station 2 = the line handoff (Terra drafter prompt + Luna line-worker prompt), Station 3 = the QA-gate rubric (Sol inspects vs the bar, routes failures back) + a Sol/Terra/Luna cheat-sheet TABLE (real $5/$30, $2.50/$15, $1/$6) + run-the-line rules + build-once callout + honest note. ZERO em/en dashes, 94 paras / 9 tables, XML verified. Caption (`factory_caption.txt`, 351w, 0 dashes) per [[caption-structure]]: opens "Comment FACTORY" → "👇 READ BELOW" → the 3-station concept + model cheat-sheet + the 1/5-cost rule + build-once (exact prompts stay in the guide) → Comment FACTORY → hashtags.
FACTORY guide docx (the 3 station prompts: Sol master-spec, Terra+Luna line handoff, the QA-gate rubric) + caption (opens "Comment FACTORY" then READ BELOW per [[caption-structure]]). Reel is v1, awaiting Alex's iterative notes.
