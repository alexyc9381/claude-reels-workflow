---
name: nightshift-reel
description: "Reel 19 — Claude works the night shift (scheduled/overnight autonomous agent runs a recurring job while you sleep); CTA NIGHTSHIFT"
metadata:
  node_type: memory
  type: project
  originSessionId: 18338ca5-bbdb-4b71-8530-25a9d4d4baa5
---

Reel 19 = "**Claude works the night shift**" (CTA **NIGHTSHIFT**) — set a scheduled/autonomous Claude agent to run ONE recurring weekly job overnight; wake up to the finished deliverable. The freshest-frontier "While You Slept" pick from the [[claude-reel-topic-engine]] trend scout (rides the June-2026 scheduled/Managed-Agents launch; broadest honest breadth; most cinematic). Built from Alex's VO `Untitled - 6:30:26, 1.18 AM.m4a` (had retakes + an embedded "cut this entire part where it says the chore you've done a hundred times" instruction → spliced out the wake-it-up retake + the whole "chore/you just stop" beat; a hidden "There's one task you do, cut cut" retake surfaced after tightening and was also cut). Comp `ClaudeNightshiftReel` (865f / 28.9s) in `video/src/`; VO `public/vo_nightshift.wav` (48kHz, cuts=0), captions `src/data/words_nightshift.json` (line=scene 0-5, CTA line 5 hidden), music `seo_music.wav`. Delivered `19_Claude-nightshift.mp4` → Final + Drive/Claude Reels + Photos. L = [0, 5.64, 13.28, 20.12, 23.86, 26.68]; scene-onset sync ±0.1s.

**Scenes (night→morning color ARC — reusable for any overnight/While-You-Slept reel):** the main `Background` takes `night`/`morning` props (driven by a master `interpolate` over L): HOOK + NIGHT scenes get a cool dark-blue vignette + a twinkling star field + a CSS `Moon`; PAYOFF gets a warm amber morning glow; CHORE/SCHEDULE stay neutral cream. (1) HOOK = a dark "device screen" card showing "Claude · running / 2:0X AM / Working on your report… / progress bar" + the clay sunburst spinning + "you're asleep. it isn't." (recognizable 2am working screen, mute-readable). (2) CHORE = "one task you redo every week" + a repeating MON·MON·MON·MON calendar row + task chips (Monday report / Competitor scan / Content roundup) + the stat "the same 90 minutes, every week, forever." (3) SCHEDULE = a recognizable "Schedule agent" card: "Every Monday · 2:00 AM / weekly" + a "Run automatically" TOGGLE that flips green (cursor) + "✓ runs even with your laptop closed" (set-it-once, satisfying — same recognizable-UI pattern as the GLM model-switch/subscription hooks). (4) NIGHT = dark card, 2:14 AM, 3 steps tick off (pull numbers ✓ / build report / drop in folder) + "laptop closed · no prompt · no babysitting." (5) PAYOFF = morning light, a finished "Monday_Report.pdf" card with a bar chart + "✓ done while you slept." (6) CTA = sunburst + Comment "NIGHTSHIFT".

**⛔ v2 VISUAL REDO (Alex 2026-06-30: "visuals too text-heavy / hook is horrible / redo ALL visuals more interesting / not enough visuals — need MORE visuals transitioning between things").** Scrapped the text-card scenes for **"The Long Night"** (design-panel winner): ONE cohesive night→dawn WORLD with an identity thread, and 6 GENUINELY DISTINCT morphing hero objects (never one diorama reused) — the fix for "text-heavy": the burned captions carry the words, the scenes are IMAGERY+MOTION with only tiny micro-labels. Build = `SkyWorld` global bg (cream base + a navy night-sky radial PANEL behind the hero that warms night→amber→cream across the back half via opacity-crossfade, a parallax twinkling starfield that fades at dawn, a faint sweeping clock-ring hand over the whole 29s, a rising horizon line) + per-scene heroes: (1) HOOK = big crescent `Moon` with the clay `ClaudeSun` as its glow + sleeping skyline silhouette + "you sleep. it works." (captivating, mute-readable, recognizable — beat the rejected device-card hook); (2) CHORE = a seamless conveyor **treadmill** of identical MON report-tiles (scroll via `-(lf*speed)%TW`, speeds up) + spinning loop-arrow + "90 min, every week ×N…forever"; (3) SCHEDULE = a tile flies into the sunburst and MORPHS into a big **AUTO dial** (snaps MANUAL→AUTO) + green strokeDashoffset timer-rings + a laptop lid closing; (4) NIGHT = data particles (slate/green/amber) stream THROUGH the sunburst and **build a document** line-by-line + chart, sliding into a glowing folder (NOT lines-into-a-logo); (5) PAYOFF = the sunburst LIFTS and becomes the **SUN** with rays, sky blooms to warm cream, finished doc + green-check stamp; (6) CTA = sunburst + "NIGHTSHIFT" stamp. Transitions are diegetic via a `Thread` (clay light-streak that travels between heroes) + the continuous sky/clock motion — no hard cuts. ⚠️ Captions live at y1244 which is BELOW the navy panel (panel 300–1060) = on cream, so captions MUST stay dark INK/SLATE (not light) — I had to fix that. Same L/sync/framing as v1. Delivered (`19_Claude-nightshift.mp4` v2). **LESSON: "too text-heavy" = stop duplicating the caption words in on-screen text cards; tell the story with ONE recognizable hero per beat + motion + diegetic transitions, let the captions carry the words.**

Framing follows the standing IG safe-zone system ([[glm-swap-reel-and-setup]] v6): hero center y856, captions y1244, progress bar y200. **NIGHTSHIFT lead-magnet doc not built yet** (see [[lead-magnet-docs]]) — the exact overnight-agent setup (schedule config + role prompt + safe credential setting). Runner-up topic still open: DISPATCH ("text Claude a chore from your phone, it does it on your laptop"). Also still scripted-not-built: the COUNCIL reel.
