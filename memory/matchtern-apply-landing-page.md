---
name: matchtern-apply-landing-page
description: "The apply.matchtern.org lead-gen landing page — source location, funnel, and one-command Netlify deploy"
metadata: 
  node_type: memory
  type: reference
  originSessionId: a464753a-9d7c-4f01-a9b3-38a569c6b0e6
---

The **apply.matchtern.org** landing page (the Meta-ads lead destination) source lives at `~/Downloads/apply-matchtern/`: `index.html` (the page), `other-programs.html` (non-business branch), `booked-thankyou.html` (post-booking pre-frame), `img/` (photos). Originally a Netlify static site whose source was lost; recovered by `curl`-ing the live HTML, then revised. (Internal notes: `~/Downloads/apply-matchtern-REVISIONS.md`, kept OUT of the deploy folder.)

**Deploy (Netlify CLI — the `netlify` binary is NOT on PATH, use `npx`):**
```
cd ~/Downloads/apply-matchtern
npx --yes netlify-cli login            # one-time, opens browser to authorize (Alex's credential)
npx --yes netlify-cli deploy --dir . --site 4242f870-e157-4755-bd56-e7ef141732cf            # DRAFT → preview URL
npx --yes netlify-cli deploy --prod --dir . --site 4242f870-e157-4755-bd56-e7ef141732cf     # LIVE → apply.matchtern.org
```
Netlify site = **incredible-fairy-880c30** (id `4242f870-e157-4755-bd56-e7ef141732cf`), custom domain apply.matchtern.org, account "alex-5bckrke's team". Always **draft-deploy first** and get Alex's explicit OK before `--prod` (it replaces the whole live site). The deploy folder must contain ONLY public-safe files (no `.env`/secrets — the Meta token lives in [[matchtern-meta-campaign-builder]]'s separate folder, never here). First deploy with a fresh recovered HTML: confirm the form has `data-netlify="true"` (Netlify strips it from served HTML after detection — 0 matches live is normal/correct) and verify a test submission lands in Netlify → Forms.

**The funnel (built/revised this session):** ad → page → form (captures lead via Netlify Forms, form name `matchtern-application`; fires Meta Pixel **Lead** — as of 2026-06-30, **ONLY for QUALIFIED (business) submits** (`isBusiness` gate; "Not business-related" is captured in Netlify but fires no Lead, so Meta optimizes toward families Matchtern can actually place — **business is a HARD constraint, they cannot serve non-business students**; "Not sure yet" counts as business/books). Then branch on "Intended field": business → **Cal.com inline embed** (event `matchtern-discovery/discovery`, namespace `discovery`) prefilled with name/email; non-business → `/other-programs.html`. After booking, Cal.com redirects to `/booked-thankyou.html` (fires Pixel **Schedule**). Meta **Pixel ID 870814882684731** is installed (so the [[matchtern-meta-campaign-builder]] `.env` should use this for Lead-conversion optimization). Cal.com settings to set: 48h booking window (Limits → 2-3 business days) + post-booking redirect to booked-thankyou.html.

**CRO pass (2026-06-30, all live):** hero form is now a **2-step form** — Step 1 = low-commitment qualifier (graduating class + intended field, the branch driver), "Continue →" reveals Step 2 = contact (parent name/email/phone); progress bar (`.form-progress`/`.fp-active`), step-nav JS with `reportValidity()` gate + Back button; dropped the redundant "student-first-name" field (handler only needs parent-name/email/intended-major). **Sticky mobile CTA** (`.mobile-cta`, ≤760px, "Schedule a Call" → `#top-form`). **All CTAs unified** to "Schedule a Call" and repointed `#apply`→`#top-form` (the real form; `#apply` bottom section only re-linked up). De-risk microcopy "Free · 20 minutes · No obligation" at the button. **Concrete scarcity = "30 students" cohort cap** (hero subtext + cohort block) — this is a PLACEHOLDER number I chose; Alex should set it to the real cap (it's honest scarcity only if honored). Video `<wistia-player>` given `autoplay muted playsinline` (⚠️ verify autoplay + turn on captions in the Wistia dashboard — captions can't be set from HTML). Below-fold imgs `loading="lazy"`. **Removed the 3 anonymous "Parent of Xth Grader" testimonials** (fake-tell liability) → that section reframed to "By the numbers / What Matchtern delivers." + stats; **still needs REAL testimonials** (name+school) — biggest remaining proof gap. Verified the 2-step interaction + validation with Playwright (file:// is blocked → serve via `python3 -m http.server` then navigate localhost). New file **`privacy.html`** (served at `/privacy`, fixes the previously-404 footer link; effective date June 30 2026, contact privacy@matchtern.org) — built from `~/Downloads/matchtern-privacy-policy.md`.

**Flow restructure (2026-06-30, live) — modeled on RISE Research:** "RISE Research" = **RISE Global Education (riseglobaleducation.com)**, a selective remote 1-on-1 PhD-mentor research program whose funnel is Parent Consultation Call → Application → Profile Shortlisting → (Interview) → Program Onboarding, with a "Book a Free 20-Min Strategy Call" CTA and price kept off-site — a near-perfect structural match to Matchtern. The "How it works" section was rewritten from the old 3-step "apply → train → placed" into a **4-step call-first flow**: (1) Book your free consultation call, (2) Submit your application, (3) We review and match personally, (4) Onboarding, then the program begins (4-week training + venture-backed placement + weekly mentorship live here). Section title now "A considered path, from first call to first placement." **Scarcity: chose HONEST non-numeric framing over a fake "X spots left" counter** (Alex asked for "X spots left," but a hardcoded number that never updates becomes a lie + breaks the no-fabricated-stats rule) — hero scarcity is Alex's own preferred phrasing (2026-06-30): "Admissions for our Summer 2026 cohort are rolling and **spots are limited due to the 1-on-1 format**. Once selected, you can choose a start date that works best for you." (he had me delete an earlier "the earlier you talk with us, the more placements are still open" clause); cohort tile = "Cohort size: Small by design." Hero sub-headline says "elite **universities**" (not "elite programs"). ⚠️ STANDING: **NO EM DASHES anywhere on the site** — see [[no-em-dashes-in-copy]]; swept all 4 pages to 0. Stays true across the whole cycle with no maintenance; if Alex ever wants a hard number, ship it ONLY in the final week of a cohort with a live watch + commit to decrementing it. Hero subtext also de-salesed (dropped "how we'd help" → "whether Matchtern is the right fit — no pressure, nothing to prepare"). Copy was researched + adversarially verified (honesty + tone lenses) via a Workflow before shipping.

**Key decisions:** "book a free strategy call" framing (not "application"); **price kept OFF the page** (revealed on the call — consultative, per [[matchtern-primary-messaging]] no-price rule); SMS consent is a submit-disclaimer + hidden field (not a required checkbox); real photos beat stock for "not vibecoded" — Subha Sundaram's real headshot (`img/subha.jpg`) sits by his quote; `img/office.jpg` is an Alex-provided photo for the "How it works" section (pending). NOTE: I can't pull chat-attached images to disk — Alex must save them into `img/` himself, then redeploy. Alex has ~10 Netlify sites incl. go.matchtern.org (matchtern-10things) and matchtern-{economics,cs,marketing}-careers.
