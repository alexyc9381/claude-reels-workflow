---
name: fable-reel
description: "Reel 22 — Fable 5 guide (you're wasting 90% of your tokens); Cooper-Simson-style 4-tip listicle; CTA FABLE + the open-loop retention system"
metadata:
  node_type: memory
  type: project
  originSessionId: 18338ca5-bbdb-4b71-8530-25a9d4d4baa5
---

Reel 22 = "**You're wasting 90% of your Fable 5 tokens**" (CTA **FABLE**) — a **Cooper-Simson-style 4-tip guide** to using Claude Fable 5 right. Alex asked for a Fable 5 guide "like Cooper Simson does it." Cooper Simson = @cooper.simson ("Actionable AI"): blunt, anti-hype, **metric-first operator voice**, one takeaway, DM CTA. ⚠️ His exact frame-level edit could NOT be scraped (IG login-walled; searches returned hallucinated/other-creator results, e.g. a "Cooper" Fable reel that was actually marc.kaz) — modeled his VOICE + tips-structure, NOT a specific video. To match a specific reel exactly, get the link from Alex + transcribe locally.

**The 4 tips (from Anthropic's official Fable 5 prompting guide, all concrete, not buzzwords — Alex rejected 2 earlier buzzwordy drafts):** (1) Give it a GOAL, not steps (Fable obeys wrong steps literally; give outcome + "done when" + verify). (2) Make it PROVE its work (one line: "before reporting progress, check every claim against a real result from this session"; nearly kills fabricated "done"). (3) Give it a MEMORY FILE (one lesson per file, update old, delete wrong → ~3x the memory boost the old model got). (4) **Turn down the EFFORT DIAL** (low/med/high/xhigh; most people leave it maxed = paying full price to fix a typo; low still beats last year's Opus → **same quality, ~1/10 the tokens** = the 90% payoff). **Fable 5 facts (HIGH-confidence):** 50M-line Stripe migration in a day, first to 90% on hard analytics, 1M context, persistent memory, $10/$50 per M, live July 1 2026 on Claude.ai/Code/Cowork.

**⛔ OPEN-LOOP RETENTION SYSTEM (Alex specifically pushed for this — "make them stay till the end like other creators"; and iterated hard: an abstract tease ["the last one makes it learn…"] was "too boring", he wanted a NUMBER + loss-aversion → "the last one cuts 90% of your tokens"):** hook = "burning 10x the tokens → the LAST fix cuts that 90%" (loss aversion + a specific-number payoff teased up front). Then the strongest/most-surprising tip is saved for LAST and genuinely pays off (effort dial = the 90%). A persistent **`Tracker` (1·2·3·4 dots at top of stage, current lit, #4 ALWAYS amber-outlined/teased)** rides every tip scene so viewers literally see #4 coming. Loop the ending ("and that's it"). **Reusable for any tips/listicle reel.** Also: keep it CONCISE (strip fluff words) but not choppy. See [[reel-retention-hook-teardown]] + [[shortform-scripting-playbook]].

Built from VO `Untitled - 7:1:26, 11.07 PM.m4a` (Python-glob narrow-space). 5 "cut cut" retakes spliced (aselect: hook restart, tip-1 "Fable fo-" + a messy DOUBLE retake on "give it the outcome", tip-3 "This will make it", tip-4 "High for height"). Pipeline: 95s raw → cut → silenceremove (stop_silence 0.32) → **atempo 1.06 speedup** (it ran long) → polish → **`vo_fable.wav` 43.55s, cuts=0**. Comp `ClaudeFableReel` in `video/src/` (**1312f / 43.8s**), captions `src/data/words_fable.json` (line=scene 0-5, CTA line 5 hidden; fixed whisper "Throbix"→Anthropic), music `seo_music.wav`. **L = [0, 7.46, 13.68, 21.88, 28.82, 40.98]** (6 scenes: hook + 4 tips + CTA); sync ±0.07s. Uses the [[books-reel]]/[[leads-reel]] nightshift-split Stage. 6 scenes: token-burn hook (🔥 counter + 10x + the 4-fix tease) → Tip1 steps✗→goal✓ card → Tip2 paste-line + fake✗/proven✓ → Tip3 .md files + "3x" badge → Tip4 segmented effort dial + token bar dropping to 1/10 + "90% fewer tokens" → CTA. Delivered `22_Claude-fable5-guide.mp4` → Final + Drive/Claude Reels + Photos.

**⛔ v2 VISUAL REDO (Alex 2026-07-01: "visuals not nearly good enough, TOO MUCH TEXT not visual substance, the initial HOOK is not good whatsoever, needs more interesting stuff" — his recurring text-heavy note again).** Design-panel winner = ONE recurring physical HERO PROP: a **Claude coin-pouch burning over a flame = your tokens**, with a **burn→bank arc** across the whole reel (hook: pouring coins into fire; tip4: dial turned down → flame snuffs → coins reverse-pour back UP into the pouch → banked; CTA: banked). Each beat = a distinct floating OBJECT with a state-flip, NO text cards: HOOK = pouch pouring gold coins into a flame + "10x" + the untouched xHIGH dial + a **padlocked, blurred "-90%"** ghost tag (the open loop to tip4); TIP1 = a red tangled step-by-step path that misses → straightens into a clay arrow hitting a green **bullseye** (dropped the tiny-robot version, it read as scattered bits); TIP2 = a red "DONE" **stamp** on a blank doc → a **magnifier** catches the lie → a green verified **seal**; TIP3 = a **brain** that brightens as verified lesson-cards dock into it + "3x" (had to thicken the brain SVG sulci + size 360 so it doesn't read as a blob); TIP4 (payoff) = the same pouch+dial, **twist the dial to LOW** → flame snuffs, coins reverse-pour, quality bar stays pinned green ("still beats old Opus"), big centered **"-90% tokens"** (loop closed). Color grammar: clay=Claude/good, RED=waste/fabrication, GREEN=verified/quality, AMBER=burning. ⚠️ watch collisions: tip4's right-side dial+quality+(-90%) collided first pass → fixed to dial-top / compact quality chip / big centered -90% payoff. **RETENTION upgrades added:** the ProgressBar is now **segmented with 4 milestone nodes** (numbered 1-4, turn green ✓ as passed, #4 always amber-teased = merges the open-loop tracker into the bar = goal-gradient effect). Re-delivered `22_Claude-fable5-guide.mp4` v2. **LESSON: for a "tips/how-to" reel, bind it with ONE recurring physical prop + a visible before→after arc (burn→bank); make each tip a real object doing a state-flip, never a text card; abstract-character metaphors (robots) read as scattered bits at reel scale — prefer bold single shapes (bullseye, stamp, brain, dial).**

**⛔ v3 REAL-ASSET REDO (Alex 2026-07-01/02: "each of the visuals is STILL too boring" + "not refined at all, not that good" + "make the top progress bar bigger/more visible"). Asked him the direction via AskUserQuestion → he chose "REAL SCREENSHOTS + ASSETS".** KEY INSIGHT: hand-coded Remotion shapes (pouch/brain/dial) have a refinement ceiling and read as amateur; his best-looking reels (leads, second-brain) used REAL screenshots. The 4 Fable tips come STRAIGHT from Anthropic's official **"Prompting Claude Fable 5" guide** (platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5), so every beat is now a REAL screenshot of that guide in a browser frame + a Saraev L-to-R marker on the exact line, cinematic push-in, captions below. Capture method (in public/fable/): headless-Chrome full page (window 1400×6000, ×2 scale → guide.png 2800×12000), ffmpeg-crop content column `guide_col` (crop=1560:12000:600:0), then crop each section by Y: HOOK=guide top (title+intro), TIP1 goal=**Strong instruction following** (y4020, marker on "steer most behaviors with a brief instruction"), TIP2 prove=**Ground progress claims during long runs** (y5560→y4880, marker "nearly eliminated fabricated status reports"), TIP3 memory=**Construct a memory system** (y6720, marker "as simple as a Markdown file"), TIP4 payoff=**Consider all effort levels** (y3050, marker "Lower effort settings… exceed xhigh performance on prior models" = the 90%). ⚠️ live per-section anchor-URL captures were FLAKY (fired 3 headless Chromes in a loop → blank pages); crop from the ONE reliable full-page guide_col.png instead. Progress bar made BIGGER (15px bar, 38px milestone nodes that pop when they lock green, #4 amber-teased). Re-delivered `22_Claude-fable5-guide.mp4` v3 (real-asset). **STANDING LESSON: when Alex says visuals are "not refined / boring" and the topic is abstract, STOP hand-drawing objects and ground it in REAL screenshots (docs, repo, UI) in a browser frame + markers, like [[leads-reel]] / [[screen-reel-github-capture]]. Ask the direction if unsure. Every reel's factual tips likely have a REAL Anthropic docs page to screenshot.**

**⛔ v4 "THE LIGHTCRAFT" (Alex 2026-07-02: reconciled ALL prior notes — "hook has way too much text, make it an intriguing VISUAL GRAPHIC not the guide"; "highlighting bad + docs overflow the container"; and the RESOLUTION: "PRIMARILY high-quality interesting custom VISUALS like the NIGHT video, supplemented by SMALL screen-recs only (screen-rec text is too hard to read to be the hero)").** So: scrapped the doc-screenshot version, went back to custom visuals but at NIGHTSHIFT'S polish bar via a design panel → winner **"The Lightcraft"** (cockpit-of-light world). ONE cohesive world, driven by the master frame: (1) **THE CORE = the token meter** — a persistent glowing ClaudeSun engine (rays + gauge-ring) low-center that redlines HUGE + orange/red in the hook (wasting fuel, needle pinned to a red "×10" zone, embers bleeding out) and by TIP4 visibly **DIMS + shrinks to a small cool green-clay ember** = the 90% made literal in ONE object watched the whole 43.8s (= the retention "unresolved element that resolves at the end"); (2) **THE THROTTLE LEVER = the open loop** — planted jammed-forward frame-0 with a locked/blurred green "−90%" tag, releases + pulls back + reveals sharp only in TIP4 (loop closes on the exact object it opened on); (3) a **console rail + traveling clay Spark** hands off between beats (diegetic, no cuts) + a speed-streak starfield (turbulent→cruise→vista) + a color arc (hot furnace→cool banked-gold). Per-tip custom heroes dock on the rail (upper-center focus, core glows below): TIP1 = a nav/radar disk (step-tangle misses → one green arc to the goal PIN); TIP2 = 3 "done ✓" chips, a scanner beam exposes one as a **hollow shell** (cracks open, "hollow", ejected) = "it fabricated done" as a physical object; TIP3 = a memory **crystal** that brightens as ✓ lesson-cards dock + "3x"; TIP4 = pull the throttle → core dims → "−90% tokens" + quality bar held. Palette cool-warm balanced (warm bloom always anchored by cool navy so never orange mud). Re-delivered `22_Claude-fable5-guide.mp4` v4. **STANDING LESSON (supersedes the v3 real-asset lesson for THIS case): Alex's true preference = NIGHTSHIFT-caliber cohesive CUSTOM visual worlds (one identity thread + polished per-beat heroes + premium depth), with screen-recs only as small supplements. The refinement fix was a cohesive WORLD + a persistent hero-that-carries-the-payoff, not real screenshots. A persistent object that IS the metric (redline→ember) is both the best retention device AND the payoff.**

**v5 (Alex 2026-07-02: "hook graphic is FINE, but the graphics after are too JUMBLED / hard to see / not straightforward"; "include Anthropic's Fable 5 release VIDEO in it"; "progress bar BIGGER / more noticeable").** Fixes: (1) DECLUTTER the middle tips — added a `tips` phase driver in CraftWorld and DIM the persistent world way down during tips 1-3 (core opacity ×(1-tips·0.72), gauge fully hidden, throttle ×(1-tips·0.6), starfield ×(1-tips·0.5)) so each tip is ONE clear hero on the panel; the core/gauge/throttle return to full only for the hook + the tip4 payoff. (2) Included Anthropic's REAL release video: grabbed the "Introducing Claude Fable 5" YouTube thumbnail (youtube.com/watch?v=Y9Wz2PV404E → img.youtube.com/vi/Y9Wz2PV404E/maxresdefault.jpg, the butterflies illustration → public/fable/fable_video.jpg) and a `VideoCard` player-frame (play button + red scrubber + "Introducing Claude Fable 5 · Anthropic" bar) that establishes the hook for ~2s (0-2.7s) then shrinks/fades to reveal the redlining core; delayed the header + ×10 to appear after it. (3) ProgressBar much bigger (22px bar, 56px milestone nodes, top 182). Re-delivered `22_Claude-fable5-guide.mp4` v5. LESSON: a persistent world/identity-thread must DIM under the per-beat hero during the middle beats or it reads as jumbled; only let it dominate at the hook + payoff. Screen-rec supplements = use `<Img>` (not `<img>`) so they load during render.

**v6 PER-TIP CRAFT PASS (Alex 2026-07-02: "each of the visuals for each number are really boring and just not that polished, needs to be much much improved").** Ran a design+adversarial-critique workflow → 4 GLOBAL POLISH RULES that fixed why they read flat: (1) OFF-CENTER + PERSPECTIVE-TILT every hero (`Plane` helper: perspective(1400px) rotateX(9deg) rotateZ(-3deg) + desynced idle sway) — never dead-center-flat; (2) REAL EXTRUDED THICKNESS (render the shape with a visible darker side-wall, e.g. rings get `boxShadow: 0 12px 0 <navy-desaturated hue>` = milled edge, not a flat gradient disc); (3) TEMPERATURE FIGHT + 3-POINT LIGHT (cool key light-cone upper-left + corner vignette + dust motes in CraftWorld; a COOL RIM on warm-on-warm props so gold-on-gold doesn't vanish); (4) ANTICIPATION → weighted IMPACT (`shakeXY` comp-shake on the whole `Plane` + ring-punch sequence) → few CHUNKY physics `Sparks` (gravity+drag, some LAND and sit). Rebuilt the 3 tip heroes premium: TIP1 = a milled concentric TARGET (5 beveled radial-gradient rings navy→gold, extruded) + a gold DART that THWACKS dead-center (fletches/shaft/barrel w/ a cool rim) + comp-shake + landing sparks + a dim losing tangle; TIP2 = a big faceted hex "DONE" seal (6 lit/dark facets, bevel, embossed) that an x-ray reveals is HOLLOW ("0 tests"), then RE-FORGES a solid green "VERIFIED" seal; TIP3 = a faceted clay GEM crystal (lit/dark facets, moving `Spec` specular, fake-refraction back-layer) that grows/brightens as ✓ lesson-shards dock → 3x, + a wiped amnesia-ghost. Added `Bloom`/`mono` (were missing in v4). Re-delivered `22_Claude-fable5-guide.mp4` v6. **STANDING CRAFT LESSON: to make hand-coded Remotion objects look PREMIUM not amateur, apply the 4 rules — off-center perspective tilt, visible extruded side-wall, cool rim + 3-point light (kill mono-warm-on-navy), and anticipation→comp-shake-impact→chunky physics particles that land. Flat-centered gradient shapes = the "boring/unpolished" tell.**

**v7 CLARITY FIX (Alex 2026-07-02: tips 2/3/4 are "too abstract / random mumbo jumbo / completely off center / no clue what it represents"; tip1 target+dart was FINE because it's literal+recognizable).** The premium-craft objects (faceted seal, gem crystal, throttle) were polished but too ABSTRACT + the off-center perspective read as "off center/broken." LESSON: for a software-tip reel, CLARITY beats abstract art — each tip must be a LITERAL, RECOGNIZABLE, CENTERED thing you get in <1s. Rebuilt 2/3/4 as clean centered UI cards: TIP2 = a "Did it actually finish?" card where Claude's "✓ It's all done" claim gets struck red + caught ("0 tests, nothing built") then a green "✓ Verified — 12/12 tests passed" (literal = make it prove it); TIP3 = an actual dark-editor **memory.md** file that types in lessons ("- always use annual pricing…") + a "3x smarter every run" badge (literal = a memory file); TIP4 = an "Effort setting" card with a segmented **LOW·MED·HIGH·xHIGH slider** sliding to LOW + a "tokens per task" counter dropping + "-90% tokens · same quality" (literal = the effort dial). REMOVED the confusing persistent throttle from CraftWorld. Kept the engine-core hook, Anthropic release video, bigger milestone bar, and tip1 (milled target + dart THWACK). Re-delivered `22_Claude-fable5-guide.mp4` v7. **STANDING: recognizable literal UI (a checklist, a named file, a labeled slider) centered + clean reads instantly; abstract "premium" objects (seals/gems/levers) + off-center perspective read as confusing "mumbo jumbo" even when well-crafted. Default to literal for how-to/software tips.**

Also answered Alex's Q on **faceless retention**: the face's job is done by the HOOK + constantly-changing VISUALS; biggest levers = win the first frame on mute + a stake-not-label hook, change the screen every 1-3s, open a loop, front-load the payoff, cut every pause, captions + progress bar, audio carries the personality. **FABLE lead-magnet doc not built yet** ([[lead-magnet-docs]]) — the 4 exact prompt blocks (goal template, prove-your-work line, memory-file convention, effort-dial guidance).

**v8 TIP1 CELEBRATION (Alex: after the dart hits "there's nothing after that, it's just really boring, want more animation afterwards so it retains").** Kept the dart STUCK (removed the fade-out cutoff), added post-impact: 3 shockwave ripple rings, 16 rising rotating confetti dots, a continuous victory pulse `vic` on the rings, a "BULLSEYE!" label (lf38-86) → green "✓ hit it, first try" chip (lf84+). LESSON: after a hero-moment (an impact/reveal), keep ESCALATING (celebration/pulse/particles) or the beat reads dead; never let a payoff freeze.

**v9 IG-FEED REFRAME (Alex viewed it in the real IG Reels FEED: progress bar + hook header were COVERED by the "Reels/Friends" tab + status bar; "content too biased up high").** Fix: added `translateY(90px)` to the inner zoom `<AbsoluteFill>` (shifts Stage+Scenes+Captions uniformly down) + moved `ProgressBar` top 182→272. Now clears the top IG chrome (verified by compositing red boxes over top 0-250 / bottom 1580-1920 / right 956+). Standing rule saved: [[reel-ig-feed-safezone]].

**v10 FINISH-LINE REWARD SEAL (Alex: "progress bars should end in a finish-line / money-back checkmark to signify a reward at the end if they stay").** Added a gold reward seal at the bar's right end in `ProgressBar` (72px, `right:-14 top:-8`): teased+glowing dim-gold ✓ from frame 0, UNLOCKS at the CTA (t≥L[5]) with a bright gold fill + white ✓ scaling in + 9 rising sparkle dots + scale pop. Distinct from the 4 numbered milestone nodes. Standing rule saved: [[reel-progress-bar-reward]]. Re-delivered `22_Claude-fable5-guide.mp4` v10 (Final+Drive+Photos).

**FABLE lead-magnet doc BUILT (2026-07-02):** `FABLE - The Fable 5 Guide.docx`, grounded in Anthropic's real Fable 5 prompting guide via a research+critique Workflow, ships 3 verbatim Anthropic blocks + honest hedges (retired the reel's 10x/90%/3x). Details in [[lead-magnet-docs]].
