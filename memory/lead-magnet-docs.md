---
name: lead-magnet-docs
description: "The per-reel lead-magnet .docx giveaways (comment KEYWORD -> 'the exact setup') — which exist, where, and how to build/verify more on this Mac"
metadata:
  node_type: memory
  type: project
  originSessionId: 18338ca5-bbdb-4b71-8530-25a9d4d4baa5
---

Each Claude reel's CTA promises a free doc ("comment KEYWORD and I'll send you the exact setup / full workflow"). Those giveaways are premium **.docx** files Alex DMs to commenters. Saved to `~/Downloads/Claude-Reels-Final/` AND copied to Google Drive `My Drive/Claude Reels/` (alongside the reels).

**EXIST so far (built 2026-06-28):**
- `DESIGN - Claude Brand Designer Setup.docx` (reel 16, keyword DESIGN) — Brand Kit + designer system prompt + per-asset prompts (posts/ad/thumbnail/one-pager/guidelines) + export method + platform-size table.
- `SEO - Steal Competitor Traffic Setup.docx` (reel 15, keyword SEO) — gap-finder prompt + rank-ready article-writer prompt (title/headings/meta/JSON-LD schema/internal links) + sample gap table + $3k/mo-agency callout.
- `SITE - 10k Websites in One Chat Workflow.docx` (reel 12, keyword "site") — brief template (incl. the reference-site trick) + build prompt + "make it premium" tweaks + AI-made-vs-$10k table + preview/deploy (Vercel/Netlify) + how to sell.
- `LEADS - Free Lead Machine Setup.docx` (reel 20, keyword LEADS, built 2026-07-01) — commands grounded by WebFetching the repo README (verbatim): install the Claude Code skill `npx skills add gosom/google-maps-scraper` (needs Docker), tell Claude "Find me every dentist in Austin, with their emails" → it runs the scrape, then a cold-email-writer prompt over the CSV; plus the Web-UI Docker command (localhost:8080) and the CLI `-email` CSV command, a "good to know" section (email extraction slower, `-depth`, CSV/JSON, CAN-SPAM/GDPR compliance), paid-tools-vs-this table, and the $500/mo-scraper callout. MIT/free. ZERO em/en dashes.
- `NIGHTSHIFT - Overnight Agent Setup.docx` (reel 19, keyword NIGHTSHIFT, built 2026-07-01) — grounded in the REAL feature via WebSearch: **Claude Code Routines** (saved prompt + connectors + a schedule trigger, runs on Anthropic's cloud with the laptop closed; set via claude.ai/code/routines or the CLI `/schedule`; research preview on paid plans, ~5/15/25 runs-per-day Pro/Max/Team, 1-hour min interval). Doc = 4-step setup (pick one job → write the routine prompt → connect tools safely via connectors not passwords → schedule it) + a copy-paste "Monday report agent" routine prompt + the `/schedule` CLI line + "make it bulletproof" (run once by hand, define done, fail gracefully, leave a trail) + weekly-grind-vs-routine table + the 90-min-chore callout. ZERO em/en dashes.
- `BOOKS - Claude Bookkeeping Setup.docx` (reel 21, keyword BOOKS, built 2026-07-01) — the folder setup + a copy-paste **rules.md** (chart-of-accounts + categorization rules) + the exact bookkeeper prompt (P&L + CSV ledger + a "Review" list + reconcile check) + "check it before you trust it" + old-way-vs-this table + an honesty note (Claude categorizes/reconciles, does not file; pro-review taxes) + the $300/mo-bookkeeper callout. Build script `build_books_doc.js` in scratchpad. ⛔ verified ZERO em dashes AND en dashes (Alex asked explicitly) via the zip+regex check; HTML-mirror screenshot passed.

- `FABLE - The Fable 5 Guide.docx` (reel 22, keyword FABLE, built 2026-07-02) — the 4 fixes GROUNDED in Anthropic's real Claude Fable 5 prompting guide (researched + adversarially critiqued via a Workflow). Ships 3 Anthropic blocks VERBATIM in gold-accent boxes labeled "From Anthropic's Fable 5 docs": (1) prove-your-work "Before reporting progress, audit each claim against a tool result from this session..."; (2) long-build self-verify "Establish a method for checking your own work at an interval of [X]..."; (3) the goal template + no-tools self-check "Before you finish, verify your answer against [my success criteria]". Fix 3 memory = Claude Code CLAUDE.md path (/init, "remember that", /memory) + starter block + API memory tool `{"type":"memory_20250818","name":"memory"}` + hygiene (delete contradictory). Fix 4 effort = real levels low/medium/high(default)/xhigh/max, surfaces (Claude Code /effort + `effort` in settings.json; API `output_config.effort`; chat apps have NO named dial) + the quotable "Lower effort settings on Claude Fable 5 still perform well and often exceed xhigh performance on prior models" + max_tokens caution. HONESTY: retired the reel's unhedgeable 10x/90%/3x (no official figure) into hedged/illustrative + an "Honest notes" box citing platform.claude.com. ZERO em/en dashes (grep-verified on source AND rendered docx). Build script `build_fable_doc.js` + HTML mirror `fable_doc_mirror.html` in scratchpad.

- `ATTACK - The Attack Guide.docx` (reel 25 ATTACK, keyword ATTACK, built 2026-07-05) — delivers the reel's promised "exact attack prompts" (plural). Core move = the verbatim reel prompt "Attack this page. List every reason someone closes the tab without buying" (upgraded box adds: no compliments, rank by cost, quote the exact line). Sections: THE ATTACK PROMPT (+ why it works callout, grounded in Anthropic's fresh-adversarial-review-beats-self-critique guidance) → MAKE IT RUTHLESS (red-accent "most skeptical buyer in my market" role prompt) → THE LOOP (paste-back line + "until it goes quiet = ready") → a Polite-Claude-vs-Attacked-Claude 2-col cmp table → THE EXACT ATTACK PROMPTS = 5 per-asset paste boxes (sales/landing page, cold/pricing email, proposal/quote, pricing page, ad/hook judged on mute) → "replaces a paid conversion audit" callout → HONEST NOTE (Fable follows literally so it over-flags; judge the flags, don't obey them). Build script `build_attack_doc.js` in this session's scratchpad (adds a `cmp` 2-col-table helper + an `asset` labeled-box helper to the shared UNLOCK/SHARP helper set). VERIFIED: XML well-formed (document.xml+footer1.xml), ZERO em/en dashes, 86 paras / 11 tables, key content present. Same proven premium style as UNLOCK/SHARP (already screenshot-verified) so no separate HTML mirror needed.

- `WORTHY - Trigger Words + Rephrase Patterns.docx` (reel 27 WORTHY, keyword WORTHY, built 2026-07-06) — delivers the reel's "trigger words and rephrase patterns" promise, matching the on-screen doc mock (5-second /status check / trigger-word list / name-the-output rephrases). Structure: honesty-hedged context up front (user reports, NOT confirmed Anthropic docs) → STEP 1 the 5-second check (/status box + claude.ai model-picker note + "note the wording, that's your suspect") → STEP 2 the reported trigger words (gold box: vulnerable/unsafe/hook with in-context examples, ONLY the 3 actually reported — no invented list) + "the pattern matters more than the list" + a catch-your-own-words callout → STEP 3 name-the-output: 4 red-instead/green-say-this before-after pairs (weak hook / unsafe checkout / find-vulnerabilities / desperate email) + the reusable pattern box "[verb] + [artifact] + so that [reader does X]" + problem-word-vs-output-named cmp table → "works even if the router reports are wrong" callout (Fable follows literally, verified) + honest notes. ZERO em/en dashes, XML verified, 14 tables/86 paras. Build script `build_worthy_doc.js` in scratchpad. Red/green accent boxes = new instead/say-this pair helper.

- `AUTOPILOT - The Autopilot Setup.docx` (reel 32 AUTOPILOT, keyword AUTOPILOT, built 2026-07-07) — the Meta Ads MCP connector guide. "Connect it in 60 seconds" callout + gold repost/win-Claude-Max callout up top → STEP 1 add the connector (blue-accent box: URL `https://mcp.facebook.com/ads` via Settings>Connectors>Add custom connector, authorize with the Facebook acct that has ad access, "List my ad accounts" to confirm) + a "if Meta moved the URL, it always lives on a Meta domain" fallback → STEP 2 the first 5 prompts, each its own labeled box (1 daily check read-only, 2 cut losers<1.5 ROAS, 3 feed top-3 winners +20%, 4 launch $500/day PAUSED, 5 weekly report) → green "It launches PAUSED by default" safety callout → "Read this before you trust it" bullets → honest note (open beta, works all plans, automates clicks not results). ZERO em/en dashes, 71 paras verified. Build script `build_autopilot_doc.js` in scratchpad (reuses the blueprint helper set: titleBlock/h1/kicker/promptBox/callout/bullet/footerCTA/mkDoc). (CLONE + BLUEPRINT docs also built earlier — see [[autopilot-reel-script]] / [[clone-reel-script]] / [[midnight-reel-script]].)

- `SOL - The Sol Playbook.docx` (reel 36 SOL, keyword SOL, built 2026-07-08) — the FIRST ChatGPT/OpenAI lead magnet (not Claude). Delivers the 3 gated GPT-5.6 Sol workflows as copy-paste prompts, grounded via WebSearch: (1) the ULTRA-MODE BRIEF (split a big job across parallel subagents + merge + max reasoning effort) + how to turn it on; (2) THE COST STACK = a Sol/Terra/Luna cheat-sheet TABLE (real per-1M pricing $5/$30, $2.50/$15, $1/$6) + a 2-box "Sol plans / Luna executes" handoff + rule-of-thumb; (3) THE AGENT BRIEF + 5 guardrails (goal-not-script, read-only-first, fence access, confirm-before-irreversible, leave-a-trail); closes with the why-first edge + do-it-today + an honest note (limited preview ~Jul 2026, UI/pricing can change). Build script `build_sol_doc.js` in scratchpad (reuses the AUTOPILOT helper set + adds a `modelTable` 3-col helper). ZERO em/en dashes, 105 paras / 9 tables, XML verified. Footer/creator = "Alex" (not "Claude") since it's an OpenAI-topic doc. See [[gpt-sol-reel]].

- `FACTORY - The Assembly Line.docx` (reel 37 FACTORY, keyword FACTORY, built 2026-07-09) — the GPT-5.6 Sol "assembly line" cost trick. Delivers the 4 STATION prompts: (1) Sol's MASTER-SPEC prompt (a spec + quality bar a cheaper model executes with zero guesswork), (2) the line handoff = Terra drafter prompt + Luna line-worker prompt (mass-produce all 50), (3) the QA-gate rubric (Sol re-inspects vs its own bar + routes failures back). Plus a Sol/Terra/Luna cheat-sheet table (real per-1M pricing), the "Sol only touches the batch twice = 1/5 cost" rule, run-the-line bullets, build-once callout, honest note. Build script `build_factory_doc.js` (reuses the SOL helper set + modelTable). ZERO em/en dashes, 94 paras / 9 tables, XML verified. See [[factory-reel]].


- `ERASE - The Erase Guide.docx` (reel 40 ERASE, keyword ERASE, built 2026-07-10) — the digital-footprint removal guide. Over-delivers vs the video (video showed 4 rough prompts; guide holds all 5 word-for-word + a broker list + email template). Sections: START HERE callout (turn on web research) -> THE 5 TAKEDOWN PROMPTS (paste boxes: 1 find yourself, 2 rank worst-first by home-address, 3 write+send every removal request w/ log, 4 the ESCALATION for sites that ignore you [second notice + state deletion right if eligible], 5 monthly re-check auto-refile) -> THE RE-LIST SITE LIST (12-row broker->opt-out-URL table: Spokeo/Whitepages/BeenVerified/Radaris/Intelius/TruePeopleSearch/FastPeopleSearch/InstantCheckmate/TruthFinder/PeopleFinders/MyLife/Nuwber) -> copy-paste REMOVAL EMAIL template -> "why this works" green callout -> paid-removal-service ($100-250/yr) vs free cmp -> honest note (removals take days-weeks, some need email/ID verify, re-listing normal, legal rights are state-dependent). Build script `build_erase_doc.js` in scratchpad (reuses the premium helper set: kicker/h1/body/bullet/promptBox/callout/siteTable/cmp; added siteTable 2-col broker helper). ZERO em/en dashes (grep-verified), 10 tables/~121 paras, XML well-formed. Caption `erase_caption.txt` (comment-first structure). See [[erase-factory-log]] (in memory/reels/).

- `BALL - The Fable 6 Setup.docx` (reel 52 BALL, keyword BALL, built 2026-07-14) — delivers the VO's "the exact setup is the whole trick / comment ball and I'll send you the full setup". Grounded in REAL Claude Code features (not invented). Cover "The Fable 6 Setup" / "The exact system that makes today's Claude run like the model that isn't out yet" → build-in-this-order callout → **UPGRADE 1 MEMORY**: `~/.claude/CLAUDE.md` (global) vs `./CLAUDE.md` (project), `/init` + `/memory` box, the `#`-prefix add-a-memory-in-one-line box, a starter global CLAUDE.md box → **UPGRADE 2 MCP**: `claude mcp add filesystem -- npx -y @modelcontextprotocol/server-filesystem ~/work` + github + `claude mcp list`, the Settings>Connectors no-terminal note, and the "when no server exists, tell Claude to WRITE one" prompt + a scope-it/read-only-first safety callout → **UPGRADE 3 SUB AGENTS**: a real `.claude/agents/reviewer.md` frontmatter file (name/description/tools) + the fan-out prompt + the "goal not a script" rule → **UPGRADE 4 THE LOOP**: a self-verify block for CLAUDE.md (audit claims vs tool results) + a real `.claude/settings.json` PostToolUse hook running lint+test → **THE OVERNIGHT RUN** (the one prompt that plans/splits/fixes/ships) → 5-row Plain-Claude-vs-the-System cmp table → honest notes (not a different model; sub agents cost more tokens; start read only; features move fast). Build script `build_ball_doc.js` in scratchpad — **rebuilt the premium helper set from scratch** (kicker/h1/body/bullet/promptBox/callout/cmp/rule/footer) because prior sessions' scripts live in expired scratchpads; reuse THIS one going forward. VERIFIED: document.xml + footer1.xml well-formed, 162 paras / 16 tables, ZERO em/en dashes, key strings present. Caption `BALL - caption.txt` (comment-first). See [[pokeball-factory-log]].

**STILL MISSING (offered to build):** BRAIN (reel 17 second brain), SWAP (reel 18 GLM, content ready in [[glm-swap-reel-and-setup]]: the settings.json env block), COUNCIL (scripted reel).

⚠️ Em-dash rule ([[no-em-dashes-in-copy]]): the DESIGN/SEO/SITE docs were built BEFORE the rule and still contain em dashes (—) in step labels etc.; BOOKS is the first fully em-dash-free doc. If Alex reuses the older three, grep + strip their "—" first.

**⭐ BUILD METHOD (this Mac has NO LibreOffice/Word, and the docx-skill validator needs Python ≥3.10 — system is 3.9):**
1. `npm install -g docx` (global root: `/Users/alexchensmacmini/.local/lib/node_modules`); require it from there in a Node script.
2. Reusable premium style (matches Alex's editorial brand): US-Letter, default Arial body; **Georgia** serif title/H1 (≈ Fraunces), **clay #C2603C** kicker/accents/rules, ink #1A1813, slate #3A5C84 subtitle; copy-paste **prompt boxes = a 1×1 shaded table** (fill #F6F3EC, thick clay LEFT border, Consolas mono lines, an uppercase clay label); 2-col comparison tables (dark #1A1813 header) + a clay-left-border **callout** ("Why this replaces the $X agency"); footer with page number + "Follow for more Claude workflows". Smart quotes in content. Helper fns (titleBlock/h1/kicker/bullet/promptBox/cmp/callout/footerCTA/mkDoc) — see the build scripts in this session's scratchpad (`build_design_doc.js`, `build_two_docs.js`).
3. **VERIFY without Word:** (a) unzip + `xml.etree` parse `word/document.xml`+`footer1.xml` for well-formedness + grep key strings + count paras/tables; (b) write a matching **HTML mirror** and screenshot it with headless Chrome (`--force-device-scale-factor=2`, [[screen-reel-github-capture]] method) to eyeball the layout. Both passed for all three docs.
4. Content rule: deliver EXACTLY what the reel's VO promised (read `src/data/words_<reel>.json` for the exact claims + CTA keyword), keep it practical + honest (e.g. SEO: note web-search/paste-competitor-pages for real data; SITE: deploy via Vercel/Netlify). Footers currently say generic "Follow for more" — swap in Alex's @ when he gives it. Offer PDF export (needs LibreOffice or HTML->print) if he wants to DM PDFs instead of .docx.

- `SHARP - 3 Fixes for Sharper Fable 5 Answers.docx` (reel 23 SHARP, keyword SHARP, built 2026-07-03) — cover title "The Fable 5 Guide" (matches the reel's guide-cover CTA), subtitle "3 fixes for sharper answers". The 3 copy-paste fixes: (1) GIVE IT AN EXAMPLE = a few-shot "here's an example of exactly what I want... now match it" template + the "ask Claude to draft one, fix once, reuse as template" fallback; (2) LEAD WITH THE ANSWER = the paste line grounded in Anthropic's real Fable 5 "lead with the outcome / TLDR first" guidance; (3) GIVE IT A GOAL NOT A SCRIPT = goal + "done when" template, grounded in Fable-5-follows-literally + old prescriptive prompts "can degrade output quality". Plus an old-model-vs-Fable-5 cmp table + "why these three work" callout + an HONEST NOTE that frames the reel's "90% worse" as illustrative (no published %). ZERO em/en dashes (grep-verified source + rendered docx). Build script `build_sharp_doc.js` + mirror `sharp_doc_mirror.html` in scratchpad. Distinct from the FABLE doc (that's the effort/verify/memory guide; this is the example/lead-with-answer/goal guide).

- `UNLOCK - The Fable 5 Playbook.docx` (reel 24 UNLOCK, keyword UNLOCK, built 2026-07-04) — delivers the reel's "all NINE prompts" promise: the 3 named in the reel (1 boundary line, 2 rules-first stack, 3 cold-read check) + 6 more (4 goal swap, 5 prove-it line = the VERBATIM gold Anthropic "audit each claim against a tool result" block, 6 answer-first line, 7 example anchor, 8 effort dial w/ /effort + output_config.effort + the "often exceed xhigh on prior models" quote, 9 memory file CLAUDE.md starter). Every move = kicker + name + 1-2 line why + a paste box; closes with "How the nine fit together" (Steer/Sharpen/Compound triads) + callout + honest notes citing platform.claude.com. Overlaps intentionally with FABLE/SHARP docs (same grounded research, different packaging per keyword). ZERO em/en dashes (verified source + rendered). Build script `build_unlock_doc.js` + mirror `unlock_doc_mirror.html` in scratchpad. STILL PENDING: ATTACK doc (reel 25 "The Attack Guide").
