---
name: fable-idea-backlog
description: ranked backlog of the next Fable 5 reel ideas — RE-RANKED 2026-07-09 by the VAULT-autopsy kill-gate (XRAY
metadata: 
  node_type: memory
  type: project
  originSessionId: f882962d-28c5-45a7-9d94-823ce95b54b2
---

⛔ SOURCING CHANGE (Alex, 2026-07-09, GHOST kill): topics come from OUTLIER DATA FIRST (the Outlier Engine sweep / proven overperformers) with wide consumer TAM — the backlog is secondary and every backlog idea needs an outlier comp attached before entering the factory. NEW #1 candidate: ERASE (digital-footprint wipe — modeled on the proven erase-footprint comp itself, universal TAM, 8/10 HARSH (1 qualifier: US resident — corrected at ERASE gate run 1)). Backlog RE-RANKED after the reel-38 VAULT failure (5s avg) through the 9 pre-build kill-rules in [[vault-reel-premise-autopsy]]. Original panel scores kept for reference. ⛔ Every one of these must still pass the kill-rules AT SCRIPT TIME, logged pass/fail, before recording.

1. **XRAY — ✅ BUILT & SHIPPED (reel 45, 2026-07-11; money-lane pivot off GREEN).** "Fable reads your whole business at once and finds real money leaking out of it." Cleanest gate pass: input already exists (their files), 1 hop, payoff = found DOLLARS with receipts (Saraev lane), pays now, universal stake. Line 1 leads with a real dollar number ("AI just found $1,140 you're losing"), NEVER a deadline. ⚠️ Cautions: "Money Map" trips the cerebral grep on "map" — the proof-shot + line 1 must lead with a dollar number + receipt on a real-reading screen by second 10; the map graphic is never the hero. CTA XRAY.
2. **GHOST — ⛔ KILLED BY ALEX 2026-07-09** (script was fully gated SHIP=true, then killed at the premise level: audience too narrow — business/CRM-adjacent "money went silent in your inbox" stake; wide-TAM mandate. LESSON: the factory's 8/10 audience score used the GENEROUS reading; the harsh reading (6) was correct. Harsh reading is now the binding one. Do not resurrect without a truly consumer-universal input.)
3. **HOLSTER — ⚠️ SAME NARROWNESS CLASS AS GHOST (pitches/proposals = business owners). Do NOT build without a wide-TAM reframe passing the HARSH audience reading ≥8.** Original notes: (a) anchor the input to the sent-mail/proposals folder (never "assemble your won pitches"); (b) the on-screen payoff = the actual pitch text FIRING + a reply receipt, never the "distilled playbook" (VAULT-DNA trace). Line 1 names the 90-second fired pitch. CTA HOLSTER.
4. **BLACKBOX — FIX-ANGLE-FIRST (do NOT record the existing script).** As pitched = VAULT twin (simulation → insight-file about risks; no receipt possible; future-only value). Only survivable reframe: **red-team-with-receipts** — Fable attacks your REAL files and the headline is one found kill-shot NUMBER from real data ("your top client is 61% of your revenue"); counter-moves file demoted to a closing kicker. If it can't produce a real on-screen receipt from real files, kill. (The gated script written 2026-07-09 is RETRACTED pending this reframe.)
5. **CODEX — KILLED.** Full VAULT failure DNA (brain-file payoff, invisible artifact, input-doesn't-exist, 4 hops, future-only value, CLONE+VAULT restitch). The gate exists to catch exactly this shape. Do not resurrect without a receipts-first reframe that is effectively a different idea.


---
**2026-07-10 factory run (reel 41):** **SLASH** (household bills overpay → Fable writes the retention call; CTA SLASH) entered via the 2026-07-09 outlier sweep (ramitsethi money-anxiety cluster, Door-A YES), beat REFUND and MONEY at the Stage-1 kill-gate (harsh 8), and passed the full adversarial gate on round 3 (all six dims ≥8, zero blockers). Factory log: [[slash-factory-log]]. Awaiting Alex VO. XRAY (39) remains gated + unrecorded — it is now the BACKUP, not the next build.

---
**2026-07-11 CUTTING-EDGE SLATE v2 (next5-v2.js — Alex rejected the v1 consumer-gimmick slate below as "completely outdated workflows"). Re-sourced on FRESH bleeding-edge signal (IndyDevDan/AI Jason/David Ondrej/Matthew Berman, pulled 7/11): agentic engineering = Ralph loops/harness, self-improving + proactive agents, multi-agent orchestration/observability, copy-the-pros-rig, model drama. Builder/founder lane.**
1. **GREEN** (clean PASS, aud 8, keyword GREEN) — one command, walk away, Claude LOOPS on a real failing test suite and self-corrects until the whole board turns green (Ralph-loop/harness; AI Jason + IndyDevDan). Artifact: a red→green terminal pass board + "loop 12/inf" counter closing itself. Distinct from NIGHTSHIFT (scheduled) + LOOPS-01 (one-shot). STRONGEST.
2. **EVOLVE** (aud 8, keyword EVOLVE) — agent fails a task, rewrites its OWN instructions file, then nails the identical task on run 2 (self-improving; AI Jason "self-improving company"). Artifact: run-1 red → "+ learned: back up first" diff → run-2 green. MUST prove via run-2 result, not the note (VAULT trap).
3. **RADAR** (aud 7, keyword RADAR) — a red error appears and, with zero typing, the agent opens the PR that fixes it (PROACTIVE agents; channel never touched). Artifact: error surfaces → unprompted "opened PR #214" on one unbroken screen. Risk: "before you asked" is an absence — must show trigger→action or it looks like NIGHTSHIFT.
4. **GRID** (aud 7, keyword GRID) — one lead agent spawns 5, and you watch all 5 ship real PRs in a live terminal grid (multi-agent orchestration/observability; IndyDevDan cmux). Artifact: 5-pane grid, "5 running · 2 done", panes flip to green PRs. Must END on shipped PRs (vs SWARM/CREW/FACTORY collision).
5. **RIG** (aud 7, keyword RIG) — drop a top engineer's 4 .claude config files into a bare repo and the SAME model goes from stalling to shipping (copy-the-pros-setup; David Ondrej "just copy him"). Artifact: same task, bare repo red vs rig-installed green. Must carry the before/after delta (not a static folder tour; avoid IMPRINT 4-file-format collision).
CUT: DETHRONE (off-brand "pros banned Fable" + SWAP dup), CHORUS (STACK dup, benchmark-bar not a real artifact).

---
**2026-07-11 OUTLIER SLATE v1 — ⛔ REJECTED BY ALEX as "completely outdated workflows" (consumer gimmicks). Kept only for the kill-record; do NOT resurrect GLOWUP/TALE/etc.** (next5-ideas.js sourcing panel — 8 candidates gated, top 5): all non-finance, structure-varied (rotation rule honored), novel vs the shipped 44 + IMPRINT(43)/HIRED(44).
1. **GLOWUP** (PASS, aud 10) — selfie → $300 studio headshot; consumer before/after transform (cindiezhu 6/23). MUST show REAL Claude directing an image MCP (not a headshot-app reel) + a photoreal on-model result. Keyword GLOWUP.
2. **RING** (PASS, aud 8) — Grok 4.5 vs Fable 5, same task, live scoreboard; head-to-head BATTLE (Greg Grok 7/10 + Nate GPT-vs-Fable 131k). Battle task MUST be VISUALLY-decodable (build a landing page / poster), objective on-screen scoring, let Grok win a round. Keyword RING.
3. **TALE** (PASS, aud 8) — one prompt → an illustrated storybook starring any kid you love; the ONE permitted spoken-prompt follow-along (Nate one-prompt-build 46k). Character consistency across pages is the promise — build one in-hand first. Keyword TALE.
4. **UNLEASHED** (KILL-as-written → reframe, aud 9) — cold-open reveal of a once-gated Claude/Fable capability now usable on the viewer's own input (cindiezhu "too dangerous → here" 11.5k, her biggest). CONTINGENT: name ONE concrete on-brand Claude capability shown as a real artifact changing state <2s; a photo-edit = off-brand KILL. Keyword UNLEASHED.
5. **LAB** (KILL-as-written → reframe, aud 8) — Claude fact-checks 50 sources → ONE bold TRUE/FALSE myth-bust verdict; reaction structure (Nate Stanford research-team 54k). CONTINGENT: must resolve to a single mute-readable verdict/number, not a cited text report; re-gate vs Council. Keyword LAB.
CUT: PRETENDER (2nd battle, text-vs-text, aud 6), CANCEL (double-gated business+desktop, aud 7), INSIDER (text-vs-text prompt-craft = killed STANDARD shape, aud 6).

**KILL-LIST additions (2026-07-10):**
- REFUND (medical-bill error audit) — KILLED, rule 8 (harsh audience 6: must hold a medical bill + US cost-sharing billing system). Strong receipt; resurrect only with a non-US-gated document (e.g. generic overcharge lane) that scores harsh ≥8.
- MONEY (bank-statement subscription cancel list) — KILLED, collision + innovation bar (near-clone of ERASE's find→rank→cancel→monitor spine; overlaps XRAY/BOOKS; Rocket-Money-class apps already do it).

---
**2026-07-11 factory run (reel 47): GRID** (1 command → lead agent spawns 4 → live 5-pane grid ships 5 real tasks; CTA GRID) — cutting-edge slate v2 pick after ⛔ GREEN DEFERRED (collision: the as-built EVOLVE "Forge Loop" absorbed loop-until-passes; revisit GREEN ≥2 reels after EVOLVE posts, reframed on the real-terminal-capture angle). 4 gate runs → terminal 8.5·9·8·8·5·9, zero blockers; topic-breadth 5 = builder-lane arithmetic ceiling (standing override). Factory log: [[grid-factory-log]]. Evidence: memory/reels/evidence/grid-comps.md + 4 builder channels appended to the outlier watchlist. Awaiting Alex: hook pick (A/B) + override confirm; then Stage 4.5 capture + BUILD-CONTRACT + Stage 6 storyboard.

---
**2026-07-12 SWEEP (two-pass, reel #52 sourcing) — full record: `outlier-engine/runs/2026-07-12/NEXT-BATCH.md`.** YT sweep (scan.py, 9 transcribed comps) = **9/9 KILL** — the watchlist over-indexes on founder/commentator channels (Greg/Berman/Ondrej/Ramit) whose host/news-driven wins don't transfer + collide with shipped agent/config/FB-ads reels. Alex-directed rescue on the transferable **consumer IG lane** (see [[outlier-transcript-tooling]]): scraped cindiezhu + raycfu (nateherk IG dormant), transcribed 7, gated → **1 ship-ready pick:**
- ⭐ **#52 CALLBACK** (aud 8) — "Claude rewrites your resume to beat the ATS bot that auto-rejects you (41%→96% match)." Source = cindiezhu RESUME comp (**1.1M breakout**, her biggest; RESUME lane un-mined by Alex). Structure = COLD-OPEN SINGLE-HERO ARTIFACT REVEAL (the sanctioned rotation target after 49/50/51 = listicle/follow-along/listicle). Hero artifact = **The ATS Scorecard**: one vague bullet reflowing into a quantified Google-XYZ line + REJECTED→INTERVIEW flip (the gauge is decoration, the bullet reflow is the <2s hero). Keyword CALLBACK clean vs all 51 shipped + backlog. Risks: keep value in the in-hand rewritten resume (not the off-screen job), keep TAM universal (aud 8 = floor). Awaiting Alex go → Stage 1 re-gate at script time → Stage 2 (inherit cindiezhu persona/reveal structure) → build.
- KILLED this sweep: 15 comps total (YT 9 + IG 6). IG kills: scroll-animations & recreate-website (SWIPE-50 dup), agentos/oneperson/portal/quant (raycfu business-context, aud 3-5, collide with NIGHTSHIFT/GRID/EVOLVE/GHOST/LEADS). Next-sweep sourcing: prioritize universal-consumer-input comps (photos/inbox/bills/life-documents) over raycfu builder premises which keep failing R8.

---
**2026-07-15 — ⛔ SKETCH (reel 59) KILLED BY ALEX ON INSTINCT ("i feel like the sketch idea is outdated"). He was right, and two workflow holes let it get to gate round 3.**
Premise: "photo of a napkin sketch → a real working app" (rileybrown awe-demo lane, keyword SKETCH).

**Why it's dead — the reveal expired in 2023.** Sketch→app was **GPT-4V's March 2023 launch demo**; tldraw "make it real" went viral on it that Nov. It is arguably the most-replicated AI demo ever made. The hook's "No way" asks for a reaction the audience already had three years ago. ⛔ **Do not resurrect the sketch angle** — the sketch is the only thing that made it distinct AND the only stale thing in it; strip it and it's a generic vibe-code deploy reel (= SWIPE-50's lane).

**HOLE 1 — it entered through the BANNED door and I mislabeled it.** Source = `script-style-replicator/topic-ideas/rileybrown.md` E14, a file whose own header reads **"HYPOTHESES"**. No comp, never had one. I logged it `door:A/replicator`. That is *VAULT's door* — the exact door that produced the 5s-avg failure. **A3's arena crowning it 8.5 unanimous is what made the mislabel feel earned**: the arena ranks candidates against each other, so a field of comp-less hypotheses still yields a confident winner. Rank ≠ evidence. → playbook A1 now: cite the comp or the door is fake; topic-ideas = Door C only; an arena win is not a comp.

**HOLE 2 — nothing downstream could catch staleness.** The playbook had **no freshness rule at all** (I quoted one that didn't exist), and **GATE A's six dimensions contain no novelty axis** — three critic rounds physically could not fail it for being old. The freshness check existed only *inside the topic file*, self-graded by the agent that wrote the idea, and it passed itself via a caveat: *"genuinely frontier **and rarely shown end-to-end**"* — then put the weight on the hook, i.e. **the wow rode the stale half and the fresh half (deploy → live link) was the afterthought.** → playbook A2 now has **rule 10 RERUN TEST**, incl. the caveat-tell.

⭐ **The diagnostic lesson.** GATE A rounds 1-3 kept reporting *"the hook has nothing left to reveal"* / *"the header already answered the only thing 'No way' could point at"*, and I kept re-writing wording (planting loops, moving the gate). **The critics were describing the symptom; the premise was the disease.** When a gate fails the same beat 3 rounds running on different wording each time, stop patching and re-open the premise. Alex diagnosed from the couch what 3 rounds of critics couldn't, because the scorecard had no box for it.

⭐ **The pattern that actually predicts a ship:** CALLBACK — the only clean GATE A pass all session (all six ≥8, no override) — came from **real outlier data** (cindiezhu resume 1.1M + raycfu resume 7.9x). SKETCH came from a brainstorm with an arena trophy. **Comps ship; hypotheses burn rounds.**

---
# ⛔ 2026-07-15 WIDE SWEEP (reel 59 re-source) — 4 candidates, 4 KILLS, winner NONE
Full record: `outlier-engine/runs/2026-07-15/NEXT-BATCH.md`. Sourced via the new **`ig_scan.py`** (private JSON API,
no browser — Alex's idea, and it changed the outcome: it surfaced **mavgpt**, who was MISSING from `watchlist.txt`
despite us having `mavgpt-dna.md` all along. 1.04M followers, 254K median, outliers at 4.6M/18.8x, and every Door-A
hit is a comment-keyword lead-magnet = our exact mechanic. **His vein maps 1:1 onto our shipped list**
(Resume→CALLBACK-58, Identity→ERASE-40, Stack→STACK-30) — which is why he predicts what to build next.)
⭐ His **"Resume" reel (1.12M, 4.53x)** independently validates CALLBACK: different creator, same fortnight, and his
caption names *"the Google XYZ formula"* + *"act as an applicant tracking system"* — CALLBACK converged on his
winning formula without ever seeing it.

**KILLED — do not re-litigate these multiples:**
- ⛔ **PHOTO** (mavgpt 4.66M / **18.84x** / 7.1d — the biggest multiple we have ever sourced). **No Claude recast
  exists**: the comp's first prompt asks the model to identify a person from a photo — **Claude refuses by design +
  Anthropic's AUP prohibits facial recognition / tracking individuals.** Fails at step one → the proof shot could
  only be faked. **The first premise killed for being UNCAPTURABLE rather than unpersuasive.** Also pays in DREAD
  (the comp's own peak line: *"the results were actually terrifying"*), and the half that PAYS (takedown requests)
  is our own reel 40 ERASE. ⭐ And the deep reason it can never work here: **a resume is a FORMAT — a mock
  substitutes for free, which is why CALLBACK works. A face is the one artifact that CANNOT be substituted, because
  its whole charge is that it is yours.** Alex can't use his own (faceless), a stranger's (that IS the harm), or the
  mascot's (a cartoon being cartoon-stalked = VAULT-38's death in a costume). No third option exists.
- ⛔ **HUMAN** (mavgpt 2.74M / 11.06x). **Ethics/brand, binding alone:** "0% AI detected" is a cheating tool in its
  dominant use. *Alex's channel says "Claude does real work for you." This says "Claude hides that no work was done."*
  Recruits a student-cheating cohort that converts to nothing. Not rescuable — the reel HAS to promise
  detector-evasion to work. Also R7 hard-collides with CALLBACK (shipped the same day: hostile bot + gauge flip),
  R10 (2023 humanizer wave), and R5 ("0% on every detector" cannot survive being checked — detectors disagree by
  design; the top comment would be a red screenshot).
- ⛔ **SCROLL** (cindiezhu 518K / 6.81x / 3.4d — a TRUE topic outlier, her 7 sibling reels all sub-median). **The wow
  is frame interpolation = a video-gen model, which Claude does not have.** Claude's only role is step 4's canvas
  boilerplate — least magical beat, and the one that costs −3. R3 owns-nothing (step 1 = "generate two images"),
  5-6 hops across 3 tools, aud 3. ⛔ Do NOT park the Flow angle: the interpolation is simultaneously the only thing
  that wows and the only thing Claude cannot do. *(Its R10 PASSES — it dies on brand/breadth, not staleness.)*
- ⛔ **FOREMAN (reel 52) — KILLED 2026-07-15, Alex accepted.** See [[foreman-factory-log]]. R10 retroactively voids
  both overrides. ⛔ **Do not resurrect the overnight framing** — like SKETCH's napkin, "unattended all night" is
  simultaneously the only thing making the hook land and the only stale thing in it; strip it and you have
  NIGHTSHIFT-19. **SALVAGE = the TRIPWIRE only:** *"Claude re-checks every morning and catches the thing you shipped
  three weeks ago, before you do."* Real capability (scheduled tasks), first-order want, payoff = a caught error (an
  artifact, not a proposition). It re-enters at A1 **with its own comp**; it does not ride in on FOREMAN's hook or
  inherit its gate score.
- Auto-rejected without a gate cycle: raycfu Daq51yAz64H (297K/6.18x, "Everything OpenAI just launched" — news
  roundup, **15 comments** = no keyword mechanic) · gregisenberg Dal9I7KRA1L (1.77M/**89.46x**, "Giant robots are
  coming?" — 6,214 shares vs 888 comments = share-driven news virality, not a lead-magnet).

⭐⭐ **THE LESSON (now playbook §A1.5, two PRE-CHECKS):** all four burned a full 3-critic gate (~350k tokens) on
things a 10-minute check catches. **(1) RECAST PRE-CHECK** — does the comp's FIRST prompt name a capability Claude
has, and will Claude run it? (kills PHOTO + SCROLL in a minute each). **(2) R10 PRE-CHECK** — name the year in the
sourcing row (kills FOREMAN + HUMAN). Run both BEFORE any scorecard exists.

⭐ **The pattern across all four:** every one arrived with a big multiple, and in every case **the multiple was real
and the mechanism was welded to something Alex does not have** — a face he won't show, a taboo he can't brand, a
video model Claude doesn't run, a build tutorial the gate forbids. **A comp's number tells you an audience reacted;
it does not tell you they reacted to the part you can keep.** And a fresh comp on a stale reveal means they reacted
to something other than novelty — ask what. *Transgression is not novelty; recognition is not discovery.*

⚠️ **The honest counter-risk (the head judge flagged it against itself):** a gate that kills everything is
indistinguishable from a broken one. The defense is that these are four DIFFERENT independently-checkable mechanisms,
not one rule fired four times, and CALLBACK cleared this same bar cleanly on 07-15 with zero overrides. **If the NEXT
sweep also returns all-kills, escalate: rules 8 and 10 are over-tuned and need re-calibration, not more obedience.**
