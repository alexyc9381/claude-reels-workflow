---
name: ivy-insider-pipeline
description: "Green ported \"collegetierlist\" ranked-carousel generator for Ivy.insider at ~/Downloads/ivy-insider"
metadata: 
  node_type: memory
  type: project
  originSessionId: 89b0299f-dd48-436f-9e53-4720fa2f7dac
---

**ACCOUNT RENAMED: "College Insider" (@collegeinsider)** — was "Ivy.insider". With the rename, content is **NO LONGER Ivy-only**: it branches to **"Ivy+" elite colleges** (the 20 crests: 8 Ivies + Stanford/MIT/Caltech/UChicago/Duke/Northwestern/Vanderbilt/Notre Dame/Georgetown/Berkeley/Michigan/UVA). (The earlier "Ivy League ONLY" rule is superseded.)

**Post conventions (locked):** every slide has a **"PRESENTED BY MATCHTERN"** top-center banner (Matchtern in green accent); every post ENDS with the **RarePeer promo card** as the final page — it's `~/Downloads/RAREPEER FINAL CARD.png` (2160×2700, "View 1,000+ Admits FREE / RarePeer.com / powered by Matchtern"), copied to `ivy-insider/assets/rarepeer-promo.png` and appended verbatim by `make_posts.py`.

**Content rules (locked, Alex 2026-07-01):**
1. **≥15 colleges per post** — no more 5- or 10-college posts; ranked/countdown/tier posts must feature at least 15 schools (15/20 etc.). Needs a bigger logo pool (fetching ~30 elite college crests).
2. **Titles must be self-explanatory** — always say "Colleges" or "Universities" + the count. NOT "5 Best for Pre-Med" (meaningless); YES "The 15 Best Colleges for Pre-Med", "The 20 Hardest Colleges to Get Into".
4. **Cover headlines (FINAL spec, dialed in over many rounds):** use the **FULL title text, NOT shortened** (shortening breaks the meaning) with the key phrase in `**green accent**`; **CENTERED** (text-align:center, not left); size `fs = 106/98/90/82/74` by title char length (<=22/<=32/<=40/<=48/else); **line-height 1.3** (Alex wants generous vertical space between lines); positioned **near the BOTTOM** (`bottom:92px`), natural word-wrap; bottom-heavy dark gradient (`rgba(6,20,13,0) 26% → .5 56% → .9 100%`). History of flags: too small→bigger, too big+too high+left-aligned+shortened→wrong. This spec is what Alex approved.

3. **Caption in every post subfolder** — write a `caption.txt` in the collegetierlist style (format in `ivy-insider/_reference/feedback_caption_writing.md`: hook "Stop scrolling!" → tiered callouts (Top 5 / 6–10 / 11–15 / 16–20) → "The real takeaway" tying to Matchtern → **RarePeer funnel CTA** (NOT "comment X" for ranking posts) → 3 "." spacers → hashtag line incl. #matchtern) and drop it inside the post's Drive subfolder.

**TWO SEPARATE ACCOUNTS share this pipeline (same template/watermark/RarePeer promo, DIFFERENT color + DIFFERENT topics):**
- **College Insider** — DARK green (default palette), broad "Top 15-20 Colleges for X" rankings. Drive: `03_College Insider/Batch N/`.
- **ivy.insider** — LIGHTER green, prestigious universities (can go beyond the Ivies, not Ivy-only), but its topics must be DISTINCT from College Insider's (no reusing dorms/millionaires/wall-street/etc.). e.g. Consulting, Fortune 500 CEOs, Politicians, Engineering, Undergrad Business, Party Schools, D1 Athletes, Greek Life, Study Abroad, Social Life, Weather, Celebrities' Kids, International Students, Loyal Alumni, Overrated/Hype. Drive: `Ivy Insider/Batch N/` (own top-level folder in Social Media). Batch 1 = 16 posts done.
- **Theme switch:** `make_posts.py` colors are env-overridable — render ivy.insider with `IVY_IVY=#1F9D5C IVY_ACCENT=#63E6A0 IVY_BG=#17552F IVY_ROWCELL=#21714C IVY_OV=28,92,60 python3 make_posts.py <posts.json> <outdir>`. Omit the env vars for the dark College Insider palette.
- **Blotato → TikTok drafts (SET UP + WORKING):** posting script is `~/Downloads/social-post/post.mjs` (Node, uses global fetch) + `.env` (BLOTATO_API_KEY + account ids). **Connected TikTok accounts (verify via GET `/v2/users/me/accounts`): 49136=@ivy.insider, 49132=@collegetierlist, 49128=@matchtern.** `.env`: `BLOTATO_ACCOUNT_IVY=49136` (ivy.insider — was wrongly 49132/collegetierlist at first, FIXED), `BLOTATO_ACCOUNT_COLLEGETIERLIST=49132`, `BLOTATO_ACCOUNT_MATCHTERN=49128`. Always confirm the target account id with `--dry` before a live push. It uploads local slides to Blotato (`/v2/media/uploads` presigned PUT, no public hosting needed) then `POST /v2/posts`. Run per post: `node post.mjs --brand=ivy --dir="<post folder>" --caption-file="<cap>.txt" --draft` → pushes to TikTok **inbox/drafts** (isDraft:true, autoAddMusic off; add sound + post in-app). `--brand` = ivy|matchtern. Flags: `--now`/`--schedule=+15m`/`--dry`/`--cover=N`. **TikTok photo posts have a SEPARATE `title` field** (target.title, <90 chars) distinct from the caption `content.text`. If you don't set it, TikTok defaults the title to the caption body → title and caption show the SAME text (Alex flagged this). FIX: post.mjs now takes `--title="<post title>"` → sets `target.title`; always pass it (the post's real title). **TikTok caption limit = 2200 chars** — the full collegetierlist captions are longer, so trim for TikTok (hook + Top 5 + "swipe for the full ranking" + takeaway + RarePeer CTA + hashtags); keep the full caption in Drive for IG. Pushed all 16 ivy.insider Batch 1 posts to TikTok drafts on 2026-07-02 (16/16 ok).

**Drive location:** College Insider posts go in `…/Social Media/03_College Insider/` (its OWN top-level folder, not inside 02_Collegetierlist). **Each BATCH gets its own subfolder** `Batch 1/`, `Batch 2/`, … and inside that, one titled subfolder per post with slides `1.png..N.png` + `caption.txt`. (Batch 1 = the first 10; Batch 2 = the 20 with dorms/millionaires/etc.) The render pipeline is `make_posts.py <posts.json> <out-subdir>`; a batch = a posts JSON + a `_captionsN.json`.

Because Ivy-only would cap at 8 schools, the multiples-of-5 list format BRANCHES. Two Ivy-only pillars: (a) **Ivy League Tier List** — all 8 Ivies into S/A/B/C tiers, one flagship post (`ivy_tierlist.py` → `out/tier_*.png`); (b) **"The 5 Best Ivies for {topic}"** — rank 5 of the 8 Ivies for Finance/CS/Pre-Med/Campus Life/etc. (uses `ivy_carousel.py` with 5 schools = multiple of 5). The base mechanism is still the **"Top N {category}" ranked-list carousel** (dark iconic-photo cover + dashed-row ranked list, e.g. "20 Colleges With the Worst Dorms", "Top 20 Best Business Schools"). 4:5 portrait (1080×1350 @2x).

**Authoritative source of the workflow:** the claude.ai cloud session **"Collegeresultslist Social Media Posts"** (`https://claude.ai/code/session_01687oXeoHZup7XUz5biEP3M`), which ran on Alex's OTHER machine (user `allyy`). That "collegetierlist" workflow = a set of Python (Playwright) carousel generators that replicate Matchtern's Canva brand templates. Retrieved them via the Chrome extension: navigated to the session, fetched `claude.ai/v1/code/sessions/<id>/events` (paginated, 3467 events), bundled the written files, and downloaded them locally. **The originals are saved verbatim in `~/Downloads/ivy-insider/_reference/`:**
- `matchtern_carousel_tierlist.py` ← the one for the dark photo ranked-school posts (THE format Alex wanted).
- `matchtern_carousel_tier.py` = S-F tier-list TABLE format (square 1080²). `matchtern_carousel_value.py` = internships/"resources" format. `matchtern_carousel_thenvsnow.py` = then-vs-now. Plus `caption.txt` examples + `feedback_caption_writing.md`.

Brand: font **Garet** (Garet-Book/Heavy .otf), accent BLUE `#2C99FE`, navy bg, grad-cap badge PNG, logos in `tierlist_assets/logos/`.

**The port:** `~/Downloads/ivy-insider/ivy_carousel.py` mirrors `matchtern_carousel_tierlist.py` exactly (same layout constants, gradients, dashed rows pitch 224 rh182/gap42, headline `**`-accent parser, auto-paginate per_slide=5), re-skinned: accent→green `#2ECC71`, bg→deep forest, blue grad-cap PNG→inline GREEN grad-cap badge, logos→`assets/crests/<slug>.svg` on white chips, **renders via headless Google Chrome (no Playwright needed)**. Font: uses Garet if `garet_font/*.otf` present, else **Poppins** stand-in (Garet NOT on this Mac yet — get it for true fidelity). Cover photos live in `assets/covers/`; current pick = UChicago Rosenwald Hall gothic (`c-27276230.jpg`, Pexels) — generic modern building was rejected as "unappealing". Edit the `POST_IVY_RANKED` block + run `python3 ivy_carousel.py`. 20 elite crests/seals now in `assets/crests/` (8 Ivies + Stanford/MIT/Caltech/UChicago/Duke/Northwestern/Vanderbilt/Notre Dame/Georgetown/Berkeley/Michigan/UVA). Current sample = "The 20 Most Prestigious Universities". **Alex requirements (locked):** (1) school count must be a MULTIPLE OF 5 (5/10/15/20) so every list slide is full — no partial last slide (per_slide=5); (2) cover grad-cap badge goes TOP-RIGHT (not centered); cover headline larger (~94px) and lifted up (bottom:150) not jammed at bottom; (3) school logos must be TRANSPARENT (no white chip/background) directly on the dark rows — but dark/monochrome logos that vanish (brown, dartmouth, duke, georgetown) get a WHITE KNOCKOUT via `filter:brightness(0) invert(1)` (see `WHITE_LOGOS` set in ivy_carousel.py); (4) cover photo must be appealing/iconic (gothic), not a generic modern building.

Earlier WRONG attempts before retrieving the real workflow: a "Stats that got into X" stat-card (= Matchtern style, not wanted), a light green card-list, and a hand-rolled HTML version of the dark format. The authoritative answer is the ported `ivy_carousel.py`.

Delivery: [[social-assets-to-camera-roll]] + [[social-assets-to-gdrive]]. Always give Alex clickable file links per [[share-images-as-links]].
