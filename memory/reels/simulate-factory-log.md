# SIMULATE — factory log (reel 62)

> ⛔ **NUMBERING CORRECTION (2026-07-18):** this log said "reel 62" throughout, but **62 is TAKES**
> (already delivered in Drive). SIMULATE shipped as **reel 65** -> `Claude Reels/65 - SIMULATE/`.
> Treat every "reel 62" below as reel 65.


> ⛔ Opened STAGE 0, 2026-07-17 per [[factory-log-first]]. Alex picked SIMULATION from the sharp-systems slate and
> directed: "frame it more around a simulation workflow / process." In-lane ([[raycfu-lane-preferred]]).

## STAGE 0 — SOURCE GATE
**door:** B (Alex-directed → no velocity verification required) + proven-system grounding.
**structure_comp:** raycfu "build a Claude [system]" format — `DXqSBt0D0UE` LLM council (245k/4.88x/**4.42%**) +
`DZqXF-1vPzg` dev-team (550k/10.94x/2.57%) + `DamEcWuPvX0` agentic-OS. This is the proven "how to build a Claude
[workflow]" title/beat structure Alex likes.
**mechanic grounding:** SIMULATION is a documented *winning-formula passing system* ([[reel-winning-formula]]:
"SIMULATION — simulate the future / pre-mortem / role-play the market before you commit"), derived from Alex's own
winners. ⚠️ **HONESTY: no direct velocity comp for "simulation" specifically** — it rides Door B + the raycfu
build-a-system structure + the proven-system pattern, NOT a scanned outlier. Flagged, not hidden.

**transfer_hypothesis:** the wow is **watching your own plan BREAK before you live it** — the failure plays out on
screen (drama/retention), and the payoff is concrete (the exact point it cracks + the change that survives), not
cerebral "insight." Framing it as a repeatable WORKFLOW/ENGINE (build once, run on every decision) is the raycfu
builder framing + gives the comment-gate a real deliverable (the workflow + prompts).

## ⛔ STAGE 1 — FAST PRE-CHECK
- **R8:** down-weighted (builder lane) — and "simulate a big decision before you make it" is broad (everyone decides).
- **Innovation-bar: PASS.** SIMULATION is on the passing-systems list. Non-obvious.
- **R10: PASS.** Monte-Carlo/decision-modeling is old, but *"a Claude workflow that plays your specific decision
  forward with the players/market and shows the exact week it breaks"* is a fresh agentic framing (2026), not a
  2023 rerun. No shipped simulation reel to collide with.
- **⚠️ R2-cerebral watch (the real risk): mitigated in the draft.** "A simulation / what breaks" can read as analysis.
  Fix built in: payoff = a WATCHABLE break sequence (month-1 fine → month-3 cracks → the exact week it falls apart) +
  a concrete verdict ("the one change that survives"), framed as *fail on paper first, for free* — visceral, not insight.
- **Collision: differentiate from WORLDLOAD (cerebral-flagged, fuzzy "load the world")** — SIMULATE is concrete: run
  a specific decision forward and watch it break. And from AUTOPILOT (FB-ads, flopped). Clear.
- **Capability/honesty: REAL + capturable** (Claude runs structured pre-mortems/role-play/scenario forecasts); no
  fabricated number in the VO. Capture a real decision-sim for the visual at 4.5.
- **Lever:** none → cooldown-exempt.

**PRE-CHECK VERDICT: PASS (Door B, in-lane, proven system, R2 mitigated). Drafting.**

## STAGE 2 — STRUCTURE
raycfu build-a-system spine. **target_seconds 38 (≤163 words).** Beats: workflow stun-claim → the machine (describe
decision → builds situation/players → plays it forward) → the break spectacle (escalating failure) → verdict (exact
crack + the surviving change) → which-means (fail on paper free) → gated CTA hard-cut on SIMULATE.

## STAGE 3 — DRAFT
**keyword:** SIMULATE. **words:** ~166 (~40s, raycfu band). No em dashes ✓ · no negation ✓ · hard-cut ✓.
**LOCKED HOOK (Alex-directed, raycfu insider-gap family):** *"Most people don't realize you can make Claude run your
biggest decisions as full simulations, and watch the exact moment they fall apart, before you ever make them for real."*
Breadth NOT flagged per [[raycfu-lane-preferred]] (Alex's lane). Mute-header to burn: "watch your decision FAIL —
before you make it."
**Ladder:** L1 = "the simulation workflow I run every big decision through before I make it" + sim-running visual ·
L2 = how do you simulate a decision? · L3 = the break sequence (escalating) · L4 = "fail on paper first, run only the
version that lived" (non-obvious; workflow gated). Hooks: 10 drafted, top 3 below. Gate-the-how: show the sim + result;
gate the full workflow + prompts behind SIMULATE. **TRUTH-REQUIRED:** capture a real decision-simulation at 4.5.

## ⭐ VO v4 — IDEA-FOCUSED + MIDDLE-SCHOOL CLARITY (Alex 2026-07-17: "the break part doesn't make sense; write it for a middle schooler")
Idea-focused (v3) + plain-English rewrite: short words, clear cause→effect chain in the break, no fuzzy phrases
("sets off three more" → "causes another, and another"). **LOCKED VO:**
> **[HOOK]** Most people don't realize you can make Claude run any idea you have as a full simulation, and watch the
> exact moment it falls apart, before you ever try it in real life.
> **[REVEAL]** It works like a machine. You give it your idea, and Claude builds the whole world around it: the people,
> how they react, everything that could go wrong. Then it plays the whole thing out, start to finish.
> **[BREAK]** And you get to watch it play out. At first, everything looks great. Then one problem you didn't see
> coming shows up, and it causes another, and another, until the whole thing falls apart right in front of you. Then
> Claude runs it again and again, so you can catch every way it could go wrong.
> **[VERDICT]** What you get back isn't a guess. It's the exact thing that broke your idea, and the one change that
> makes it work.
> **[WHICH-MEANS]** So instead of just going for it and hoping, you get to fail on paper first, for free, and only go
> with the plan that actually works.
> **[CTA]** I'll send you the full workflow and the prompts. Comment SIMULATE.
⛔ Storyboard S2 icons updated to match: swap life-upheaval props (moving-truck/ring/diploma) → **IDEA icons**
(lightbulb / rocket-launch / blueprint / strategy-chart). The orb-world = your IDEA playing out, not a life move.

## STAGE 6 — STORYBOARD: authored → `claude-reels-workflow/storyboards/62-simulate.md`.

## STAGE 7 — BUILD (in progress, 2026-07-17)
- ✅ **VO processed:** Alex's `SIMULATE.m4a` → 3 "cut cut" retakes spliced out (A keep-2nd, B keep-1st-take,
  C drop dead-pause), 6 inter-beat gaps capped, VO-polished + loudnorm → **`public/vo_simulate.wav` (48kHz, 42.1s)**.
  Transcript verified = full script, 0 flubs. (whisper hears "Claude"→"Cloud" — fixed in words json.)
- ✅ **Timing:** `src/data/words_simulate.json` (185 words, {word,start,end,line}), `L = [0.0, 6.78, 14.56, 21.72,
  25.62, 30.24]` (6 scenes), `CUT = 42.07` (1262f @30fps).
- ✅ **Chassis cloned:** `ClaudeSimulateReel.tsx` from `ClaudeFactoryReel.tsx` (single-panel, 6 scenes = exact match);
  swapped words/L/CUT/kw="SIMULATE"/VO src/component name; registered in Root (both `claude-reels-workflow/video` +
  `matchtern-longform/video` = the one with Remotion installed).
- ✅ **WIREFRAME RENDERS** (frame 400): chassis compiles, VO + captions + timing all correctly synced ("the whole
  thing" lands at 13.3s). Foundation verified.
- ✅ **6 CINEMATIC SCENES AUTHORED** (ART-DIRECTORS + CODERS workflow, 12 agents): all Sol bodies replaced with the
  Architect + orb-world scenes per `62-simulate.md` + the CINEMATIC DIRECTION (deep-cosmos bg, parallax starfields,
  god-rays, camera moves). Spliced, 0 render crashers, all 6 render clean.
- ✅ **CRITIC-FIX PASS on the 2 weak beats:** S4 (was 1 big orb → now the 40-orb constellation + RUN counter) and S5
  (was an abstract red block → now red-crack → green fix → heals GOLD + SURVIVED seal). Re-authored via a focused
  fix workflow, re-spliced, verified on stills.
- ✅ **v1 full render** OK (42.1s, 1080x1920, VO+music+SFX). **v2 rendering** (with fixed S4/S5).
- ⭐ RESULT: a genuine cinematic leg-up ([[reel-cinematic-legup]]) — S1 god+orb, S2 orb-city, S3 red-kaiju break,
  S4 40-orb rerun, S5 gold-heal verdict, S6 gold-orb "Comment SIMULATE" finale.
- ⛔ **v2 REJECTED by Alex: "too abstract, too godlike everywhere."** → full remake per [[reel-cinematic-legup]]
  CORRECTION: **colorful fantasy realm + mythical Claude-spirit CREATURES (no robots), god-view ONLY in the opening.**
- ✅ **v3 REMAKE (character-driven, colorful):** re-authored all 6 via workflow — S1 mythical Claude sun-god over a
  kingdom-orb · S2 bright town builds itself + happy spirits · S3 storm kingdom + shadow-beast + panicking creatures ·
  S4 2x2 grid of the kingdom failing 4 ways + RUN counter · S5 divine hand spotlights the cracked tower → heals gold ·
  S6 (re-authored solo after it shipped a VO-echo banner with NO CTA) colorful celebration + big clean **Comment
  SIMULATE** + unlock padlock. Nested each scene's helpers inside its FC (zero const collisions). 0 render crashers.
- ⏳ **NEXT:** full render v3 → delivery-encode (bt709 + faststart) → deliver Final. Optional: SFX densify
  ([[reel-sfx-pass]]) + more-vibrant S1 creature. Files: `ClaudeSimulateReel.tsx`, `out/SIMULATE_v3.mp4`,
  `public/vo_simulate.wav`; synced to `claude-reels-workflow/video`.
- ⭐ LESSON: "cinematic" for this account = COLORFUL CHARACTER THEATRE (Claude-spirit creatures, CALLBACK-style),
  NOT abstract cosmic god-orbs. And the CTA scene MUST end on a clear "Comment KEYWORD" money-shot (S6 v3 shipped
  without one — always verify the CTA keyword renders).

- ⛔ **v3 REJECTED: "characters don't follow the Claude sprite design; not detailed/interesting enough; no story
  arc or villain like CALLBACK; villain horrible; VO repeats the CTA line."**
- ✅ **VO RE-FIXED:** the CTA had TWO takes ("I'll send you the full workflow and prompts" x2). Removed the first
  take + "cut cut", kept only the complete 2nd take → single clean CTA. New **CUT=40.15s / 1204f**; L[5]=30.26.
- ✅ **v4 REMAKE (the one that landed):** used the REAL Claude **`<Mascot>` sprite** for every character (32 uses) —
  a **wizard-Claude** hero-mage + **citizen-Claudes** — plus a proper **recurring GREMLIN villain** (bat-ears,
  glowing acid-green eyes, jagged grin). Real **story arc**: wizard conjures the kingdom (S1) → it thrives (S2) →
  the gremlin attacks + citizens panic (S3) → 40-run "40 DISASTERS" grid, gremlin recurs (S4) → wizard SEALS the
  gremlin + kingdom heals (S5) → celebration + gremlin CAGED "CONTAINED" gag + **Comment SIMULATE** (S6). Dense,
  colorful, detailed. Nested helpers per scene, 0 crashers. **This is the ship candidate.**
- ⭐ LESSONS (v4): (1) "follow the Claude sprite design" = USE `<Mascot>`, not custom blobs. (2) needs a real
  RECURRING VILLAIN + story arc across scenes (CALLBACK model), not disconnected pretty shots. (3) always re-verify
  the VO for duplicate retakes AT THE JUNCTIONS (whisper hides repeats; check by ear/count, not just the transcript).

- ⛔ **v4 note: "each scene needs WAY more detail + ACTION; hook needs a big pattern-interrupt (the Fable/wizard-Claude
  CHANGING the world); more stuff happening, maximize retention."**
- ✅ **v5 (ACTION pass):** re-authored all 6 as ACTION SEQUENCES (3-6 escalating beats, motion every ~1s, frame-0
  mid-action). Hook = the wizard-Claude actively REWRITING the world (shockwave → terrain rises → towers materialize →
  day/night sky-wipe → orb reveal). Kept Mascot cast + gremlin arc. Fixed 2 bugs: an out-of-scope `broken` ref in S5
  (render crash) + a double-title collision in S1 (the coder added an in-scene "THE WIZARD REWRITES THE WORLD" that
  fought the "CLAUDE SIMULATES YOUR IDEA" ScreenHead → blanked the in-scene title). 0 crashers after.
- ⭐ LESSON (v5): scene coders sometimes add their OWN in-scene title text that collides with the ScreenHead / echoes
  the VO — grep every splice for big-fontSize title strings + blank them (keep only the S1 ScreenHead + the CTA).

- ⛔ **v5 notes: "keep the simulation-GLOBE in the opening (more detail); the camera BOUNCE looks broken — panning
  must move CONTENT within a steady frame, not translate the whole frame."**
- ✅ **v6:** (1) removed whole-frame camera movement — S3 had translate+shake+roll, S5 had translate+tilt on full-scene
  wrappers → stripped to a centered gentle zoom only (S2/S4/S6 were already steady). (2) Re-authored S1 as a
  **GLOBE-CENTERPIECE hook**: a crystal snow-globe holding a detailed living mini-kingdom (towers/river/path/tiny
  Claude citizens), the wizard-Claude casting his wand INTO it to build the world, rich magical surroundings (runes,
  candles, spellbook, floating isles), **LOCKED steady frame** (motion from elements only). Verified on stills.
- ⭐ LESSON (v6): ⛔ scene "camera" moves must NOT translate/shake/rotate the full-scene wrapper (reads as the whole
  video bouncing) — only a CENTERED subtle zoom is safe; all other motion must be element-local. Brief coders with
  "camera LOCKED" explicitly. **v6 = current ship candidate.**

- ⛔ **v6 notes: "some scenes don't make sense / aren't intriguing enough; make the story arc MORE interesting."**
  → Alex chose **"Ground it in a real idea"** (AskUserQuestion). The abstract fantasy-kingdom arc was pretty but
  had no relatable stakes.
- ✅ **v7 = SHIPPED (the grounded remake).** Re-cast the whole arc as ONE recognizable real business — **a corner
  cafe** — so every beat has real cause-and-effect the viewer already understands:
  - **S1 Hook (globe kept):** wizard-Claude casts into the crystal **simulation globe**, which now holds an empty
    **"FOR LEASE"** retail shell (the *idea* being planted) + a "SIMULATE A BUSINESS" scroll. LOCKED frame. →
    seeds the cafe.
  - **S2:** the **CORNER CAFE** builds itself + THRIVES — striped awning, menu board (LATTE $4 / LOAF $6 / COOKIE $3),
    a QUEUE of customer-Claudes, founder-Claude at the till, **SALES $112 · THRIVING · QUEUE 5**.
  - **S3:** the **real failure** — rival **MEGA BREW** opens across the street with **50% OFF**, customer-Claudes turn
    away, gremlin lurks, "50%?!" bubble, **CUSTOMERS 3 ↓**.
  - **S4:** **SLOW DAY** — MEGA BREW's neon 50% sign glowing "right in front," till at **$0**, gremlin at the counter.
  - **S5:** **CLAUDE'S CORNER = CLOSED**, 0 customers, **$0**, founder-Claude slumped, gremlin present → wizard-Claude
    spotlights it to apply **the one fix**.
  - **S6 CTA:** **DREAM CAFE · GRAND OPENING · ★4.9 TOP RATED · ORDERS 96 · TODAY $4,240**, gremlin villain **CAGED
    "CONTAINED / FIXED"** callback gag, big clean **Comment SIMULATE**.
  - Fixed one render crasher: S5 had a leftover in-scene SFX map passing `name={s.n}` to `<Sfx>` (expects `src`) →
    `sfx/undefined` 404 at frame 653; removed it (root cue map at `L[]` already carries the bed). ([[sfx-root-timeline-trap]])
  - **Full render clean** (1204f / 40.19s) → delivery-encoded (bt709 + faststart, QT-verified via qlmanage) →
    **`~/Downloads/SIMULATE_FINAL.mp4`** (13.7 MB). Synced tsx to `claude-reels-workflow/video`.
- ⭐ **LESSON (v7):** "make the story more interesting" ≠ prettier fantasy — it means **GROUND it in a real, familiar
  situation** with stakes the viewer already feels (a shop that opens, thrives, gets undercut, closes). Keep the
  signature hook motif (the globe), but let the *contents* be a recognizable real idea. Relatable cause-and-effect >
  abstract spectacle. **v7 is the ship.**

- ⛔ **v7 REJECTED: "sprites aren't the Claude sprites, too small vs the buildings, FLOATING in the air, interacting
  oddly with the buildings; used to be way more detailed; the run-it-again grid is off-centre; header overflows;
  tie it more to simulations; do a pass with sound effects."** Then: **"header should say 'Make Claude simulate your
  idea'"** + **"each of the run it again scenes need to be way more detailed throughout."**
- ✅ **v11 = SHIPPED.** Full 6-scene re-author + 2 critic-driven fix rounds + an S4 detail pass.
  - ⭐ **ROOT CAUSE of the floating sprites (my bug, not the coders'):** I built the `<Actor>` grounding wrapper with a
    contact shadow **0.68 × size wide — but the Mascot body is 0.66 × size wide**, so the shadow rendered almost
    entirely BEHIND the sprite and was invisible. Every critic independently reported "no contact shadow, reads as a
    sticker." Fixed with a **wide pool (1.12 × size, extending past the footprint) + a tight contact core**.
    **LESSON: a contact shadow must be WIDER than the sprite or it does not exist.**
  - ⭐ **`<Actor x groundY size>` is now the standing grounding primitive** (19 uses): plants feet on a drawn floor
    line + auto shadow. Plus `<SimScan/>` (hologram scanlines) and `<SimTag/>` for the simulation motif.
  - **SIZING LAW** codified: hero 210-300px, mid 150-200, crowd 110-150, hard floor 110; a character at a shopfront
    reads DOOR-HEIGHT. (v7 had 74px customers against a 470px cafe.)
  - **SPACING LAW** (new failure mode I introduced by upsizing): never place two Actors closer than
    0.85 × (sizeA/2 + sizeB/2). Bigger sprites without spacing turned S2's queue into one merged orange blob and
    piled up S6's right cluster. Fix = **fewer, well-spaced characters** (3-4 max per scene).
  - **Header:** now **"MAKE CLAUDE SIMULATE / YOUR IDEA"**, 2 lines @62px + `whiteSpace:nowrap`.
  - **S1 globe:** the "giant robot face in a fishbowl" was the mini-cafe rising 92px via translateY while its awning
    stayed pinned at a fixed top → awning landed BELOW the windows (eyes over a striped mouth). Awning re-pinned to
    the rising roofline; globe interior rebuilt as a properly-scaled diorama (half the globe width, headroom, ground
    plinth, tiny citizens).
  - **S4 "RUN IT AGAIN":** centring fixed (was top-aligned over an empty void), then a dedicated draft→audit→revise
    detail pass took it **268 → 637 lines**: all four cells now tell complete stories (TOO PRICEY price tags + "$52?!"
    + empty rope line; WRONG SPOT 12 MI AWAY + bench + crowd walking off; RAN OUT OF CASH revenue chart + BANK
    BALANCE + spilling coins + OVERDUE/DECLINED; COPIED CLONE CAFE + COPY stamp + share bar), each with its own drawn
    floor, RUN #, FAIL bar, plus a SIM LOG.
  - Other critic blockers fixed: S2 menu truncation ("OOKIE $3"), S3 CLOSED showing only "CL" + lamp post cutting
    "ORNER CAFE", **S5's diagnosis ring circling EMPTY BACKGROUND** instead of the price, S5 mirrored CLOSED sign
    (a `rotateX` flip showing the back face → swapped to a `scaleY` squash), **S6's caged-gremlin payoff rendering
    not at all**, and sprites/props clipped by panel edges.
  - **SFX pass done:** the generic inherited bed replaced with **44 story-tailored cues** (construction pops as the
    cafe builds, a cash register per customer, a record-scratch the instant the rival appears, escalating cascade
    hits, a downer on CLOSED, sim blips/glitch for the reruns, magic-fix chime + heal, grand-opening cheer, CTA slam).
  - Rendered clean 1204f/40.19s → delivery-encoded (bt709 + faststart, qlmanage-verified) → **`~/Downloads/SIMULATE_FINAL.mp4`**.
- ⭐ **PROCESS LESSON:** the vision-critic round was decisive — it caught defects I would have shipped (the empty
  globe, the ring circling nothing, the missing cage, "OOKIE $3"). **Always run adversarial vision critics on
  rendered frames before delivering; "it rendered without crashing" is not "it looks right."**

## v14 = FINAL SHIP (2026-07-18)
Alex's closing round of notes and what each turned into:
- **"header should say Make Claude simulate your idea"** → `MAKE CLAUDE SIMULATE / YOUR IDEA`, 2 lines @62px + `nowrap`.
- **"remove the SIM RUN text top right"** → S1 SimTag deleted (other scenes keep theirs).
- **"the ball needs motion / shaking + the inside needs to be an interesting simulated world"** → globe now has an
  idle wobble + hard decaying rattle on the cast + jolts per build event (globe **and stand only**, camera locked),
  and the interior is a living miniature STREET (4 buildings rising on a stagger, road, lamp, bench, 6 walking
  citizens, a vehicle, a looping day/night sweep) instead of one static shop.
- **"the corner cafe scene needs more animation and action"** → continuous customer cycle (arrive → served → cup
  physically handed over → exit), sales ticking per handover, rush climax.
- **"the 50% off sign text is overflowing"** → badge rebuilt: "50%" / "OFF" on two centred lines with ≥16px clearance.
- **"there shouldnt be glowing components - use the animation color palette"** → **175 glow effects → 0 real
  emitters** across all 6 scenes (S4 alone had 62). Neon/cyan/acid-green replaced with CREAM/CLAY/AMBER/GOLD/GREEN/
  SLATE/RED; depth now = gradient + directional shadow + value contrast. Gremlin keeps muted purple, plain eyes.
- **"soundtrack every living breathing moment, make sure sound actually starts at 0 seconds"** → the fade-in envelope
  was the cause (volume 0 → 0.11 over 1.4s). ⭐ But a FLAT gain was also wrong: the track itself swings **~11dB**
  (rms .186 @0s → .053 @24s), so it would blare over the hook, vanish under the diagnosis scene and swell over the
  CTA. Gain is now shaped **inversely to measured per-2s bed rms** for a constant perceived level. Verified: first
  2s of the delivered file is non-silent (mean −17.9dB / max −3.1dB). ([[music-duck-song-build]])
- **"VO needs to be like 1.04x speed"** → re-encoded via atempo (40.24s → 38.68s) and the WHOLE reel re-timed: all
  185 word timings ÷1.04, `L` → [0, 6.5192, 14.0, 20.8846, 24.6346, 29.0962], `CUT` 40.15 → 38.6058,
  `durationInFrames` 1204 → 1158. Also caught a pre-existing SFX misalignment: the hook's magic-cast sting fired at
  3.20s while the wizard casts at ~1.6s — the whole S1 cue block was realigned to its real beats.
- **"near the end theres nothing going on for 5 seconds plus... each scene needs something that BUILDS the story"**
  → built `tools/motion_audit.py` and fixed it by measurement, over two passes. See [[reel-dead-air-motion-audit]].
  **Median motion 2.15 → 5.14 · p10 1.04 → 3.14 · S6's complained-about window 1.5-2.1 → 4.8-5.6.**
  ⭐ The key lesson: **pass 1 failed** (window only 1.8→2.3) because it added SMALL motion (confetti, ticking
  counters, floating chips). Small elements move almost no pixels. Pass 2 imposed a quantitative rule — at every
  frame ≥1 element of ≥40,000px² travelling ≥6px/frame — implemented as overlapping customer waves (spawn every 20f
  instead of 52, dwell removed) + full-width background traffic + cloud-shadow sweeps. That took it to 4.8-5.6.
- Delivered: 1158f / **38.66s** / 22.0 MB, bt709 + faststart, qlmanage-verified → `~/Downloads/SIMULATE_FINAL.mp4`.
- ⚠️ **Known remaining weak spot (disclosed, not hidden):** S4 second 1 still measures **2.4** (every other bucket in
  the reel is ≥3.7). Left alone deliberately — S4 is the densest scene (640 lines, 4 detailed cells) and it is a
  3.8s scene bookended by 18.7 and 14.8, so one soft second is a poor risk/reward to touch.
- ⚠️ **Not verified by me:** the SFX cue placement is reasoned from scene beat frames, not confirmed by ear.

## v16 = FINAL SHIP (2026-07-18, second delivery)
Alex's notes and what each became:
- **"corner cafe scene is WAY too long of just the guy standing greeting customers — ADD MORE SCENES"** + **"each
  scene needs more scenes and actual storyline"** → ⭐ the structural fix: the reel went from **6 long takes to 19
  distinct SHOTS** with hard cuts + whip-wipe accents. Implemented as a shot switch inside each SxBody
  (`const SHOT = lf < A ? 0 : lf < B ? 1 : 2`) with each shot animating from its own local frame 0.
  The cafe slot became 4 shots that illustrate the VO line by line: idea-card insert → street self-constructs →
  **"GENERATING PEOPLE"** persona cards (STUDENT/price sensitive · COMMUTER/in a hurry · REGULAR/loyal every day,
  "3 OF 3 SIMULATED") → fast full-day time-lapse. Its motion went 9.9/4.3/7.1/4.4… → **27.1/19.6/10.8/19.7…**
- **"globe: come together QUICKER, START GLOWING, more detailed/vibrant/cooler"** → ⭐ **a deliberate, scoped
  reversal of the no-glow rule: the GLOBE ONLY may emit.** Breathing `GLOW` that flares on the cast + a `Pool`
  helper spilling warm light onto the desk, wizard and back wall. World assembles ~2x faster.
  ⚠️ First attempt REGRESSED: chasing "more detailed" made the lab near-black, killed the glow, and the dive shot
  lost the globe framing entirely (read as a plain night city). Fixed in a dedicated relight pass — warm candlelit
  lab, luminous globe, curved glass rim + spherical vignette retained on the dive shot, vibrant golden-hour toy town
  inside. **LESSON: "more detail" and "more vibrant" pull in opposite directions unless lighting is briefed explicitly.**
- **"remove the SIM RUN text top right"** → deleted from S1.
- **"the word simulation at ~34s is cut off"** → NOT the caption (it renders fully). It was a travelling "NOW OPEN"
  blimp sign being **sliced by the panel edge into "NO"**. New standing rule: a moving prop may exit frame, but any
  TEXT it carries must fade to 0 before it comes within 40px of the edge.
- **"sound effects design throughout from the sfx library"** → rebuilt as a cut-driven bed, **~115 cues** keyed to
  every shot boundary. ⭐ Two real bugs caught by MEASURING the library first ([[sfx-dur-truncates-tails]]):
  (1) `lib_riser.wav` is 2.58s but was started 1.35s before each cut with dur 1.5 — so it played only its QUIET
  OPENING and cut before the peak. **A riser must be pre-rolled by its full length so its peak lands ON the hit.**
  (2) stings were chopped mid-tail (lib_cinematic_hit 5.63s given 1.3; cinematic-impact 7.97s given 1.0;
  lib_confirm 4.55s given 0.9; the domino hits used hit.mp3 5.67s truncated to 0.4 → swapped to impact.wav, which
  is naturally 0.62s).
- Final numbers: **motion median 6.07** (was 2.15 when dead air was first flagged), p25 4.08, no static stretches.
  Delivered 1158f / 38.66s / 20.4 MB, bt709 + faststart, qlmanage-verified, audio non-silent from 0s (max −3.4dB).
- ⚠️ **Still weak (disclosed):** S4 second 1 = 2.4 and S6 second 5 = 3.4 (everything else ≥4). S4 left untouched
  deliberately — densest scene, strong bookends. SFX placement still not verified by ear.

## v17 = FINAL SHIP (2026-07-18, third delivery) — the DETAIL pass
Alex: *"each of these scenes is not detailed enough — much much MORE DETAILED and interesting throughout, the
BACKGROUNDS, each of the COMPONENTS etc. Also some of the Claudes might need BETTER OUTFITS. And improve the
ANIMATIONS and the detail for each scene."*
- ⭐ **HIGHEST-LEVERAGE MOVE: upgrade the shared `<Mascot>` component, not the scenes.** It is used ~30x across
  every scene, so one edit re-dressed the entire cast. 261 → 541 lines: every existing costume got real tailoring
  (seams, collars, lapels, cuffs, buttons w/ highlights, pockets, belts, hems, fabric shading) **plus 9 new
  mix-and-match wardrobe props** — `apron beanie cap scarf hoodie backpack tote headphones cup` — with secondary
  motion (scarf tails + tote lag behind the body bob, steam off the cup).
  ⛔ **Compatibility contract:** every pre-existing prop name kept identical semantics and all new props default to
  0, so ~30 existing call sites render unchanged except richer. Verified by diffing the signature before splicing.
  ⛔ **Pin the prop names in BOTH briefs** (the component agent AND every scene agent) or the two halves drift and
  scenes silently pass props that do not exist (React ignores unknown props — it fails invisibly, not loudly).
- **Scene detail pass, briefed on 3 concrete axes** rather than "more detail": (1) BACKGROUNDS = 5-7 receding
  layers with aerial perspective (far layers hazier/lower-contrast) + real surface texture (brick courses, paving
  joints, wood grain, roof tiles); (2) COMPONENTS = every prop gets sub-detail (a shopfront = mullions, sill, door
  handle, letter slot, house number, kick plate, scalloped awning + its wall shadow, bracketed hanging sign with
  chain links, drainpipe, step, doormat, posters, crates, leaning bike); (3) ANIMATION QUALITY = overshoot-and-
  settle, 2-4 frame stagger, anticipation, secondary motion on cloth/hanging objects, per-layer parallax.
  Scene code roughly doubled: S1 1024→1672, S2 490→770, S3 532→742, S4 640→944, S5 302→472, S6 559→925.
- ⭐ **Detail did NOT cost motion — it added it.** Median 6.07 → **6.94**, p25 4.08 → 4.59, p10 2.60 → 3.00, and the
  two weakest buckets in the whole reel finally came up (S4 sec1 2.4 → 3.0, S6 sec5 3.4 → 4.0). Folding the known
  weak bucket into the detail brief is why. Every bucket is now ≥3.0.
- Delivered 1158f / 38.66s / **24.3 MB**, bt709 + faststart, qlmanage-verified, audio live from 0s (max −3.4dB).
- ⚠️ Still the weakest: S4 sec1 = 3.0 and S3 sec4 = 3.8 (bar is 4). SFX still unverified by ear.

**Cross-links:** [[raycfu-lane-preferred]] · [[reel-winning-formula]] (SIMULATION = passing system) · [[factory-log-first]] ·
[[premise-staleness-rerun-test]] · [[reels/council-factory-log]] (the sibling that did well) · [[reel-cinematic-legup]] ·
[[sfx-root-timeline-trap]] · [[reel-sprite-grounding-law]] · [[reel-declutter-single-hero]] ·
[[reel-dead-air-motion-audit]] · [[music-duck-song-build]] · [[sfx-dur-truncates-tails]].

## v20 = RETENTION REVISION (2026-07-19) — after the trial-reel data came back
Alex ran v17 as an Instagram TRIAL and sent the insights. The data was unusually clean to read:
- **Comment 4.0% · Save 4.6% · Follow 1.07% · Like 3.3%** — all strong (the raycfu comp did 4.42% comments).
- **Skip 49.2% · avg watch 10s of 38.66s (25.9%)** — a near-vertical retention cliff in the first 2-3s.
- ⭐ **DIAGNOSIS: the content + CTA are excellent; the first 2 seconds are the entire problem.** Everyone who
  stays converts; half never start. So: change ONLY the opening, keep everything downstream byte-identical, so the
  A/B stays clean.
- The old open broke FOUR of Alex's own standing rules (audited from frames): no headline at 0.0s (eased in at ~1.0s);
  darkest frame in the reel (a brown lab) in a bright feed; header stated the MECHANIC not viewer VALUE; and 2.0s was
  a build-in-progress (an empty field with a sign flying in).
- **FIXES SHIPPED:**
  1. **COLD OPEN on the failure.** S1 restructured to 3 shots: A (f0-66) the DEAD cafe already happened (bright
     daylight, CLOSED + FOR LEASE + boarded door + $0 register + a "RUN 40 / FAILURES LOGGED" counter ticking
     9->36, cars passing) — frame 0 complete + bright + a real number; B (f66-130) hard-cut to the glowing-globe
     cast; C (f130-196) inside the vibrant world + the red crack. The globe stayed, it just stopped being the thing
     you sit through first. And the cold open MATCHES the VO ("watch the exact moment it falls apart").
  2. **HEADER rewritten** to value+specificity, judged 12-across-4-families: shipped **"CLAUDE FINDS THE ONE THING /
     THAT KILLS YOUR IDEA"** (contains Claude for OCR; states what the viewer GETS not what Claude does; claims no
     fake number so no bait-and-switch; its curiosity gap IS the reel's payoff). Sized 62->54px to fit the longer
     line, ease-in 0.4s->0.14s so the claim is on frame 0.
  3. **S1 SFX re-timed** to the new shot map (sad downer+breeze+failure-tick over the cold open; the cast sting moved
     1.6s->2.6s so it no longer fires over the dead cafe).
- ⛔ **DID NOT trim the VO** (I had proposed ~26s). Honest call: the cliff is the first 2-3s (skip rate), and the only
  clean cut is a 1.7s line at the 12s mark — it touches the TAIL not the cliff, needs a full re-time (fresh bug
  surface), and gains ~1pp completion. A true 26s needs a re-recorded VO. Wrong lever; skipped it.
- Motion held (median 6.66, cold-open buckets 9.2/5.8/16.6, all >=4). Delivered 38.66s / 23.6MB, overwrote the
  canonical `65 - SIMULATE/65_Claude-simulate.mp4` in place (cloud-verified, same file id, new size, no pile-up).
- ⭐ **LESSON: read the trial data before touching anything.** A polished reel that dies at 1-2s is a HOOK failure,
  not a craft failure ([[reel-retention-hook-teardown]]) — the fix is upstream (frame-0 claim + bright stun-gun +
  value header + a receipt), and you change ONLY the opening so the trial's strong comment/save signal is preserved.

## tv1 / tv2 A/B PAIR (2026-07-19) — Alex: "two separate versions, keep tv1 untouched, make tv2 with a better pattern interrupt"
Both live in `Claude Reels/65 - SIMULATE/` (cloud-verified, non-zero sizes, no un-suffixed dup):
- **`65_Claude-simulate_tv1.mp4`** (23,572,179 b) = the cold-open version (dead cafe + "CLAUDE FINDS THE ONE THING /
  THAT KILLS YOUR IDEA" header). Unchanged.
- **`65_Claude-simulate_tv2.mp4`** (23,728,222 b) = tv1 with a harder PATTERN INTERRUPT opening: shot A rebuilt as a
  rapid 6-failure flash montage (TOO PRICEY / WRONG SPOT / NO CASH / COPIED / NO TRAFFIC / CLOSED — each ~3-4 frames,
  hard cut, big red stamp, a RUN counter blurring 1->40, tear lines) then a screen-shake SLAM onto the settled dead
  cafe. Frame 0 bright + mid-action. It doubles as a literal render of the header (Claude killed the idea 40 ways).
  ⭐ SFX had to differ per variant: tv2's montage got its own audio (glitch bed + 6 escalating stamp-hits synced to
  the flashes + a cinematic slam), NOT tv1's calm sad-drone. Since the SFX block is global, tv2 = splice tv2 S1 +
  swap the S1 SFX block, render, then restore the full tv1 backup.
- tv1 and tv2 are byte-identical from ~2s onward (shots B/C + S2-S6 untouched) = a clean A/B on the first 2 seconds.
- The ORIGINAL trial reel (old dark-lab opening = the one that got 49% skip) is preserved locally as
  `out/SIMULATE_v19.mp4`, untouched.
- Both S1 variants stay staged (`scratchpad/S1_tv1.tsx` + `s1_tv2.json`); repo source rests on tv1.
- ⛔⛔ **DRIVE UPLOAD JAM (real, cost time):** `matchtern-longform/video/out/` is Drive-MIRRORED, and across this long
  iterative session it bloated to **24 renders / 8.2GB all uploading to Alex's Drive**, which congested the DriveFS
  queue so hard that the real deliverable (tv2) kept getting its upload CANCELLED and never reached the cloud, while
  the local mount happily showed it "there." Fix: prune `out/` to the last 1-2 renders, wait for the queue to settle
  (watch drive_fs.txt for churn to stop), THEN re-verify against the cloud via the Drive MCP. ⭐ **Prune out/
  periodically during any long reel session, and NEVER trust the mount for a delivery — always cloud-verify fileSize.**
