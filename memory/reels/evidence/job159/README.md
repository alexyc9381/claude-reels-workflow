---
name: job159-evidence
description: "Text evidence for final JOB R8 / numbered 159, earlier motion failures, keyword reveal, audio exceptions, source rebuild and synced Drive metadata."
metadata:
  node_type: memory
  type: evidence_index
  date: 2026-09-18
  reel: 159
---

# JOB 159 evidence

[Revision record](../../job159-revision-record.md) · [reusable guidance](../../../alex-job-hook-action-sound-and-delivery.md) · [storyboard](../../../../storyboards/159-job.md).

These are small text reports copied from the production workspace; absolute workspace prefixes are normalized to `<production-workspace>` and trailing whitespace is removed. Movies, portrait images, sound recordings and the editable project remain in [159 - JOB on Drive](https://drive.google.com/drive/folders/1gcZtEYaM9yt12KBqDWtzLpo93WeSt69-). Nothing here claims independent creative approval or retention lift.

| Evidence | What it establishes / limit |
| --- | --- |
| `r7-draft1-motion.txt`, `r8-draft1-motion.txt` | Preserve the failed detector/robot-tail and faster-hook-tail passes. The final pass does not erase these defects. |
| `final-motion.txt` | Final scene minimum/dead-run and tail checks; these are motion metrics, not engagement scores. |
| `final-look.txt` | Blocking look checks pass; advisory values retained. |
| `final-verify.txt` | 9/9 signal/completeness checks. Mixed energy cannot isolate SFX audibility; caption checks do not negate the explicit late JOB visual exception. |
| `final-decode.txt` | Empty error log from the completed full decode. |
| `final-probe.json`, `final-loudness.txt` | Final encoded stream metadata and measured input loudness/true peak. The loudnorm report's output values describe its analysis transform, not another delivered master. |
| `sfx-audit.txt`, `audio-review.txt` | Original thresholds with keyboard/robot phrase flags and the specific rationale. Not a clean SFX-audit pass or broad waiver. No subjective listening was performed. |
| `audio-source-check.json` | Independent source 8.000s PCM match over three seconds; no event tail truncated at endpoint. |
| `r7-vs-r6-sfx-comparison.json` | Like-window SFX stem RMS increases for R7, not a subjective or R8 gain claim. |
| `r8-mix-report.json`, `r8-sfx-cues.json` | 66 cues, source excerpt/contact parameters, stems and pre-limiter peak. The floating-point premaster peak is not the final encoded peak. |
| `keyword-proof.json` | Reveal timing, examined packaged-source frames and their hashes. Frame images/contact sheet are in the delivered source archive. |
| `source-rebuild.txt` | 39 representative packaged-source frames rebuilt; not a second whole-movie render. |
| `portrait-sources.json` | Exact source pages and illustrative-avatar scope; no actual identity/endorsement claim. |
| `159-drive-local-copy.json` | Byte sizes, local SHA-256 and MD5 for all numbered deliverables. |
| `159-drive-delivery-verified.json` | Synced DriveFS metadata: remote IDs, size/MD5 matches for all 13 files. No independent cloud re-download or Drive playback test is claimed. |

R8 was copied byte-for-byte to numbered 159. Historical source-project names inside the archive still say JOB-139. The new filenames and checksum list are reflected in the numbered manifest.
