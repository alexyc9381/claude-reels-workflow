---
name: pokeball-factory-log
description: "Reel 52 BALL (Pokemon-themed): don't wait for the unreleased legendary (Fable 6) — build a smarter SYSTEM around today's Claude (memory/MCP/sub-agents/loop) until it battles like the version that isn't out yet. CTA: comment BALL. v1 shipped 2026-07-13."
metadata:
  node_type: memory
  type: project
  originSessionId: 34c266ae-aeaf-4063-b27a-b16a33164df3
---

# Reel 52 — BALL / Pokemon theme (v1 shipped 2026-07-13)

Second themed reel after Mario (reel 51). Alex picked **Pokemon** as the theme, then recorded + provided the VO (`POKEBALL.m4a` in Drive `Claude Reels/*VOs/`). File `src/ClaudePokeballReel.tsx`, comp `ClaudePokeballReel` (2056 frames / 68.5s), delivered `52_Claude-build-fable6.mp4` to Final + Drive `52 - BALL/`.

## Premise (Alex's VO, not a listicle — a "build the system" narrative)
"Everyone's waiting for **Fable 6** to drop — but you can build it yourself right now." Wrap a smarter SYSTEM around today's Claude: **Memory** (a second brain), **MCP** (reach files/apps/internet + writes its own tools), **sub-agents** (one lead Claude running 20 specialists), a self-fixing **loop**. Then a BATTLE (plain Claude chokes vs the system that plans/splits/fixes/ships overnight), the reveal ("same model underneath… runs like a version of Claude that isn't out yet"), CTA **comment BALL**. Maps perfectly to Pokemon: don't wait for the legendary — evolve/train the one you have.

## Scene→Pokemon map (9 scenes, L=[0,6.54,17.42,21.18,25.44,32.32,41.34,54.96,64.5], CUT=68.5)
S0 hook: locked "LEGENDARY FABLE 6 — NOT RELEASED" + pokeball throw/catch → build it yourself · S1 problem: weak Lv.5 CLAUDE Pokedex card, ailments MEMORY 0/TOOLS ✗/ALONE · S2 build: cross out waiting → 4 ??? upgrade slots · S3 Memory: equip brain-orb, stats fill · S4 MCP: FILES/APPS/INTERNET nodes + TM writes its own move · S5 team+loop: 20 specialist pokeballs + auto-heal loop · S6 BATTLE (peak): PLAIN CLAUDE faints vs THE SYSTEM Lv.77, 4 sweep moves → SHIPPED · S7 evolution: EvoGlow → the "version that isn't out yet" legendary (payoff hook) · S8 CTA: pokeball + comment BALL + follow.

## Build (reused the [[skills-mario-remake]] full-theme method)
- **VO spliced** from POKEBALL.m4a (96.6s raw → 68.5s): 3 "cut cut" retakes removed (kept the clean MCP / "isn't out yet" / "whole trick" takes), RMS-snapped boundaries + 16ms crossfades, raw (no voice processing). faster_whisper 1.2.1 on system python3.
- **Chassis cloned** from ClaudeSkills2Reel.tsx (kept Mascot/Panel/captions/ProgressBar/SkillHeader/brand-burst/drop-slam); swapped VO/words/L/CUT/keyword/header ("BUILD FABLE 6 YOURSELF"). Registered in Root (2056 frames).
- **Foundation first** (verified before scenes): Mascot `trainer` costume (clean base face + red League cap + pokeball emblem — the [[skills-mario-remake]] "Claude-as-a-costume" rule), Pokemon KIT (PkField route bg, Pokeball(wobble/open), PokedexCard(sprite window+types+desc), TypeBadge, StatBar, EvoGlow, PkSlug), TYPE_COL palette.
- **9 scenes** authored by a 9-agent parallel Workflow (`pokeball_scenes_wf.js`, general-purpose, effort high, each wrote a scratch file → anchor-splice; R0-R6 replace, R7/R8 append, dispatch → 9). 0 crashers on the render sweep.
- **Chiptune SFX** synthesized (`tools/gen_pokeball_sfx.py`, numpy): pk_throw/wobble/**caught**(gotcha)/levelup/**evolve**/hit/faint/heal/select. Wired to the 9 beats + kept hook + CTA + per-scene risers.

## v5 re-render + scene-verify (2026-07-14, fresh session)
The V1 authoring chat froze (transcript bloat, see [[claude-code-freeze-transcript-bloat]]); a render had actually **completed at 00:06** and was already in Final+Drive, but Alex never got the confirmation. Re-rendered clean from current source (`src/ClaudePokeballReel.tsx`, comp `ClaudePokeballReel`, **1979 frames / 66.0s** — VO cut-cut flubs removed so shorter than the old 2056) → `out/52_build-fable6_v5.mp4` (29.3MB) → delivered `52_Claude-build-fable6_v5.mp4` to Claude-Reels-Final/ + Drive `52 - BALL/`. Render mechanics: `npx remotion render src/index.ts ClaudePokeballReel out/…mp4 --codec h264` (Remotion 4.0.370; audio fully self-contained in the comp — VO+pk_music+all SFX via `<Audio>`, no separate mux). Bundled ffmpeg for stills: `node_modules/@remotion/compositor-darwin-arm64/ffmpeg` with `DYLD_LIBRARY_PATH` set to that dir. ⚠️ public dir is 700MB+ → every render copies it (slow); worth trimming.
**Verified all 9 scenes render + read** (extracted stills): hook/pokedex, wild-battle (menacing purple ghost villain + clothed trainer both land), build-the-system gym, MCP nodes (FILES/APPS/INTERNET), TEAM+AUTO-FIX split, THE BATTLE (Plain Claude FAINTED vs THE SYSTEM, SPLIT ACROSS TEAM), evolution sunset payoff, CTA "comment **BALL**" + FOLLOW + podium. The 3 headline V1 fixes (VO flubs cut / trainer clothed / villain redesigned) + detail overhaul are all in this cut.

## v6 MAJOR overhaul (2026-07-14, Alex's punch-list) — delivered `52_Claude-build-fable6_v6.mp4` (63.2s, 1896 frames)
Alex's 10-item note list, all done + render-verified:
1. **Soundtrack**: swapped bg music to Drive `Claude Reels/*Soundtracks/Pokemon Sound Tracks/Pokemon Theme Song Instrumental.mp3` → `public/pk_music.wav` (first 74s; old Mario music backed up `.bak_mario`). Also extracted `pk_open_real.wav` + `pk_battle_real.wav` from that folder for real SFX.
2. **Background** (`Bg`, line ~45): rebuilt the outside-the-panel world → detailed Pokemon region: warm sky+sun bloom, bird-Pokemon flocks, layered clouds+mountains, **Pokemon Center (red dome+cross) + Gym (lightning badge tower)** skyline peeking above the panel, rolling hills, winding dirt path, tall-grass tufts, flowers, resting pokeballs.
3. **Villain → real GENGAR** (`GhostMon`, ~631): spiky all-around silhouette (13 procedural spikes via `spike(deg,len,hw)`), dark indigo body, angular glowing red eyes, giant Cheshire grin w/ jagged teeth, stubby 3-claw legs. Unmistakably Gengar. Used both as wild monster (R1/R6) + team SEARCH mon.
4. **Team sprites way better**: `Pikachu` (chubby, black-tip ears, cheeks, lightning tail+spark), `FireLizard`=**Charmander** (upright, cream belly, snout, bigger glowing flame tail, side arms), `FoxMon`=**Eevee** (big fluffy cream RUFF, bushy cream-tip tail, tall ears). Verified via a temp `SpriteLab` comp (since removed).
5. **Opening catch** (R0): added a **wild Pikachu** ("WILD" tag) that the trainer's thrown ball absorbs (red capture wash + shrink-into-ball) → wobble → GOTCHA sparkle burst. Vars `wildIn/captureP/wildO` ~line 1298.
6. **Turn-based team attack** on SPLIT ACROSS TEAM (R6, move mi===1): `splitP`+`lungeOf(idx)` → Pikachu→Eevee→Charmander each lunge at Gengar in sequence, type-colored HIT! burst + xN COMBO counter + monster reel/shake/flash.
7. **VO fixes**: cut the "it already, it already" **stutter** (removed 0.28s at 58.64–58.92 w/ 12ms xfade) → then **1.04x speed**. Speed baked into the comp cleanly (ffmpeg -vf/-af is BROKEN in the Remotion compositor ffmpeg build — filtergraph parser errors): `fr(s)=round(s*FPS/VO_SPD)` (VO_SPD=1.04) compresses the whole visual timeline, `words_pokeball.json` times ÷1.04, `pokeball_vo.wav` resampled ×1.04 in numpy (music left 1.0x). durationInFrames 1971→**1896**. Caption sync verified early/mid/late.
8. **CTA caption**: `>BALL<` → `>&ldquo;BALL&rdquo;<` — now shows comment **"BALL"** with quotes.
9. **More SFX**: catch sequence (throw/real-ball-open/wobble/gotcha), 3 team-lunge hits (pk_hit+pk_battle_real+thock), battle-start pk_battle_real, CTA pk_open_real.

⚙️ Render mechanics unchanged (see v5 section). ⚠️ The Remotion compositor `ffmpeg` at `node_modules/@remotion/compositor-darwin-arm64/ffmpeg` (needs `DYLD_LIBRARY_PATH` = that dir) can decode/encode + extract stills but its **-vf/-af filtergraph is broken** — no system ffmpeg on this Mac, so do audio/timeline speed changes in numpy + the `fr()` bake, NOT ffmpeg filters.

## v7 detail + sound-design pass (2026-07-14) — delivered `52_Claude-build-fable6_v7.mp4` (34.9MB)
Alex: "make the scenes AND background a lot more detailed/interesting + better sound design throughout."
- **Cinematic atmosphere toolkit** (added before R0): `GodRays`, `Motes` (spark/dust/ember/leaf/snow/data/firefly/confetti), `DepthVignette`, `AuroraWash`, + `SceneAtmo({preset})` wrapper. Applied one themed preset per scene (route/wild/gym/psychic/tech/team/battle/sunset/cta) inserted right before each `<PkSlug>` so it layers behind content. This — god-ray light shafts + themed particles + focus vignette + aurora depth — is the biggest uplift; every scene now reads cinematic. Panel-local 1012×792, uses `seed()`/`interpolate`, deterministic.
- **Backdrop enrichment**: PkField (R0) got far mountains, distant Gym+PokeCenter skyline, a "ROUTE 6" signpost, a fluttering Butterfree-ish bug. PkSunset (R7) got a twinkling starfield + far mountain. Outside `Bg` got a spinning windmill + a foreground berry tree.
- **Sound design**: 3 original ambient beds synthesized in numpy (`tools`-style inline script) → `public/amb_breeze.wav` (breeze + bird chirps), `amb_crowd.wav` (stadium murmur swells), `amb_hum.wav` (tech drone + blips), each 8s seamless-looped. Wired via `ambEnv(ff, windows, v)` gating: breeze on R0/R1 + R7, crowd on R2/R6/R8, hum on R3–R5. Levels 0.055–0.075 under VO (1.28) / music (0.14). Verified render peak 0.828, 0% clipped.
- Verified all 9 scenes via stills — richer + readable, no clutter (respects [[reel-declutter-single-hero]] / [[reel-no-emoji-no-lowopacity]]). ⚠️ music still the copyrighted Pokemon Theme (IG detection risk — flagged to Alex).

## v8 audio-mix + Claude-clarity pass (2026-07-14) — delivered `52_Claude-build-fable6_v8.mp4`
Alex: VO too quiet near end / music too loud; repeated "now here's the part" ~40s; make it clearer this is about CLAUDE not just Pokemon (or people scroll away).
- **VO leveling** (quiet tail): measured per-3s RMS — tail (36–63s) was ~0.03 vs front ~0.055. Applied smooth block-gain fader automation (target 0.052, gain 0.85–1.95, noise-gated, 11-tap smoothed) → tail now ~0.05 even. Peak capped to 0.82 for headroom. NOTE: this is gentle level automation (no EQ/compression) to respect [[alex-vo-recordings]] raw-VO rule, done because Alex explicitly asked.
- **"now here's the part" double-take**: fine RMS scan found flub take at 39.83–40.32 then clean take 40.44+. **Silenced** [39.83,40.42] (15ms fades) — kept total duration so NO scene/caption resync needed; then retimed the 7 caption words "Now here's the part that gets you" to the clean take (40.42–41.55). (The earlier "it already" fix used a CUT+shift; this one used SILENCE-in-place — simpler when at/near a scene boundary.)
- **Mix**: VO Audio volume 1.28→1.15; music envelope lowered (sustained 0.14→0.092, back-half 0.17→0.072–0.08); crowd bed 0.075→0.05. Verified output peak 0.709, 0% clip, rms 0.049 — VO clearly on top throughout.
- **Claude clarity**: `SkillHeader` (persistent top strip) now uses the actual `ClaudeLogo` (sunburst) instead of a generic lightning bolt + a gold **"AI"** badge after "TURN CLAUDE INTO FABLE 6" — brands every frame as Claude/AI. Hook Fable-6 card ribbon "NOT RELEASED YET" → **"UNRELEASED AI MODEL"**. Scenes already carry Claude/AI terms (PLAIN CLAUDE, MEMORY, MCP, subagents, "same model underneath") so the persistent header logo + AI tag is the main fix.

## v9 retention + Claude-branding pass (2026-07-14) — delivered `52_Claude-build-fable6_v9.mp4`
Alex: scenes not retentive enough; make the opening clearer it's Claude (Claude-themed color that flips); improve the Pikachu→pokeball catch; make the OUTSIDE bg the traditional Claude beige/orange instead of the Pokemon world.
- **Retention** (informed by a parallel per-scene audit workflow — 8 general-purpose agents, one per scene, verdict was unanimously "ok-but-plateaus": scenes build then coast, hits/numbers don't LAND). Implemented the highest-leverage subset: (1) **global zoom-punch** — expanded the `KICKS` array in the main comp to every scene's key impact beat + a `HARD` list (KO/reveal/SHIPPED/keyword) that punches at full weight for escalation, and bumped `zoom` punch 0.028→0.045; this snaps the camera on every scene's beat with ONE change. (2) **R6 drain tightened** `fr(2.0)→fr(1.35)` + new `menace = drain*(1-faint)` makes the wild task visibly LOOM (scale+lunge+aura) as Plain Claude chokes — kills the reel's longest dead hold. (3) **R8 social-proof**: a live "247 comments" green pill ticks up + lands (~+2.7s, must land BEFORE R8 ends ~+3.9s) to hold viewers through the CTA. NOTE: the audit's per-scene number-land-pops / per-node hits (medium/low priority) were NOT individually implemented — the global punch covers the recurring "hits don't land" issue; deeper per-scene polish still available (audit saved in the workflow journal).
- **Claude clarity**: (a) outside `Bg` rebuilt from the Pokemon world → a warm **Claude cream/clay stage** (cream gradient + rotating Claude sunburst rays + clay depth blobs + bokeh) so the game reads as running inside a Claude production. (b) A top-level **CLAUDE intro card** (cream + `ClaudeLogo` + "CLAUDE" + "build the Fable 6 AI yourself →") holds 0–0.66s then FLIPS into the game: content fades FIRST, then the cream panel lifts away (two-stage — a one-layer fade ghosted the dark CLAUDE over the green grass). Set the old in-game `cbV` CLAUDE brand-burst to 0 to avoid double-CLAUDE.
- **Catch animation** improved: ball bonk flash (thock) → Pikachu reddens + sucks DOWN into the ball (saturate/brightness ramp) → absorb pop → **GOTCHA!** yellow stamp + ding. New SFX for the intro flip (swooshup+magic), catch bonk/gotcha, R8 counter.
- Verified: audio peak 0.709 / 0% clip; flip clean; catch/menace/counter all land.

## v10 opening rework + SFX de-clutter (2026-07-14) — delivered `52_Claude-build-fable6_v10.mp4`
Alex: opening SFX way too obnoxious; the title-card intro isn't a good idea.
- **Killed the title card** (a title card is a retention killer for short-form — people scroll past intros). Reverted to a **cold-open hook**: the giant pokeball SLAMS + cracks open and the in-game CLAUDE reveal (`cbV`, which I'd zeroed in v9) bursts out — restored `cbV = cbIn*cbOut` and strengthened the reveal (logo 72→88 + CLAUDE 76→92 + a gold "BUILD THE FABLE 6 AI" badge under it) so it's an animated hook AND crystal-clear it's about Claude/AI in the first second. LESSON: no title/intro cards on reels — cold-open on motion.
- **Opening SFX de-cluttered**: the first ~2.7s had ~11 stacked hits (boom+crash+**vine_boom**+swooshdn all at 0.3, plus double risers, double shimmers, stray pk_throw/wobble/caught). Cut to 3 clean ones: one soft riser (0.22) + one swooshdn+boom slam (0.3) + one crack-open shimmer (0.56). Verified first-1.5s peak dropped ~0.7→0.39. Removed the title-card flip SFX too.

## v11 audio remix + cold-open (2026-07-14) — delivered `52_Claude-build-fable6_v11.mp4`
Alex: SFX obnoxious at the start; VO not loud enough / music too loud; the "title card" intro isn't a good idea; make the very beginning more obviously about Fable 6.
- **Killed the title-card intro** (the top-level Claude flip card) — a title card is a retention killer for short-form. Restored the ORIGINAL cold-open: the giant pokeball SLAMS + cracks open (`gp*`) and the `cbV` reveal bursts out. Set `cbV` back to `cbIn*cbOut`.
- **Fable-6-obvious reveal**: rebuilt the cbV burst → "EVERYONE'S WAITING FOR / **FABLE 6** (112px) / [Claude-logo] BUILD IT WITH CLAUDE" — leads with FABLE 6 to match the VO's first line. (Header still carries CLAUDE + AI persistently.)
- **Opening SFX de-obnoxified**: the 0–2.7s had ~11 stacked hits (boom+crash+**vine_boom** meme+swooshdn all at 0.3s, plus double shimmers + the flip whoosh). Cut to 3 clean ones: soft riser + ONE swoosh+boom on the slam + ONE crack-open shimmer.
- **Mix rebalance** (VO measured RMS 0.049 was only 4:1 over music and quiet in absolute terms because sparse transients hold the average down): applied a **gentle tanh soft-limiter** to the VO wav (tames peaks → +~2dB headroom to push level; `.bak_v9` saved), then VO Audio volume 1.15→**1.42**; music envelope roughly halved (0.1/0.092/0.08 → 0.066/0.056/0.05); ambient beds ~−40% (breeze .06→.036, crowd .05→.028, hum .055→.036); **SFX_GAIN 0.6→0.38**. Result: mix peak 0.911 / 0% clip, overall RMS 0.049→0.066 (+2.6dB), VO:music now ~8:1. ⚠️ Couldn't verify subjective balance by ear — told Alex to listen + can fine-tune.

## v12 pause-trim + early-scene engagement (2026-07-14) — delivered `52_Claude-build-fable6_v12.mp4` (62.1s)
Alex: remove unnecessary long pauses; "BUILD IT YOURSELF" text overflowing its card; make each scene (esp. the beginning) more interesting / more stuff going on.
- **Pause trim −1.09s** (63.25→62.14s). ⭐ SAFE RECIPE (reusable): VO pauses can only be cut where the silence sits **just BEFORE a scene boundary** — cut the silence, then shift that L[i] **and all later L** earlier by `D_sped*1.04`, shift caption words after by `D_sped`, and drop durationInFrames by `D_sped*30`. This preserves every scene's internal VO↔visual sync (scene internals are `lf`-relative, so scene-start and its VO shift together). Cutting silence MID-scene would desync that scene's internals — don't. Cuts made (sped sec, all verified rms<0.003): [24.26,24.52] [39.58,39.83] [51.38,51.58] [58.90,59.28]. New L=[0,6.62,17.52,20.88,25.27,31.45,40.91,52.942,60.566], CUT=64.546, duration=1863. (`.bak_v11` = pre-trim VO.)
- **Text overflow fix**: R0 "BUILD IT YOURSELF" fontSize 33→27 (494px card − 108 sprite − padding ≈ 332px column; 33px nowrap overflowed).
- **Early scenes — more going on** (from the retention audit's un-implemented items): **R1** now has floating damage numbers (−44/−29/−19 rising+fading on each hit at 1.9/4.3/6.6s), a knockout screen-darken, and a red **FNT** stamp at +7.0s so the faint is the real climax (was the softest beat). **R2** now has a running **n/4 ONLINE** tally chip that pops per slot ignition + a **SYSTEM ONLINE** seal snap + white flash at `done` (+3.0s) — the unified payoff it lacked.
- Verified: duration 62.14s, mix peak 0.879 / 0% clip, caption sync intact late (54s), overflow gone, all new beats land.

## v13 middle-scene retention polish (2026-07-14) — delivered `52_Claude-build-fable6_v13.mp4` (62.1s) — CURRENT
Finished the last un-implemented HIGH-priority items from the retention audit (the middle scenes still plateaued):
- **R3 (Memory)**: stat numbers now LAND — `memLand`/`recLand` ramps at +3.18/+3.48s drive a `Math.sin(p*PI)` pop (scale ×1.55 + white-hot color + colored bloom) as each bar tops out. Was: climbed 0→100 then just stopped. + ding SFX.
- **R4 (MCP)**: killed the ~1.4s dead patch (1.85→3.3s) with a **"SYSTEM LIVE" surge** at +2.35s — `surge` flares the hub (r ×1.6) and rips 2 expanding wavefront rings out from it; also added `lockPop(i)` per-node connect pops. + levelup/swoosh SFX. Verified clearly visible.
- **R5 (Team)**: **THUNDERBOLT shockwave** at the deploy peak (+2.35s) — `boltHit` drives an expanding yellow ring from the lead + a strobing electric radial flash, so the 1→20 fan-out no longer finishes silently. + boom/levelup SFX.
- The global KICKS zoom-punch already covered these beats (L[3]+3.4, L[4]+2.4, L[5]+2.35). Verified: 62.14s, mix peak 0.879 / 0% clip.
⇒ The whole retention audit's HIGH-priority set is now implemented across R1–R8. Remaining audit items are medium/low only (per-row stamps, hero second-beat, move cascade, speed-lines) — see the workflow journal if ever wanted.

## v14 CLAUDE THEMING pass (2026-07-14) — delivered `52_Claude-build-fable6_v14.mp4` (62.1s) — CURRENT
Alex: "more stuff alluding to Claude in the beginning + throughout so it's clearly a Claude video… even the MCP part make it Claude themed… interesting and funny."
⭐ LESSON: **real Claude product nouns land harder + funnier than generic "AI" framing** — the dev audience recognises them instantly. Swaps made:
- **R3 Memory**: `MEMORY.dat` → **`CLAUDE.md`** (the actual memory file) withdrawn from the Pokemon PC; `BOX β` → **`~/.claude`**. Best single gag in the reel.
- **R4 MCP** (Alex called this out): generic FILES/APPS/INTERNET → **real MCP servers** `filesystem` ("read + write your files") / `github` ("opens its own PRs") / `chrome` ("drives your browser"); `MCP WIRED` → `MCP · 3 SERVERS`; `NO TOOL FOUND` → **`NO MCP FOR THAT`**. (Icon switch keys renamed to match: `k==='filesystem'|'github'`.)
- **R5 Team**: `1 LEAD → 20 SPECIALISTS` → **`1 OPUS → 20 HAIKUS`** (real model names = the trainer/team gag, and it matches the VO "one lead running 20 specialists"); `LEAD CLAUDE` → **`OPUS 4.8`**; `×20 RUNNING` → `×20 HAIKU AT ONCE`.
- **R1 Wild battle**: the GLITCH's attacks → **`CONTEXT LIMIT`** / SHADOW BALL (kept, Gengar signature) / **`RATE LIMIT`** (the KO — bumped sz 46→66 so the climax is the biggest hit); box line → "CLAUDE forgot the whole chat!".
- **R0 beginning**: `TRAINERS IN LINE` → **`DEVS REFRESHING X`**.
- **Panel window-chrome labels** (the macOS-dots strip reads as a window, so it brands every scene): R0 `claude.ai · ROUTE 0`, R4 `claude · mcp servers`, R5 `claude · subagents`.
- 🐛 Fixed a real occlusion: the lead label sat *behind* the 20-pokeball grid (rendered as "PUS 8") — added `zIndex:24`. This was the long-standing "LEAD CLAUDE partly behind trainer" polish item.
- Verified: 62.14s, mix peak 0.879 / 0% clip.
💡 UNUSED Claude-gag ideas for future reels: HAIKU→SONNET→OPUS as a literal 3-stage Pokemon evolution line; "You're absolutely right!" as a useless move; context-window as an HP bar; Artifacts/Projects/Claude Code as items.

## v15 leading-pause fix (2026-07-14) — delivered `52_Claude-build-fable6_v15.mp4` (61.8s) — CURRENT
Alex: "the beginning of the VO there's a pause 0 needs to be fixed."
- The VO had **0.69s of dead silence before the first word** — a cold-open killer. Trimmed 0.40s (cut [0.02,0.42], verified rms 0.00000) → **onset 0.69s → 0.29s**, which now lands the first word ("Everyone's…") exactly on the giant-pokeball SLAM (0.28s). Nice accidental sync.
- ⭐ NEW RECIPE (the leading pause is a special case — the earlier "cut only before a scene boundary" rule can't apply, there's no earlier scene to shift): **keep the slam on the raw `lf`** so the cold-open drop still plays from frame 0, and add `const SH = fr(0.40); const lfs = lf + SH;` at the top of R0 — then swap `lf`→`lfs` in every POST-slam beat (tick/lockP/moveUp/trainerIn/windUp/throwP/landed/wobbleP/openP/burstO/cardP/statP/matchP/ballO/streakO/wildIn/wildHop/captureP/capBonk/absorbFlash/gotcha). Keep `lf` for slamP/settle/gpDrop/gpSlam/gpOpen/cbIn/cbOut/cbSpin. A uniform `lf` offset would have SKIPPED the ball-drop windup — this two-timeline split is why it works.
- Also shifted: all caption words −0.40; L[1..8] −0.416 (=0.40×1.04) → `[0, 6.204, 17.104, 20.464, 24.854, 31.034, 40.494, 52.526, 60.15]`; CUT 64.13; duration 1851; R0's catch SFX −0.40 (2.18/2.8/3.1/3.22/3.92/4.1); R0 KICKS 2.6/3.6/4.5 → 2.2/3.2/4.1.
- Verified: 61.76s, first audio at 0.29s, mix peak 0.890 / 0% clip, caption sync intact late (53.5s).

## Lead magnet + caption (2026-07-14) — DELIVERED to Drive `52 - BALL/` + `Claude-Reels-Final/`
- **`BALL - The Fable 6 Setup.docx`** — the guide the CTA promises (memory / MCP / sub agents / the loop, all grounded in real Claude Code features + copy-paste boxes). Full spec in [[lead-magnet-docs]]. Build script `build_ball_doc.js` (⭐ the rebuilt premium helper set — reuse it, the older scripts are gone with their scratchpads).
- **`BALL - caption.txt`** — follows [[caption-structure]]: line 1 = `Comment "BALL" and I'll DM you The Fable 6 Setup…(Follow so the DM lands 📩)`, line 2 = `👇 READ BELOW…`, then the 3 holes + the goal-not-a-script rule + a "didn't fit" list (2 memory paths, the `#` shortcut, scope-one-folder, subagent token cost, no-terminal Connectors path), closes with the CTA again, then hashtags. GATE THE HOW respected (caption gives what/why, the exact prompts stay in the DM'd doc). ZERO em/en dashes.

## ⛔ STILL TO POLISH (minor, non-blocking)
- Top HUD still the Mario level-strip (reads OK as pokeball-collection); the Pokemon-region skyline is now mostly hidden behind the panel/HUD (only roof/badge tops peek) — could pull the panel down or raise the skyline.
- Background-label occlusions (S5 "LEAD CLAUDE", S7 "SKILLS", S8 follow-bar) still partly behind the trainer sprite.
- VO resample is linear-interp (tape-style, +~0.68 semitone) — imperceptible but a phase-vocoder would be cleaner if ever needed.

Cross-links: [[skills-mario-remake]], [[reel-build-gotchas]], [[alex-vo-recordings]], [[script-factory-pipeline]], [[reel-no-emoji-no-lowopacity]].
