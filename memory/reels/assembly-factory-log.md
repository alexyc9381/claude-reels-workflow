---
name: assembly-factory-log
description: "Factory log for ASSEMBLY (run Claude as a software FACTORY — a line of 7 single-job agents, the first READ-ONLY so a wrong assumption never propagates into your whole codebase; CTA ASSEMBLY). Anchor: raycfu DbEW382vRRV 2.69x/4.3d (Outlier scan 2026-07-25, hook transcribed). ⛔ CHART-68 adjacency — OVERRIDE-BY-ALEX. PROVISIONAL."
metadata:
  node_type: memory
  type: project
---

# ASSEMBLY — factory log

**Reel #: TBD · Keyword: ASSEMBLY** (verified free vs REELS.md + factory-log list; ⛔ FACTORY-37 and CHART-68 are taken)
Opened per [[factory-log-first]] when Alex picked the raycfu software-factory comp from the 2026-07-25 Outlier sweep ("software factory is a good idea here do that").

## STAGE 0 — SOURCE (Door A, real transcribed comp)
- comp: **raycfu DbEW382vRRV — 148,917 views / 2.69x / 4.3d** (Outlier scan 2026-07-25; median 56,152). HOOK (transcribed):
  "Most people don't realize the reason their AI code keeps breaking is one agent doing six jobs at once... analyst, architect,
  backend, frontend, tester, reviewer all at once. Wrong assumptions in the planner become wrong code everywhere... Here's the
  setup: 7 Claude Fable agents. Agent 1 = researcher, completely read-only. Before any code..."
- transfer_hypothesis: builder pain (AI code breaks) is felt by every Claude-Code user; the fix is a concrete SYSTEM (7-agent
  line) with a comment-gated setup = our mechanic. Fable-agents = on the hot model. lever none.

## STAGE 1 — KILL-GATE (PRE-CHECK)
- ⛔ COLLISION (the binding flag): **CHART-68** = "split Claude into specialist agents → bulletproof work" (shipped). Same FAMILY.
  Also FACTORY-37 (assembly-line, but that was Sol mascots doing 50 BUSINESS tasks cheaply — different topic), CREW-33 (C-suite),
  GRID-47 (lead spawns 4 subagents, parallel PRs). DIFFERENTIATION (must hold or it's a CHART re-run):
    - CHART hero = the ADVERSARY (attacker breaks the work → bulletproof / red-team).
    - ASSEMBLY hero = the READ-ONLY RESEARCHER + separation-of-concerns (agent 1 can't touch code; locks facts FIRST so a wrong
      assumption never propagates). Insight = "role overload → early guess → bug spreads everywhere," fix = one-job specialists.
  → Lead HARD on the read-only-researcher / no-guessing visual, NOT a generic line of agents. Max-distance option: drop "factory"
    framing, make it purely "the read-only agent that stops Claude guessing."
- ⛔ OVERRIDE-BY-ALEX (2026-07-25): building despite CHART adjacency, on Alex's direction. Differentiated as above. Logged per protocol.
- R10: fresh (raycfu 4.3d, current) → PASS.
- CAPTURE / TRUTH-REQUIRED: the 7-agent build + "works the first time" = real captured run required (Stage 4.5) or reword.
- GATE-THE-HOW: read-only-researcher INSIGHT is the hook; full 7-role wiring + prompts + handoffs gated → comment ASSEMBLY.
  (mild tension: the insight is part of the how; mitigate by gating the COMPLETE system, DROP/SKILLS-style.)
- AUDIENCE (harsh): builder-tier (~5-6) → OVERRIDE per [[raycfu-lane-preferred]].

## STAGE 2 — STRUCTURE
- WINNING-FORMULA / INSIGHT spine: hook (why your code breaks = role overload) → insight (one guess propagates) → fix
  (factory line + read-only researcher first) → payoff (works first time) → outlasts (wire once) → CTA ASSEMBLY. Single dark panel. ~40s.

## STAGE 3 — DRAFT v1
- keyword ASSEMBLY. CTA cut hard on keyword.
- HOOK: "Here's why Claude keeps breaking your code. You're making one Claude plan it, build it, test it, and review it all at once. So it guesses wrong early, then builds your whole app on top of that guess."
  - alt: "Most people run Claude like a solo genius. The people who actually ship run it like a factory."
  - alt: "One wrong guess early, and Claude spreads that bug through your entire codebase before you notice."
- FRAME-1 (mute): one Claude juggling 6 labeled hats → red bug spreads → clean assembly line of agent stations, first stamped "READ-ONLY · FACTS LOCKED" → green SHIPPED ✓ feature; header "Stop making one Claude do six jobs."
- VO v1:
  "Here's why Claude keeps breaking your code. You're making one Claude plan it, build it, test it, and review it all at once. So it guesses wrong early, then builds your whole app on top of that guess. And that's the trap. One bad assumption up front doesn't stay small. It spreads into every file before you ever notice it's wrong. The people who actually ship don't run Claude as one genius. They run it as a factory. A line of separate agents, each doing exactly one job. And the very first one is read-only. It can't touch your code at all. Its only job is to find the facts and lock down the truth, so nothing after it is ever building on a guess. So what reaches you was planned before it was coded, and built by a specialist that only does that one thing. A feature that works the first time, instead of a pile of bugs you babysit. You wire the line up once, and every feature after that comes off it the same way. The full seven-agent setup is in a free guide. Follow and comment ASSEMBLY."
### DRAFT v2 (Alex 2026-07-25: "read-only angle not good — frame it as a SOFTWARE FACTORY from the beginning + a viral hook FORMAT template")
- DIRECTION: drop the read-only-researcher differentiation; embrace the software-factory framing head-on (CHART adjacency accepted,
  override stands). Distinction now carried by the VISUAL (literal factory conveyor line), not a different concept.
- ⭐ VIRAL HOOK FORMAT = "THE MACHINE REVEAL" (reusable template): "This is a [MACHINE] built entirely out of [Claude agents].
  [simple input] goes in one end, and [impressive finished output] comes out the other." (variants: transformation-flex
  "I turned one Claude into a 7-person team that ships features while I sleep"; contrarian "one Claude writes broken code, a
  factory of Claude agents ships working software.") — save this template for future "AI builds X" reels.
- FRAME-1 (mute): factory line of Claude-agent stations; a note card "add login" rides the conveyor in → each station stamps it →
  glowing SHIPPED ✓ feature rolls off; header "A software factory made of Claude agents."
- VO v2:
  "This is a software factory built entirely out of Claude agents. One sentence goes in one end, and a finished, tested feature comes out the other. Instead of one Claude trying to do everything and breaking your code, you split it into a line of agents that each do one job. A researcher, an architect, a builder, a tester, a reviewer, passing the work down the line like a real assembly line. You hand it one feature to build, and you just watch. Each station does its single job and hands off, and nothing moves forward until the station before it signs off. What rolls off the end isn't a rough draft full of bugs. It's a finished feature that actually works, built while you were doing something else. You build the factory once, and it runs every feature you throw at it after that. The full seven-agent factory setup is in a free guide. Follow and comment ASSEMBLY."
- FLAGS: CHART adjacency accepted (override); factory/conveyor VISUAL carries the distinction. "works while away" = TRUTH-REQUIRED
  (real capture). 7 roles named (the line) but full setup/prompts/handoffs gated → comment ASSEMBLY.
- NEXT: capture a real 7-agent run → Stage 4 gate (RULES LOGGER + COLD VIEWER; COMP FIDELITY has comp raycfu DbEW382vRRV) → Stage 6 storyboard.

Pairs with [[script-factory-pipeline]] · [[reel-winning-formula]] · [[gate-the-how-in-scripts]] · [[raycfu-lane-preferred]] ·
[[reel-chassis-cinematic-not-abstract]] (CHART-68, the collision guard) · outlier-engine/runs/2026-07-25/NEXT-BATCH.md.
\n
## STAGE 4.5 — VO RECORDED (Alex, 2026-07-25) → `~/Downloads/ASSEMBLY.m4a`
1:36.6 raw, ~8 "cut cut" retake markers. ⛔ Built throughout under a WRONG number, **70** — that slot was already
taken by `70 - LANGFUSE` on Drive. Caught only at delivery time by checking the Drive listing before creating the
folder. **Correct reel # = 75** (69 SERENA shipped; 70-74 = LANGFUSE/LEADS/UNLOCK/ROUTE/SLOP, all taken).
Internal source files (public/70_assembly_vo.wav, ClaudeAssemblyReel.tsx composition id, etc.) still say 70 — the
finished mp4 is self-contained so this is cosmetic, but a future session should rename them before reusing 70_ as a
prefix, since [[reel-asset-name-collisions]] means a stray `70_*` write could still clobber this reel's own assets.
⭐ LESSON: check the Drive folder listing for the true next-free number BEFORE picking one, not after — "the next
number after the last one I shipped" is not reliable once multiple sessions are building in parallel.
⚠️ whisper errors to fix in captions: **"cloud" = "Claude"**, **"locks the fax" = "locks in the FACTS"**.

**RECORDED SCRIPT (retakes spliced, keep the last complete take at each cut) — differs slightly from draft v2:**
1. This is a software factory built entirely out of Claude agents. One sentence goes in one end, and a finished,
   tested feature comes out the other.
2. Normally you just tell Claude to build the whole thing. But that one Claude is playing researcher, architect,
   builder and tester all at once.
3. So it guesses wrong early, and it builds your entire app on top of that guess.
4. You think a smarter model would fix that, but it doesn't.
5. So the people who actually ship six-figure apps build it completely differently.
6. They split Claude into a line of agents that each do one job.
7. The researcher locks in the facts and hands it off to the builder. The builder writes the code and hands it off to
   the next person, but nothing moves down the line until the station signs it off.
8. So you give the system one feature, and you just watch it travel down the line.
9. And what rolls off the end isn't a buggy first draft. It's a feature that actually works, built while you were asleep.
10. And here's the best part. You build the factory once, so every feature after that rolls off the exact same line.
11. The full seven-agent setup is in a free guide. Comment ASSEMBLY.

⭐ v2's "read-only researcher" differentiator is GONE from the recorded take (Alex's call). The CHART-68 distinction is
now carried ENTIRELY by the literal factory/conveyor VISUAL + the sign-off gate. That raises the bar on the board:
if the line does not look like a real factory, this reads as a CHART re-run.

## ⛔ CONSTRAINTS CARRIED IN from the SERENA round (do not relearn)
- No full-frame shake; never translate an `inset:0` container. Wobble the OBJECT (period >= 1s, amp <= 2px).
- Stage motion across the WHOLE scene, never fire it all in second 1. Run the frame-diff audit BEFORE showing a cut,
  and eyeball the hook anyway (the metric is fooled by shake).
- A mover you cannot see is not a mover: LIGHTNESS separation, clear lane, no heavy blur.
- Sprite legibility: costume accents only, never a drape over the face; arms only while actually bearing a load.
- Prefix every asset with **70_**. Hard cut on "Comment ASSEMBLY".
- Caption file must follow [[caption-structure]] (CTA line 1 + follow tag, READ BELOW line 2, blank line every 1-2 lines).

## STAGE 6 — STORYBOARD: `storyboards/70-assembly.md` (filename stale, content is the shipped board). Hook variant A
"Wonka Contraption" approved by Alex (B/C built as alternates, not used). All 7 scenes built to the board.

## ✅ SHIPPED 2026-07-26 → Drive `75 - ASSEMBLY` (cloud-verified via Drive MCP, not the lying local mount)
- `75_Claude-ASSEMBLY.mp4` (21.5MB, 1206f / 40.2s) · `75_ASSEMBLY_caption.txt` · `ASSEMBLY - The Assembly Line.docx`.
- **Ship-gate 8/8** (verify_reel.py): VO onset 0.000s, all SFX cues fire, captions monotonic, ends tight.
- ⛔⭐ **A "cut cut" retake SURVIVED an early splice and rode through every downstream edit** (speed change, trim,
  two rounds of de-gapping) because the original splice kept one long 27s take (67.1-94.0s raw) on the assumption it
  was clean, and nothing ever re-scanned INSIDE an already-kept segment for an embedded flub. Alex caught it by ear at
  ~25s in a finished render; whisper could NOT find it even amplified +22dB and isolated — only a direct RMS scan for
  the silence→short-burst→silence energy SIGNATURE (two ~0.3-0.4s blips) located it. ⭐ LESSON: after any VO splice,
  don't just transcribe each KEPT segment once — scan every kept segment's raw waveform for that burst signature too,
  since a retake buried mid-segment is invisible to both a full transcript and a short isolated-window transcript.
- **DOCX** grounded in the REAL Claude Code subagents feature (`.claude/agents/*.md`, YAML frontmatter, verified via
  search 2026-07-26): ships all 7 station files (researcher/architect/builder/tester/reviewer/fixer/shipper) + the
  orchestration prompt that turns them into a gated line (reviewer can REJECT, fixer reworks, only then shipper runs).
  Honest framing: the "nothing moves until signed off" behavior is a PROMPTING pattern layered on the real subagents
  feature, not a separate hard-enforced mechanism — never overclaimed as one.
- Caption: comment-first, READ BELOW, 1-2-line rhythm, zero em/en dashes — mechanically verified before shipping.
