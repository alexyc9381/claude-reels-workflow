---
name: evolve-factory-log
description: "Factory log for EVOLVE (a Claude agent that fails once, rewrites its OWN instructions, then nails the same task on run 2; CTA EVOLVE) — self-improving agent, cutting-edge slate"
metadata: 
  node_type: memory
  type: project
  originSessionId: f882962d-28c5-45a7-9d94-823ce95b54b2
---

# EVOLVE — factory log

## STAGE 0 — SOURCE
- door: A — cutting-edge slate v2 (next5-v2.js, 2026-07-11). Comp: AI Jason "build PROACTIVE agents & a SELF-IMPROVING company" + the peaking self-improving/compounding-agents theme (David Ondrej "best AI engineers", IndyDevDan agentic engineering).
- transfer_hypothesis: "an AI that gets smarter every run" is the freshest builder dream this week; the pain ("you keep correcting the AI on the same thing") is universal to every AI user, and the fix produces a hard before/after ARTIFACT (run 1 red -> run 2 green) that is fully mute-readable. Non-finance. deadline_batch NO. lever none.
- ⛔ WATCH-OUT (from the gate): do NOT let the payoff be the self-authored "learned:" note (that is the VAULT-38 cerebral insight-file failure). The PROOF must be Run 2 visibly avoiding Run 1's mistake and shipping GREEN. Distinct from IMPRINT (you write static .md files to SOUND like you) and LOOPS-01 (loops but never mutates its own memory): here the AGENT edits its own instruction file AFTER a real failure and proves it next run.

## STAGE 1 — KILL-GATE (harsh)
1 frame-1 receipt: PASS — split: Run 1 red "build broke", a green diff "+ always back up first" writing into the agent's own file, Run 2 green "shipped". Decodes "it broke it, taught itself, then fixed it" <2s.
2 cerebral: PASS (functional) — payoff = a build going RED -> GREEN (state change on screen), not an insight file. The note is the BRIDGE, the green run is the payoff.
3 input-exists: PASS — a task you already give Claude (a bug/build/edit); no dataset to assemble.
4 one-breath restate: PASS — "give Claude a task -> it fails, writes the lesson into its own rules, and ships the same task clean next run." 1 hop.
5 proof-shot: PASS — run-1 red vs run-2 green, a real captured session (TRUTH-REQUIRED, Stage 4.5).
6 no negation + value noun <=w12: PASS — "AI agents" w3, "mistake" w6; opens on a stated pain, not a "the smartest move isn't" negation.
7 lever: PASS (none). 8 qualifier: PASS (1: uses an AI agent / Claude — near-universal for the channel's audience; framed on the universal "you keep re-correcting the AI" pain). 9 first-order task: PASS — stop re-correcting the AI / get reliable output; pays now.
- AUDIENCE (harsh): 10 -2 (1 qualifier: uses AI/agents) -0 (consumer-legible pain, phone) -0 (value = a working/green result, screenshot-able) = 8. PASS.
- COLLISION: nearest = IMPRINT (agent-edits .md) + LOOPS-01 (self-prompt). DISTINCT: EVOLVE = the agent AUTONOMOUSLY rewrites its own instructions AFTER a failure and PROVES it via run 2 (IMPRINT is static/you-authored; LOOPS-01 never mutates memory). Must nail this on screen.

## STAGE 2 — STRUCTURE
- BEFORE/AFTER (run-1 fails -> run-2 self-corrected). Cold-open on the transformation, ZERO spoken-prompt walkthrough (rotates off the follow-along family). target_seconds 40 (cap ~172 words).

## STAGE 3 — DRAFT v1
- word_count ~104. HOOK GATE (10 drafted, top 3 across 3 families):
  - PRIMARY (capability + contrast): "Most AI agents make the same mistake forever. This one rewrites its own instructions so it never does it twice." 9/10 (value noun w3, contrast, mute-read via red->green, no negation-open, fresh family).
  - alt A (you-pain): "You keep correcting Claude on the same thing. This setup makes it fix itself so you only say it once." 8.5.
  - alt B (demo/number): "I watched an AI agent break my build, teach itself the fix, and ship it clean on the next run." 8 (first-person = TRUTH-REQUIRED).
- GATE-THE-HOW: VO names the mechanism at rough level (writes the lesson into its own instructions, backs up, ships); the exact self-improve HOOK + the reflection prompt that appends ONLY verified learnings + the run-2 regression check = gated.
- TRUTH-REQUIRED: the run-1-fail -> self-edit -> run-2-green must be a REAL captured Claude session (Stage 4.5, pre-record). Numbers/example (delete-without-backup) labeled illustrative demo.

### DRAFT SCRIPT (v1)
HEADER (mute, top-third): "Claude that fixes its own mistakes 🔁"
FRAME-1: split — LEFT "Run 1" pane, a task with a red "BUILD BROKE ✗"; a green diff line "+ learned: always back up first" writes into the agent's OWN instructions file; RIGHT "Run 2" pane, same task, green "SHIPPED ✓". The red->green is the mute hero.
B1 "Most AI agents make the same mistake forever. This one rewrites its own instructions so it never does it twice."
B2 "You give it a task, and the first run it breaks something. It deletes a file, the build goes red."
B3 "But instead of waiting for you to catch it, it writes the lesson straight into its own instructions."
B4 "Then you run the exact same task again. This time it backs itself up first, and it ships. Green."
B5 "Every mistake it makes, it makes once. Give it a week and it barely needs you."
B6 CTA "The setup that makes it self-correct is in a free guide. Follow and comment EVOLVE."

## STAGE 4 — GATE RUNS (3), NON-CONVERGING — EVOLVE has premise-inherent short-form problems:
- RUN 1 (wf_464c59e8-848): 6·7·6·6·**4**·9, 1 blocker. Coder framing tanked topic-breadth.
- RUN 2 (wf_f21d75b4-5cf): 7·6.5·7·7·6·9 + audienceSize **5**, 1 blocker. Job-agnostic VO = cosmetic dodge: the gated MECHANISM (self-editing rulebook + auto-rerun) is builder-tier, so the desktop -3 applies to the SETUP; + GREEN collision (both red→green self-fix reels).
- RUN 3 (wf_6e6436b9-765, cross-session v3): 6·6·6·7·7·8, 2 blockers. Cross-session reframe fixed the GREEN collision but introduced a bigger one: the payoff became PROOF-OF-ABSENCE (a fresh chat that's "already correct" is a non-event, not a watchable <2s state change).
- THREE inherent issues the gate cannot wording-fix: (a) audience is builder-tier (~6) — EVOLVE's value IS a power-user setup; (b) the autonomy (self-editing = the whole novelty vs IMPRINT) is invisible on mute; (c) the payoff is an absence (either collides with GREEN or shows nothing).

## STAGE 3 — DRAFT v4 (FINAL — the "VISIBLE CATCH" fix for the payoff; presented to Alex honestly, NOT re-gated — remaining gaps are premise-inherent)
- KEY FIX (resolves the payoff non-event): the hero is a VISIBLE CATCH — in a NEW task the recurring mistake STARTS to form, then the learned rule CATCHES + flips it GREEN in-place ("↙ applied from your rulebook"). A watchable red→green state change, distinct from GREEN (GREEN = loop on one test suite; EVOLVE = a learned rule auto-catching a NEW mistake). + header=OUTCOME / B1=pure PAIN (no dup) + autonomy front-loaded to B2 (~s4) + concrete recurring mistake (a date format) + compounding shown as a GROWING rulebook.
- HONEST STATUS: builder/founder-lane reel, audience ~6 (OVERRIDE-BY-ALEX: he chose the cutting-edge builder lane + rejected consumer gimmicks). Ships ONLY after Stage 4.5 real captures: (1) a session with NO user turn between the slip and the rule-append (autonomy), (2) the visible-catch on a new task, (3) a real growing rulebook. GREEN is the cleaner-gating sibling if a safer bet is wanted.

### DRAFT SCRIPT (v4 FINAL)
HEADER (mute, top-third, has "Claude" for OCR): "Fix a Claude mistake once, for good 🔁"
FRAME-1: a real Claude session on an everyday task. t=0: the ONE recurring thing shown WRONG (e.g. it wrote the date MM/DD when you always want DD/MM) with a red "✗ again" tag = sole hero. ~s4: the agent writes "+ rule: dates as DD/MM" into its OWN rulebook with a small "↙ it wrote this" autonomy tag (bridge, synced to B2). ~s7 HERO: a NEW task, the same mistake starts to form, then its rule CATCHES it and flips it GREEN in-place with "↙ applied from your rulebook" — a watchable red→green.
B1 PAIN "You fix the same AI mistake in every single new chat."
B2 MECHANISM+AUTONOMY "This one catches its own slip and writes the fix into its rules itself, no reminder from you."
B3 HERO (visible catch, "So" causality) "So the next time that mistake creeps in, its own rule catches it and flips it green on the spot."
B4 COMPOUNDING (shown as a growing rulebook) "Correct it once, it is handled for good, and its list of your fixes just keeps growing."
B5 CTA "The setup that makes it self-correct is in a free guide. Follow and comment EVOLVE."
- word_count ~74 (~30s). Zero em dashes. Keyword EVOLVE final + hard cut. GATE-THE-HOW: rough mechanism spoken; the exact self-improve hook + reflection prompt (append only verified learnings) + regression check gated. TRUTH-REQUIRED: the 3 captures above (Stage 4.5, pre-record).

## ALT EVOLUTION HOOKS (2026-07-13, Alex-directed trial variants)
Two swappable hook variants on the EVOLVE solo chassis (ClaudeEvolveSolo.tsx `variant` prop; comps ClaudeEvolveSoloEvoA/EvoB in Root.tsx; rest of reel byte-identical). Built the "tiny -> bigger -> bigger" evolution Alex asked for, then rebuilt FUNNY after his note (headers must not say "the evolution of AI" BS; every form must be a gag):
- Shared `EvoForm` component, 5 funny forms: v1 "confidently wrong" (wobbling dunce cap) -> v2 "panic-googling" (sweat + trembling coffee) -> v3 "ok it's cooking" (shades + flame) -> v4 "galaxy brain" (brain hat + orbiting planets) -> v5 "FINAL BOSS" (crown + halo + lightning + cape + fireworks, oxblood label).
- Hook A "WAIT FOR THE FINAL FORM": single hero levels up 44->262px center stage, FORM x/5 counter, IQ meter pinning to infinity, aura rings, per-stage white flash + chime + riser into a crash MEGA slam.
- Hook B "FROM INTERN TO FINAL BOSS": march-of-progress line of all 5 forms lit L->R by a spotlight sweep, rising gold dashed arrow, cave-dark -> tech-bright backdrop, funny label pills per form.
- LESSONS: (1) never fire the evolution white-flash at t=0 - it washes the stun-gun frame (flash on evolution beats only, tS.slice(1)); (2) critic scored the first pass 5-6/10 for shallow escalation + generic header + plateau climax - fixed with steeper size curve, distinct MEGA terminal form, pre-filled meter, funny per-form labels.
- Delivered: Trial Reels/EVOLVE_evoHookA_powerup_REVIEW.mp4 + EVOLVE_evoHookB_march_REVIEW.mp4 (58.11s, onset 0.0) + Drive/45 EVOLVE/ same names.
