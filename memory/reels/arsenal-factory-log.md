---
name: arsenal-factory-log
description: "reel 69 ARSENAL factory log — 4 free GitHub repos that upgrade Claude (Code Router / Repomix / Graphiti / Task Master), CTA ARSENAL. Warm/cozy no-neon; bold-metaphor repo scenes + money-incinerator hook + GitHub cards + blur CTA. v10 (CALLBACK-density beginning) delivered 2026-07-20; 6+ rejection rounds — META-LESSON: calibrate against SHIPPED reels first. Awaiting Alex feedback."
metadata:
  node_type: memory
  type: project
  originSessionId: 9d3513c4-773b-496e-ac82-174a597b90e1
---

# ARSENAL (reel 69) — factory log

**Keyword ARSENAL · reel #69** (68 = CHART/DESIGN). Built 2026-07-19 from a pre-recorded Alex VO (`~/Downloads/ARSENAL.m4a`). Stages 0-3 were pre-locked (see the workflow copy of this log); this session did Stages 4.5-7 (verify → storyboard → build → render).

## The 4 repos — VERIFIED LIVE (TRUTH-REQUIRED gate satisfied via GitHub API 2026-07-19)
1. **musistudio/claude-code-router** — 35.9k★ — routes easy work to cheaper models (token saver)
2. **yamadashy/repomix** — 27.2k★ — packs whole repo into one AI-friendly file
3. **getzep/graphiti** — 28.9k★ — real-time knowledge-graph memory
4. **eyaltoledano/claude-task-master** — 27.9k★ — one goal → managed task list
Star counts are burned on-screen (wall monitors) so they had to be real. Re-verify if reused.

## VO processing (the delivered VO = `public/69_arsenal_vo.wav`, 38.53s)
- 3 "cut cut" retakes spliced out (measured, single aselect pass from the 48k master, NOT the 16k whisper wav): removed flub A [7.15,13.66] (two "I went through" attempts) + flub B [58.44,61.82] ("the full list…"). Keepers start 13.73 / 61.87.
- Dead air was BRUTAL: two genuine ~4.8s and ~5.5s silences inside the Repomix beat — capped all gaps ≥0.42s to ~0.30s.
- Then **1.04x** (Alex's explicit ask, not the default 7%) + polish chain (highpass 85 · afftdn · acompressor · loudnorm I=-16). 68.4s → 38.53s. 0 "cut" remain (re-transcribed to confirm). See [[video-editing-toolchain]] for the cut-cut method.
- Captions: `src/data/words_arsenal.json` built by aligning the KNOWN script to whisper word-times via difflib (script = spelling truth: Claude/Repomix/Graphiti/Task Master/ARSENAL).
- Soundtrack: **Every Living Breathing Moment** (`~/Downloads/`, "elbm"). Trimmed source mp3 at **4.42s** = first downbeat → `public/69_arsenal_bed.wav` starts loud at 0s (−16dB, matches the 62/66 trim). Bed vol envelope 0.15-0.22, ducks to 0.04 at CTA.

## STORY — "THE LOADOUT" (gear-up hero's-journey; storyboard `claude-reels-workflow/storyboards/69-arsenal.md`)
Struggling solo Claude discovers an ARSENAL and gears up room by room; payoff hero (fully-loaded) escalates past the setup hero (struggling). 7 scenes, `L=[0,5.78,9.66,15.98,23.02,28.48,34.98]`, CUT 38.62, durationInFrames **1159**:
0 HOOK (dim workshop, prompt fizzles → wall of repo-crates reveals) · 1 VAULT (grid of ~40 ✕ crates, 4 ignite gold "100→4") · 2 ROUTER (dispatch room, belt, easy work → cheap grey bots, token counter 100→38%) · 3 REPOMIX (press SLAMS messy code → one "1 FILE" cube that flies to the hero) · 4 GRAPHITI (cork evidence board, pins+red-string knowledge graph = living memory) · 5 TASKMASTER (command room, hero hands goal to a HARD-HAT FOREMAN → kanban board fills, workers tick tasks) · 6 CTA (loaded hero + 4 gear badges + power rings → cream wipe → ARSENAL guide card + comment ARSENAL).
Distinct characters: hero, dim cheap-model bots, hard-hat foreman + workers. Every scene a REAL PLACE per [[reel-chassis-cinematic-not-abstract]] (this is what CHART got wrong — I did NOT repeat it).

## BUILD — clone of `ClaudeCallbackReel.tsx`
New file `src/ClaudeArsenalReel.tsx` (registered in Root.tsx, id `ClaudeArsenalReel`). Reused verbatim: Bg/Panel/Mascot/ScreenHead/ProgressBar/Captions/Sfx. New: `Room` (back-wall + perspective floor + light cone + haze), `Critter` (grounded mascot w/ contact shadow WIDER than sprite), `Monitor` (wall repo-plaque w/ handle+★), `Crate`, 7 `Ar*Body` scenes. SFX cue map re-authored from scratch (`at = L[i]+local`, ≤2 risers). Camera LOCKED per [[reel-motion-hierarchy]].

## ⭐ Lessons this build
- **Motion audit exposed a static reel**: camera-locked + small movers (threads/tickets/cards/fades) → median 0.55 (target ~4-5). A **motion-lift pass** (continuous belt ticket-stream, cube-flies-to-hero, memory-pulse sweep, CTA power rings, rising sparks, pacing worker, bobbing crates) doubled it to **1.02**. Still calmer than the metric ideal — but [[reel-motion-hierarchy]] warns chasing the metric → chaos (reel 66 median 15.3 unwatchable), so I stopped at "alive, not chaotic." **If Alex says "boring / needs more energy," the lever is MORE SCENE CUTS (split the 6-7s rooms into 2 shots each) rather than more jitter.**
- 2-line ScreenHead headers OCCLUDE top-of-scene props → had to push monitors/boards/tags below the header band (taskmaster TODO column, graphiti tag, router counter were all hidden in v1).
- `tools/motion_audit.py` prints SIMULATE's scene labels/boundaries (hardcoded) — the RAW per-second numbers are valid but the scene attribution is wrong; map buckets to ARSENAL's L[] by hand.

## ⛔⭐ REBUILD (v4/v5, 2026-07-19) — Alex REJECTED v3 as "boring throughout, no story arc, no theme, weak hook+ending" + "show the actual repo LOGOS"
v1-v3 were the failure: **7 single-composition tableaus** where each tool "just did a thing" in a calm frame = a UI demo, no conflict/stakes. That is the [[reel-multishot-structure]] boredom pattern.

**The two fixes that mattered:**
1. ⭐ **MULTI-SHOT** — every VO slot cut into **2-3 distinct hard-cut SHOTS** (different comp/scale, each COMPLETE at its own frame 0, a `CutFlash` streak on each cut) → ~18 cuts / a cut every ~2.1s. This is THE "not interesting" fix, per [[reel-multishot-structure]] (Alex's reel-62 lesson: "more scenes, not longer takes"). NOT more motion inside one wide shot.
2. **Dramatic spine "BURIED → BOSS"** (a story-doctor agent studied CALLBACK's scene bodies + killed my first idea). My rejected idea was a "monster + weapons boss fight" — WRONG: forcing dev tools into combat = each tool becomes a metaphor to DECODE (violates LITERAL-BEATS-CLEVER, the CHART-rejection trap) + 2 subjects moving = chaos. The RIGHT spine: one continuous **dev workshop**; the antagonist is a **literal, universal PILE** (avalanche of dev-work boxes) + a red **TOKEN-COST METER bleeding $** — both mute-readable ("buried + broke"), and both HONESTLY unify all 4 repos (cost/context/memory/task-overwhelm). Each repo is an **UPGRADE CRATE (real logo) Claude INSTALLS**; the shop levels up (4 loadout slots + the pile clears slice by slice); ends with Claude running the shop itself (feet up, list self-completing) — **inverting the hook** (buried→in command, the SAME pile swept away). Beat plan lives in `storyboards/69-arsenal-v2.md`.

**Per-scene:** HOOK avalanche SLAM buries Claude ~f15 + meter redlines (the L1 stun-gun, opens ON motion) · VAULT cabinet opens, 4 crates THUD in w/ logos · ROUTER flood redlines → install → sorter shunts easy jobs to a cheap bot → meter flips GREEN "$156 SAVED" · REPOMIX spill → compactor SLAM → one cube → terminal "✓ WHOLE REPO LOADED" · GRAPHITI squeegee wipes board blank → install → blue memory-web springs → wipe bounces off · TASKMASTER drop 1 goal → checklist unspools → clones tick it off, hero kicks back · CTA sweep the pile + 4 logos snap + guide card (GitHub octocat + each repo's real logo + stars) + comment ARSENAL.

**LOGOS (Alex's specific flag):** real assets in `public/logos/ar_*.png` — github (transparent octocat), repomix (orange cube), graphiti (=**Zep** robot on purple, no distinct Graphiti mark), taskmaster (knot+wordmark on BLACK pill), router (**recreated** SVG, no logo exists). ⛔ graphiti+taskmaster have BAKED backgrounds → render every logo inside a **light chip** (`RepoLogo`) so they read on the dark scenes.

⭐ **Motion metric is a red herring here:** median stayed ~0.65 (low) even after the rebuild, because the interest comes from the CUT RHYTHM + drama, not per-frame pixel churn — chasing the metric caused reel-66's chaos rejection. Judge by watching, not the audit number. New reusable props: `Shop`, `WorkPile`, `TokenMeter`, `LoadoutBar`, `UpCrate`, `RepoLogo`, `ClayHand`, `CutFlash`.

## ⛔⛔⭐ v5 REJECTED TOO — "each scene way too boring, backgrounds not detailed enough, it's HORRIBLE, not following my github guidelines" (Alex, 3rd rejection)
⛔ **THE ROOT CAUSE (finally found): I had NEVER looked at the shipped reference reels.** I kept building to my OWN idea of "good" and calling it done. I finally extracted frames from the shipped mp4s (`~/Downloads/Claude-Reels-Final/58_Claude-resume-callback.mp4`, `68_Claude-CHART.mp4`, `37_GPT-5.6-Sol-assembly-line.mp4`) and the gap was obvious: my scenes were **WIREFRAMES** (flat gradient wall + a few plain divs) while the bar is **fully art-directed cinematic SETS** — one-point-perspective corridors with converging walls + vanishing point, **wet reflective floors**, riveted metal machines, brick **furnaces**, hanging **lamps with light cones**, **brass nameplates/gauges/terminals with real text**, packed props in every corner, atmospheric haze. The playbook's **Phase D OVERHAUL** ("first render is a WIREFRAME, never delivered") is exactly the stage I'd been skipping. ⛔⛔ **CALIBRATE AGAINST THE SHIPPED REFERENCE FRAMES BEFORE declaring any reel done — my internal "good enough" was ~5% of the real density bar.**

## THE FIX (v6) — cinematic primitives + parallel scene rebuild
1. Built a reusable **`CineRoom`** (one-point-perspective room: converging side-wall polygons to a vanishing rect, reflective floor w/ receding grid, neon rim strips, haze, dust) + prop kit: **`Lamp`** (fixture+cone+bulb), **`Furnace`** (brick+fire), **`Plate`** (riveted brass nameplate), **`WallClock`**, **`Pipes`**, **`Reflect`** (flipped/faded floor reflection). Proved the look by rebuilding the HOOK myself → instantly matched the CHART corridor.
2. Fanned out **6 parallel agents** (one per scene), each briefed with: the primitives API, the proven `ArHookBody` as the density TEMPLATE, and `CbRewriterBody` (CALLBACK's forge) as the richness bar; each wrote a dense scene body to `/tmp/scene_<name>.txt`, unique `CineRoom seed0` per scene. Assembled via `scratchpad/assemble.py` (splices by scene markers). Result: each scene now packs a machine + 4-6 detailed props (gauges, terminals w/ real mono text, brass plates, clocks, reflections) — e.g. REPOMIX is a full riveted BALER press + hydraulics + furnace + "FILES 2,337→1" counter (modeled on CbRewriterBody), ROUTER has dual needle gauges + req/s monitor + routing machine, VAULT a riveted loadout cabinet, GRAPHITI a framed board + blue memory-web + "48 facts linked" terminal, TASKMASTER a self-ticking BUILD PLAN + feet-up Claude w/ steaming mug, CTA a bright victory corridor w/ FULLY LOADED / LVL MAX plates.
⭐ Fanning rich scene-authoring out to agents (each given the same primitives + a proven template + the reference source) worked well — 6 dense scenes in parallel, all self-validated against esbuild, spliced clean.

## v6 → v7 (color/action/characters) → STILL REJECTED, then v8 the WARM PIVOT
- v6 (cinematic detail) → Alex: still boring, coloring OFF. v7 added warm/varied palettes + costumed sprites + more action (fanned out to agents twice). STILL rejected.
- ⛔⛔⭐ **v7 REJECTION revealed the REAL miscalibration: I'd been building a COLD NEON SCI-FI aesthetic (glowing cyan/teal rim-strips, neon meters, dark corridors). Alex: "NO NEON. no neon animation color palette." + "each repo needs a GitHub repo CARD (stars, info)" + "the comment card is OUTDATED, use the style nowadays" + "scrap it, make a cohesive STORY".**
- ⛔⛔ **THE FIX (v8): I finally studied the RECENT shipped reels' actual look** (`~/Downloads/Claude-Reels-Final/65_Claude-simulate.mp4`, `66_..posts..`, `68_..CHART..`): the house style is **WARM, MUTED, COZY, character-driven** (SIMULATE = a warm brown cafe, a purple problem-monster, apron/beanie characters) — NOT neon corridors. And the CTA "nowadays" = the **final scene BLURRED + "comment KEYWORD" overlaid** (NOT a cream pop-up card).

## THE WARM REDO (v8) — the aesthetic Alex actually wanted
1. New primitives: **`Den`** (warm cozy wood room, soft lamp, soft vignette — NO neon, replaces the rejected `CineRoom`), **`GitHubCard`** (a real github.com repo card: octocat + owner/repo + description + language dot + ★stars + a "Star" button — REPOS now carries `owner/repo/desc/lang/langC`), the `WARM` palette object. Proved the look on one scene first (warm den + card render), THEN fanned out.
2. Story = ONE cohesive cozy world: **Claude's late-night dev DEN** (warm desk, monitor, bulb, plant, mug, bookshelf, night-city window). Overwhelmed (hook: avalanche of work + a purple "chaos BUG gremlin" + red OVER-BUDGET monitor = pattern interrupt) → GitHub search (vault: real GitHub search UI, 4 repos starred out of 100+) → installs 4 repos (each: its **GitHubCard** shown prominently + a warm vignette + costumed helper) → tidy/thriving → **blurred-scene CTA** "comment ARSENAL".
3. Characters costumed (glasses/constr-hardhat/beard/suit) + the gremlin. Warm/muted throughout, no neon.
⛔ Build gotcha this round: an agent wrote `interpolate(cost,[478,60],…)` — DESCENDING inputRange throws "must be strictly monotonically increasing" at render (not bundle). Fixed to `[60,478],[1,0]`. Scan the assembled file for descending literal interpolate ranges before rendering.

## v8 → v9 (BOLD METAPHORS)
v8 (warm cozy) = "slightly better" but Alex: **"represent each repo's info in way more INTERESTING, ABSTRACT ways — scrap how each scene represents the info"** + **"the beginning captures ZERO attention, zero pattern interrupt — completely redo it."** (Note: "abstract" here = bold/creative/striking metaphor, NOT diagram-on-black. Keep warm + GitHub cards — those were right.)
- Approved bold metaphors (via AskUserQuestion): HOOK = a **MONEY BONFIRE** (Claude shovels tasks into a "PREMIUM" machine burning stacks of cash, a red $-counter spiking; then its "clever prompt" magic-WAND fizzles uselessly) — a real cold-open stun-gun. ROUTER = reroute the bonfire → cheap 1-coin lane, flames die, saved-cash mound. REPOMIX = a towering **MOUNTAIN of code collapses/vacuums into ONE glowing amber GEM**. GRAPHITI = **goldfish memory** (a thought pops & vanishes + a literal fishbowl) → a glowing **amber SECOND-BRAIN constellation** (⛔ WARM amber, never neon-blue). TASKMASTER = one **goal-orb bursts into an ARMY of ~8 mini-Claude clones** that swarm-clear the task-mountain while the hero kicks back.
- Kept from v8: warm `Den`, `GitHubCard` per repo, blur-overlay CTA, costumed characters, the VAULT (GitHub-search) + CTA scenes. Only HOOK + 4 repo scenes rebuilt (fan-out to 5 agents, `/tmp/scene4_*` → assemble4.py).

## v9 → v10 (BEGINNING = CALLBACK DENSITY) — Alex: "the beginning is way too UNDETAILED — so much more detail, more components, more SOUND-EFFECTS DESIGN on the beginning scenes. The header '4 free GitHub repos' is NOT enticing. Reference the CALLBACK video and see how we did it."
The money-bonfire idea (v9) was right; the *execution density* of the opening two scenes was still ~wireframe next to CALLBACK. Fix = rebuild HOOK + VAULT to `CbHookBody` density (CALLBACK's `ClaudeCallbackReel.tsx` lines ~3275-3596 = the reference: fully-modeled TALENTSIFT shredder + stamp arm + gag sign + confetti + lamps + bolts).
- **HOOK rebuilt** into a fully-modeled money **INCINERATOR**: riveted furnace body + "PREMIUM API" brass nameplate + hopper feeding cash into a **barred fire grate** (real flames) + pressure gauges + ash/coal pile + hanging work-lamps with light cones + background shelves/pipes. Claude shovels cash while a panicked **suit-Claude** waves a fizzling "clever prompt" magic-wand (sparkle→downer fizzle). Live red **"$2,976 · TODAY · STILL CLIMBING"** burn counter. ⭐ Header upgraded from a flat label to a real hook line: **"YOU'RE BURNING CASH ON CLAUDE"** (big/clay two-tone, chip off).
- **VAULT rebuilt** to den density: desk-lamp light-cone, plant, books, keyboard, mug, pencils, glasses-Claude, string-lights + bunting, framed sunset, bookshelf, night-city window, and a **GITSCAN** search panel listing all 4 repos w/ stars + language dots + Star buttons. Header **"THE ARSENAL — 4 REPOS, $0"**.
- **SFX design densified on the hook** (the specific ask): construction-fire bed + cash-register cha-ching + coin + glitch_counter on the counter spike + alarm + sparkle→wand-fizzle (downer) + lib_magic_reveal+chimehi on the github reveal. Loudness checked: hook SFX peak −3.9dB, VO −4.5dB (both clear). SFX cues use ROOT seconds per [[sfx-root-timeline-trap]].
⛔ Only HOOK + VAULT bodies swapped (assemble5.py); the 4 metaphor repo scenes + CTA unchanged from v9. Both rebuilt frames vision-checked at native 1012×792 before delivery.

## STATUS
**v10 = `out/ar/69_ARSENAL.mp4`** (38.68s · 1080×1920 · 1159f · AAC · 11.5MB, faststart) delivered to Alex 2026-07-20 — CALLBACK-density money-incinerator hook + cozy GITSCAN vault + richer hook SFX + enticing header, on top of v9's bold metaphors / warm-no-neon / GitHub cards / blur CTA. Caption `out/ar/69_arsenal_caption.txt`. NOT on Drive. NEXT: await feedback.
⭐ v10 lesson: "the beginning is too undetailed" = a **density**, not concept, gap — the metaphor was approved, the SET around it was thin. Fix is to clone the named reference scene body (CbHookBody) prop-for-prop (machine + nameplate + gauges + lamps + pile + 2 acting sprites), NOT to invent more. And "the header isn't enticing" = write an actual **hook sentence**, not a topic label.
⭐ Recurring build gotcha: agents sometimes emit `interpolate(x,[HI,LO],…)` (descending input) → throws "inputRange must be strictly monotonically increasing" at RENDER (not bundle). assemble4.py now scans for descending literal ranges; still watch dynamic ones.
⭐⭐ META-LESSON across this whole reel (6+ rejections): **calibrate against the SHIPPED reels FIRST** — I burned 5 versions building to my own idea of "good" (neon cinematic) when the house style is warm/cozy/muted. Also: Alex's asks are LITERAL (GitHub card = show a github card; no neon = no neon) — take them at face value, don't reinterpret.

Pairs with [[reel-chassis-cinematic-not-abstract]] · [[reel-motion-hierarchy]] · [[reel-dead-air-motion-audit]] · [[reels/callback-factory-log]] · [[gate-the-how-in-scripts]] · [[specificity-effect]] · [[soundtrack-onset-at-zero]] · [[video-editing-toolchain]].
