---
name: glm-swap-reel-and-setup
description: "Reel 18 (GLM-5.2 swap: Opus quality in Claude Code at 1/5 the price, CTA SWAP) + the verified GLM-5.2 -> Claude Code setup/pricing for the lead magnet"
metadata:
  node_type: memory
  type: reference
  originSessionId: eb984a59-7042-45cb-93dd-680df7a004ed
---

Reel 18 = "**Stop overpaying for Claude Code: GLM-5.2 = Opus at 1/5 the price**" (CTA **SWAP**). Rides the hottest mid-2026 trend on both Nick Saraev + Nate Herk (GLM-5.2-in-Claude-Code cost-arbitrage). Comp `ClaudeGlmReel` (1131f, 37.8s) in `video/src/`; VO `public/vo_glm.wav`, captions `src/data/words_glm.json`, music `seo_music.wav`. Delivered `18_Claude-code-GLM-swap.mp4`. Brand nuance flagged (it tells people to swap Claude's model for cheaper GLM) — Alex chose to ride it anyway; framed pro-Claude-Code (keep the tool, swap the model). VO had 5 cut-cuts spliced.

**⭐ VERIFIED GLM-5.2 → Claude Code setup (June 2026, for the SWAP lead-magnet doc), sources docs.z.ai/devpack/tool/claude:**
1. Get a Z.AI API key at the Z.AI Open Platform.
2. Either run `npx @z_ai/coding-helper` (automated), OR edit `~/.claude/settings.json`:
```json
{ "env": {
  "ANTHROPIC_AUTH_TOKEN": "your_zai_api_key",
  "ANTHROPIC_BASE_URL": "https://api.z.ai/api/anthropic",
  "ANTHROPIC_DEFAULT_OPUS_MODEL": "glm-5.2[1m]",
  "ANTHROPIC_DEFAULT_SONNET_MODEL": "glm-5.2[1m]",
  "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "1000000"
}}
```
3. `cd project && claude` → `/status` shows `glm-5.2[1m]` (1M context). `/effort max` for hard tasks.
**Pricing:** GLM Coding Plan Lite ~$10-18 / Pro ~$30 / Max ~$80-160 per mo (30% promo discounts common) vs Claude Max $100-200/mo → ~80-90% cheaper ("1/5 the price", "~5x cheaper vs Opus 4.8"). GLM-5.2 released ~June 16 2026.

**⚠️ z.ai BLOCKS headless-Chrome screenshots** — but **thum.io (server-side screenshot, free, no key) gets the REAL page**: `curl "https://image.thum.io/get/width/1280/fullpage/noanimate/https://z.ai/subscribe" -o page.png` → a real 1280×6566 full-page PNG of the actual z.ai GLM Coding Plan site (hero + Claude-Code-style terminal demo + real pricing **Lite $12.6 / Pro $50.4 / Max $112** + Code Arena benchmark). Drop `/fullpage` for above-the-fold only. **This is the go-to workaround whenever the [[screen-reel-github-capture]] headless method is bot-blocked.** Saved `public/refs/glm_zai_full.jpg`; real z.ai "Z" logo via google favicon `https://www.google.com/s2/favicons?domain=z.ai&sz=256` → `refs/zai_logo.jpg`. **v2 (Alex 2026-06-28): "side-by-side must show the OUTPUT not the code / screen recordings of the ACTUAL website / better hook + real logos everywhere."** So I scrapped the Remotion `GlmPage` recreation and used the REAL z.ai page in a `Browser`+`Screen` pan (WhatScene focus≈0.05 on the "GLM Coding Plan" hero, KeyScene focus≈0.22 on the real $12.6 Lite card); hook + CTA use the real Claude sunburst (`CLAUDE_PATH`) + real `GlmLogo` (z.ai Z favicon Img); cost framed **$200→$12/mo** ("a fraction of the price"); and the PROOF scene now shows the rendered OUTPUT side-by-side — two browser frames each showing the real `bh_site_a.jpg` landing page (Opus·$200 | GLM·$12) → "✓ identical, can't tell which built it" — NOT terminal logs. `settings.json` env-var config + `/status` terminal stay as legible Remotion recreations (a config file isn't a "website"). See [[screen-reel-github-capture]] for the annotation system reused here.

**⛔ v3 (Alex 2026-06-28): "the initial visual is STILL horrible — just text and a plain comparison / use the ACTUAL Claude logo / DON'T do the boxes that highlight stuff / screen-record the actual ACTION not random terminal code / the ~24s visual is way too long+boring, replace it / no random-terminal visuals."** The big lesson: **Alex rejects (a) plain logo-vs-price comparisons, (b) static terminals/code dumps as heroes, and (c) rectangle highlight boxes — replace EACH with a dynamic VISUAL MECHANISM that shows the literal action.** I ran a design judge-panel workflow (12 concepts→3 judges) then built the winners — these are now the reusable scene mechanisms in `ClaudeGlmReel.tsx`:
- **HOOK = REDLINE cost gauge** (`HookGauge`): a big SVG semicircular tachometer (green/amber/red arc via `POL`/`ARC` helpers + a rotating needle) pinned screaming-red at $200/mo at frame 0, then the GLM Z (`GlmLogo`) flies in and the needle WHIPS to green $12 with an odometer roll + red→green vignette/Bloom crossfade. A live mechanism, mute-readable. (Removed an early "cash stack" under it — read as a confusing lightbulb base.)
- **CONFIG = reroute patchbay** (`ConfigScene`+`Socket` + `BEZ` bezier helper): a slate-navy device where Claude Code's request CABLE physically unplugs from the Anthropic jack (it desaturates to grey = dead) and re-seats into the GLM/z.ai jack (blue rim ignites), flow-dots recolor clay→blue, and the **two env-vars** lock in as the clean bottom payoff (`ANTHROPIC_BASE_URL → https://api.z.ai/api/anthropic ✓` / `ANTHROPIC_AUTH_TOKEN → z-ai-key ✓`). NO settings.json terminal.
- **RUN = engine hot-swap + falling bill** (`RunScene`): a glowing engine-core socket holds the clay Claude sunburst with live agent work-rows (Read/Edit/test, spinner→✓, looping) + a "build passing" pill; mid-run the core hot-swaps to GLM (model chip flips `Claude Code | Opus`→`Claude Code | GLM-5.2`) WITHOUT the rows pausing (= "same workflow" proven by motion); then act3 = a clean labeled **money column that drops $200/mo → $12/mo** with a "−80%" wax-seal stamp (NOT tumbling slabs — those read as a glitch). 3 escalating beats, no terminal.
- **REAL Claude logo**: `ClaudeSun` (bare clay sunburst, = the simpleicons/`CLAUDE_PATH` shape) — on DARK sockets put it on a cream inner disc so it reads as the logo, not a glowing spark. `ClaudeCodeLogo` = sunburst + Fraunces wordmark. All highlight boxes removed; emphasis via zoom/glow/soft-spotlight (e.g. KeyScene dims around the $12 Lite card — a radial spotlight, not a box).
- **PROCESS**: for a 2x+ rejected creative beat, don't free-hand a 3rd time — run a concept→judge workflow, then ADVERSARIALLY ship-gate the rendered frames with a fresh subagent against the client's exact constraints before delivering (it caught the ambiguous bill + off-brand spark here).

**⛔ v4 (Alex 2026-06-28): "the hook + the config graphic look UNPOLISHED / not visually captivating / very off."** Root cause: my hand-built **flat SVG/abstract graphics read as unpolished diagrams** to Alex (his REAL-screen scenes always pass; the abstract ones get flagged). Fixes that landed: (1) **HOOK gauge → a premium dark INSTRUMENT tachometer** (not flat arcs floating on cream): a glossy dark dial face (radial `#2B3344→#0A0D16` + inset shadows + rim), the green/amber/red redline arc + white ticks/$-labels on it, a **metallic tapered-polygon needle** (with a drop-shadow + tip glow) on a **chrome hub** (`radialGradient`), and a recessed **glowing LCD readout** ("$200/mo" red → "$12/mo" green) — pinned-red → whips to green when the GLM Z flies in. A dark glossy instrument reads premium; flat arcs on cream did not. (2) **CONFIG patchbay → clean premium TILES on the cream brand bg** (killed the dark slab + the "fried-egg" Claude node [sunburst on a white disc in a heavy black ring] + the crude two-tone cable + the broken-looking faint Anthropic node). New `Tile` (rounded-square app-chip: gradient + SH + Sheen + colored glow), `ClaudeGlyph` (bare clay sunburst on a light tile = the real logo look), `CShadow` (contact ellipse); Claude tile → premium **gradient cable** (`linearGradient` clay→blue + soft glow + white-cored light-pulse flow dots + a round plug cap) that re-plugs from a dimmed Anthropic tile to the z.ai tile (blue glow ignites); the two env-vars as clean light cards. **LESSON: when Alex calls an abstract graphic "off/unpolished," the fix is usually depth + real-object fidelity (glossy instrument / app-chip tiles / gradient cables with glow), on-brand cream — not a dark flat slab and not a flat SVG diagram.** Delivered v4.

**⛔ v5 (Alex 2026-06-28): "the initial visual STILL doesn't cut it / isn't interesting / you can't even discern WHAT it is."** Even the polished dark tachometer FAILED — the decisive issue was **LEGIBILITY: an abstract graphic (gauge/dial/patchbay) is NOT instantly readable; a viewer can't tell what it is in <1s, which is fatal for a hook.** The fix that finally landed: **a RECOGNIZABLE real-UI OBJECT.** Process (do this when a hook is rejected 2x+): ran a concept workflow constrained to "instantly legible in <1s, concrete/recognizable, NOT gauge/logo-compare/terminal," it returned 3 (iOS payment-charge alert / iOS Cancel-Subscription screen / retail price-tag markdown); I BUILT ALL 3 AS STILL MOCKUPS (`src/HookOptions.tsx`, registered HookCharge/HookSub/HookTag, rendered frame-0 PNGs) and had Alex PICK via a question rather than guessing a 5th time. **He chose the iOS Cancel-Subscription screen.** Final hook = `HookSubscription` in `ClaudeGlmReel.tsx`: an iOS "Subscriptions" card showing **Claude Code · Renews monthly · $200/mo** (red) with a red **Cancel Subscription** button + a cursor that presses it → the row flips to **GLM-5.2 · in Claude Code**, the price odometer-rolls $200→$12 (red→green), the button turns green **Subscribed ✓**, and a **"one fifth the price"** stamp pops; headline "Stop overpaying for **Claude Code**" (Claude Code stays clay) above. Instantly legible + sticker-shock + a built-in cancel→swap payoff. **STANDING TAKEAWAY for hooks: lean on instantly-recognizable real objects/UI (a charge alert, a subscription screen, a real product screen) over any abstract custom graphic; if rejected repeatedly, render 2-3 concrete still options and let Alex choose.** Delivered v5. (Dead code left in the reel: unused `HookGauge`/`POL`/`ARC` + the `HookOptions` comps — harmless.)

**⛔⛔ v6 — IG REELS FRAMING (Alex 2026-06-29: "the graphic is way too high, not where my eye rests; frame the WHOLE video properly for IG — header/progress bar, captions, graphics"). STANDING SAFE-ZONE SYSTEM for every 1080×1920 reel (sourced: IG Reels central safe zone ≈ 1010×1280, set 220px from top + 420px from bottom → safe vertical band y220–1500):**
- **HERO CONTENT MUST CENTER AT ~y856** (the middle of the 220–1500 safe band = where the eye rests). My heroes had been centering ~700–760 = "too high." Re-anchored EVERY scene's main visual mass to straddle y856 (hook card, the z.ai `Browser` panels, the config tile-chain, the run engine/bill, the proof side-by-side, the CTA via `paddingBottom:210` on the centered flex).
- **Captions: top ~1286** (height ~170 → ends ~1456, inside the 1500 bottom-safe boundary; lower third). Unchanged — correct.
- **Progress bar: top ~200** (just at the top-safe boundary so it clears the notch/status and is reliably visible — Alex repeatedly worries it's not visible on his phone).
- **Browsers/cards must END above ~1200** so they don't collide the caption band (shrank the z.ai `Browser` from h700@top580 [bottom 1280, hit captions] to h624@top556 [center 868]).
- Keep key content within x ~70–960 (right ~120px is the like/comment/share column, esp. lower half).
- **⭐ VERIFY METHOD:** composite an ffmpeg safe-zone overlay on the contact sheet — `drawbox` red top(0–220)+bottom(1500–1920)+right-buttons, a green safe rect (60,220,900,1280), a green CENTER line at y856, a cyan caption line at y1286 — and confirm every scene's hero straddles the y856 line and nothing important sits in red. Delivered v6.

**⛔ v7 (Alex 2026-06-29):** four asks, all done. (1) **Use the real z.ai benchmark chart in the WHAT scene + circle GLM vs Claude.** He couldn't attach the PNG to disk so I RECREATED z.ai's "LLM Performance Evaluation: Agentic, Reasoning and Coding" chart faithfully in Remotion: `GlmBenchChart` (title + z.ai logo + 5-model legend + 4×2 grid of mini bar-charts; data hardcoded in `BENCH`/`BMODELS` for AIME25/GPQA/LiveCodeBench v6/HLE/BrowseComp/SWE-bench/Terminal-Bench/τ²-Bench, global 0–100 bar scale, GLM-4.6 "w/Tools" darker-blue stacked segments). The WHAT scene now shows it (scaled 0.87, centered y856) and at `hl=ramp(116,156)` it **circles the two compared models in the legend** (GLM-4.6 = blue ring, Claude Sonnet 4.5 = red ring) + fades the other 3 models' bars to 0.22. ⚠️ Darkened Claude Sonnet 4.5 from the chart's near-white `#DEE1E6`→`#AEB4BD` so the GLM-vs-Claude bars are actually visible in the comparison. (Note: chart is GLM-4.6/Sonnet-4.5 while the VO says GLM-5.2/Opus — a minor version mismatch Alex accepted by giving me this exact chart.) Replaced the z.ai-page-scroll that was in WHAT; the z.ai pricing page stays in KEY. (2) **Config patchbay "sucks / not captivating" → rebuilt as "The Model Switch"** (config panel winner): a recognizable Claude-Code MODEL-PICKER dropdown — "Claude Opus · api.anthropic.com" (clay, ✓) flips to "GLM-5.2 · api.z.ai" (blue check snaps in + blue row wash) when a cursor clicks it, then the 2 env-var cards rise + a green "Now using GLM-5.2 ✓" pill. Same recognizable-UI + satisfying-state-flip pattern as the approved hook. Killed the cable/tiles patchbay. (3) **First-3s more engaging** on the hook: price `$200` alarm-pulse while red, a swap `shakeX` + a soft `flash` AbsoluteFill on the Subscribed flip. (4) **Hook framing:** header was "quite small" → bumped Fraunces 64→80; card moved DOWN (CT 686→716); captions UP (global 1286→1244). Delivered v7. (Dead code now also includes the patchbay helpers `Tile`/`CShadow`/`ClaudeGlyph`/`BEZ` — harmless.)

Next-idea backlog (from the research workflow, [[claude-reel-topic-engine]]): #2 "Claude runs a $5k/mo service business while you sleep" (CTA SERVICE, on-brand money) and #3 "Claude clones any competitor's website in one prompt" (CTA CLONE, high-retention demo).
