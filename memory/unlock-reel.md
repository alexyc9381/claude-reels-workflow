---
name: unlock-reel
description: "Reel 24 UNLOCK — the first TREND-JACK CURATION LISTICLE reel (3 named Fable 5 moves), replicating the DaDBB43sPOJ structure Alex asked to copy"
metadata:
  node_type: memory
  type: project
  originSessionId: 4cf40acd-d5e7-490d-8f72-803b59af6ae1
---

Reel 24 = **UNLOCK**, the first reel built on the [[creator-video-structure-templates]] **trend-jack curation listicle** (the structure Alex sent me to reverse-engineer, IG reel DaDBB43sPOJ). Comp `ClaudeUnlockReel` in `src/ClaudeUnlockReel.tsx`, durationInFrames 1755 (~58.6s, 30fps), registered in Root.tsx. Delivered `24_Claude-fable5-unlock.mp4` to Final + Drive/Claude Reels + Photos. 3rd Fable 5 reel (after 22 FABLE, 23 SHARP).

**Structure (the replicated template):** (1) TREND-JACK open "Everyone's obsessed with Fable 5, almost nobody uses it right" + real release VideoCard (recognizability); (2) CURATION "went through Anthropic's own Fable 5 Playbook" = a 9-row playbook panel, 3 rows highlight green + a "pulled 3 of 9" badge (borrows authority + teases the 9); (3) primer "does exactly what you say, fast" (caption) + a "3 / prompts worth stealing" transition card; (4) 3 NAMED framework cards each = header(number + name) → mechanism visual → a `StealBox` ("STEAL THIS PROMPT" dark clay box with the exact line); (5) STAKES-RAISING CTA = "The Fable 5 Playbook · 9 prompts worth stealing" guide cover (the 3 named + "+6 more inside") + Comment UNLOCK + "all nine". Progress bar = 3 nodes + star(0.83) + gift reward.

**The 3 named moves (genuinely-useful, specific, grounded — NOT generic; Alex rejected the first generic version):** (1) **The boundary line** — Fable over-executes/rewrites stuff you didn't ask for (Anthropic "state the boundaries"); line "Do exactly this, nothing else. Show me your take first, then stop." Visual: "you asked: fix one bug → ✓ Fixed + ✗also refactored/renamed/rewrote (red)" reined to just the fix. (2) **The rules-first stack** — it follows literally + in order, so front-load constraints; line "Rules first: never X, always Y, format Z. Then the task." Visual: rules slide from bottom(ignored/red) to top(obeyed/green). (3) **The cold-read check** (the big one) — self-review barely works, it agrees with itself; Anthropic found fresh-context beats self-critique; line "Review this as if someone else wrote it. List what's wrong." Visual: self-review "Looks good" (bug missed) → fresh read "Found the bug".

**Scene map L (sec) = [0, 18.9, 31.8, 40.8, 53.6]** (hook is long ~19s, inherent to the trend-jack+curation open). CTA keyword UNLOCK.

**VO pipeline:** "Untitled - 7:3:26, 4.12 PM.m4a", 139.7s raw with MANY cut-cut retakes. Spliced via aselect keep-regions (kept the final clean take of each line; dropped ~10 false-start takes incl. a messy 5-retake stumble on "so make it review the..."), then silenceremove(0.45) + polish → 58.25s. RE-TRANSCRIBE mis-heard "Claude"→"Cloud", "Anthropic"→"Enthropic", "stack"→"step", "itself"→"the self"; corrected those in `words_unlock.json` for captions (the audio is correct).

**Lead magnet (CTA promises "all 9"):** the doc MUST hold 9 Fable 5 power-moves (the 3 shown + 6 more: match effort to task, verify-against-tool-result, memory file, lead-with-the-outcome, raise max_tokens with effort, don't-early-stop, give-an-example, etc.). NOT built yet.

Reused SHARP's infra ([[sharp-reel]]): Bg/Stage/ProgressBar(gift+star)/Captions(cw-merge)/VideoCard/guide-cover CTA/safe-zone translateY(90). Photos import uses the FIXED invocation ([[social-assets-to-camera-roll]]: zsh, DIR then ALBUM).

**Opener asset swapped (Alex: "give me a different Fable 5 video for the beginning"):** replaced the reused butterflies release-thumbnail (`fable_video.jpg`) in the UNLOCK VideoCard with a FRESH real launch-video frame: `public/fable/fable_lineup.jpg` = the Anthropic Fable 5 launch clip's model-lineup card ("Fable" highlighted yellow above Opus / Sonnet / Haiku on grid paper). Grabbed by downloading the real launch video (yt-dlp with `--extractor-args "youtube:player_client=android,ios"` bypasses YouTube's SABR block; plain yt-dlp fails; video id Y9Wz2PV404E, 640x360) and extracting the frame at 9.2s. Moved the player's play-button to left:72% so it sits in the empty grid, not over the model names. STANDING: rotate opener assets across Fable reels (butterflies vs the Fable-lineup card vs the "Prompting Claude Fable 5" docs page `guide.png`) so every reel doesn't open on the same clip. The launch video `fl.mp4` (113s) is in this session's scratchpad if more frames are wanted.

**v2 fixes (Alex):** (1) A "cut, cut, cut" leaked into the final CTA audio. ROOT CAUSE: the splice used small.en word times which DRIFTED ~2.7s in the CTA region; my last keep-range started at 128.08s, inside the "Cut cut cut" right before the good CTA take (small.en/base.en both DROP the fast "cut" so the transcript looked clean but it's audible). FIX: re-transcribe the CTA region with base.en (accurate), found take-3 "Comment..." at 130.72s; changed the last keep to between(t,130.72,133.06). LESSON: after splicing, whisper CAN silently drop short "cut" fragments, so verify take boundaries with base.en word-times in retake-heavy regions, don't trust that a clean transcript = clean audio. New dur 57.5s, durationInFrames 1730. (2) Replaced the mock "Fable 5 Playbook · pulled 3 of 9" panel in the curation beat with a REAL SCREEN-RECORDING SCROLL of the actual "Prompting Claude Fable 5" docs page: new `ScrollBrowser` component (browser chrome + "platform.claude.com › prompting-claude-fable-5" URL bar + a moving scrollbar) that pans `public/fable/guide.png` (the full 2800x12000 page screenshot) upward via translateY over fr(6.2)->fr(15.6), + a "pulled the 3 best" badge. Reads as authentically scrolling Anthropic's real page while the VO says "I went through Anthropic's own Fable 5 playbook." Redelivered.

**v3 fixes (Alex):** (1) OPENER now real MOVING VIDEO, not a static frame: embedded the actual launch clip via Remotion `<OffthreadVideo src={staticFile("fable/fable_launch.mp4")} startFrom={fr(5.4)} muted objectFit:cover>` in the player card, so the model names animate in (Haiku -> Fable/Opus/Sonnet/Haiku) = "stuff flipping". Removed the play-button overlay (it's actually playing) and added a small pulsing red "LIVE" chip instead. STANDING: use OffthreadVideo of `fable_launch.mp4` (the real 640x360 launch video, in public/fable/) for a moving, recognizable Fable opener; pick startFrom for a dynamic segment (~5.4s catches the name reveal). (2) PLAYBOOK beat vibe fixed: the trend-jack header was overlapping the browser; now the header FADES OUT as the scroll browser rises (opacity *(1-book*0.94)) and a clay kicker pill "Anthropic's Fable 5 playbook" titles the beat instead. Real page still scrolls (ScrollBrowser + guide.png). (3) COLD-READ (M3) reworked: was too text-heavy/cramped. Now a cleaner 2-card compare (self-review "Looks good" with a hidden bug | fresh read "Found it", Anthropic-backed chip) + a 🔍 MAGNIFIER that sweeps L->R (Easing.inOut, ~1.5s, synced to "so make it review" ~6s) and the missed bug gets a red CIRCLE drawn around it (caught @fr7.3); dropped the redundant bottom "Anthropic:" line; clear vertical gaps (cards 506-~674, steal box 790) so nothing overlaps. Redelivered.

**v4 RETENTION PASS (Alex: hook still boring/blank; playbook scroll stays too long; more animation throughout):** (1) HOOK = a FAST-CUT MONTAGE: built `public/fable/fable_montage.mp4` via ffmpeg trim+concat (14 cuts x 0.55s = 8.2s) of the launch video's flashiest shots (Fable lineup card, fractal-grid graphic, red-circle binary, petri dish, presenters, tarot, mushroom, gallery-wide) — python-built the filter_complex (zsh loop chokes on the colons). New `MontageCard` plays it with a WHITE-FLASH pop + zoom punch + rotation jitter on every cut boundary (CUT=16.5 frames; flash=pow(max(0,1-cp*3.4),2)) + a red-dot LIVE chip. (2) FIREWORKS: reusable `Firework` component (12 dots, gravity + fade); 8 bursts staggered around the montage + 2 more on the "3" transition. (3) OPEN-LOOP SLAM: red "you're missing 3 of them" pill back-eases in at 4.7s under the montage. (4) PLAYBOOK beat cut from ~9.4s to ~3.8s (ScrollBrowser fr7.9->11.7, scroll 1150) and ANIMATED: green "✓ move 1/2/3" stamps back-ease onto the scrolling page (fr8.8/9.6/10.4) + "pulled the 3 best" badge. (5) NEW PRIMER BEAT (fr12.4-16.8, was dead scroll time): "it does EXACTLY what you say" = prompt pill '"make the header navy"' -> ⚡ + gold speed lines -> a mini header card flips grey->NAVY instantly + green "EXACTLY. instantly." stamp + "the whole game: steer it precisely". (6) GLOBAL: white flash pop (~0.23s) at every scene boundary. Redelivered. STANDING: for hook energy, fast-cut montage + per-cut flash + fireworks + an open-loop slam is the pattern; never leave a screen-recording scroll on screen >4s without something animating on top of it.

**v5 (Alex):** (1) Hook title was 2 lines at 66px and its second line overlapped the video card top → now ONE line, fontSize 53, nowrap, top 384 (clears the card at 468). Rule: hook headers must fit ONE line or the content below must start lower. (2) Music was too loud → seo_music volume dropped 0.18→0.11 (0.13→0.08 at CTA), ~4dB down; SAME drop applied to ATTACK (reel 25) which shipped with identical levels. **NEW STANDING music level: 0.11 main / 0.08 CTA** for seo_music under VO. Both re-rendered + redelivered.
