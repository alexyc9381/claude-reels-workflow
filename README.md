# Claude Reels — Workflow (code + brain)

The version-controlled half of a Greg-Isenberg-style AI/Claude Instagram-reel system: the **code** (a
Remotion reel project), the **ideation + style tooling**, the accumulated **memory "brain"**, and
measured **creator style packs**. Clone it, `npm install`, and you're developing new reels.

> 🤖 **If you're an agent, read [`CLAUDE.md`](CLAUDE.md) first** — it's the map (what's here + where to go
> for each task). This README is the human landing page.

> **Two-tier setup.** This repo is **code + text only**. The heavy media (voiceovers, music, finished reels,
> SFX, render outputs) is NOT in git — it lives in Google Drive **`My Drive / Claude Reels`** (see
> `matchtern-longform-editing-engine.zip` there, the same project fully populated). Keep code here, media in
> Drive.

<!-- INDEX:AUTO -->
| reels (code in-repo) | **44** | factory logs | **24** |
|---|---|---|---|
| storyboards | 6 | caption files | 46 |
| memory `.md` | 133 | creator packs | 2 |
| script-DNA creators | 8 | | |
<!-- /INDEX:AUTO -->
*(generated by [`tools/build_repo_index.py`](tools/build_repo_index.py))*

## What's here

Every subsystem has its own `README.md` (same skeleton). The map with a task-router is [`CLAUDE.md`](CLAUDE.md);
the full subsystem list is there. The highlights:

- [`video/`](video/) — the Remotion project: every reel's `.tsx`, `Root.tsx` (registers them), word-timed
  caption JSONs, and the small visual assets (audio/video gitignored).
- [`memory/`](memory/) — the AI brain; point Claude Code here. [`memory/MEMORY.md`](memory/MEMORY.md) is the index.
- [`packs/`](packs/) — measured, verified creator **edit**-style packs (e.g. the two Saraev packs).
- [`script-style-replicator/`](script-style-replicator/) — replicate a creator's **script** voice from real
  transcripts. Start at its `SKILL.md`.
- [`REELS.md`](REELS.md) — the generated registry: every reel joined across its code / log / storyboard homes.
- [`CLAUDE-REELS-PLAYBOOK.md`](CLAUDE-REELS-PLAYBOOK.md) — the complete production manual.
- [`SPLIT-SCREEN-FORMAT.md`](SPLIT-SCREEN-FORMAT.md) — the two-screen stacked tutorial layout (used in
  `video/src/ClaudeSlashReel.tsx`).

## Setup on a new account

```bash
git clone <this-repo>
cd <repo>/video && npm install        # Remotion, React, three, etc.
cd ../tools     && npm install        # ffmpeg-static + ffprobe-static
pip3 install --user faster-whisper    # transcription (playbook §3.3)
```

Open in **Claude Code with `ultracode` on**, pointed at `memory/`. To render existing reels, first restore
media: unzip `video/public/` from the Drive `matchtern-longform-editing-engine.zip` over this checkout. For
brand-new reels you record fresh VOs, so you don't need the old media.

## Full pipeline

Idea/script → record VO → transcribe (faster-whisper) → splice/clean/×1.10 speed → aligned captions →
author scenes in a new `Claude<Name>Reel.tsx` → `remotion render` → deliver. Every step with exact commands
is in [`CLAUDE-REELS-PLAYBOOK.md`](CLAUDE-REELS-PLAYBOOK.md) §6. Naming + consistency rules are in
[`docs/CONVENTIONS.md`](docs/CONVENTIONS.md).
