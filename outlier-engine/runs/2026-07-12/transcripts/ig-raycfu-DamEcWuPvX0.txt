### raycfu | DamEcWuPvX0 | 240K | 2d
CAPTION: How to build an Agentic OS using Fable 5 part 1 #ai #tech 
TRANSCRIPT:
Most people don't realize that Cloud Fable 5 can run for hours unintended, dispatch its
own sub agents, and do a whole week of work in a single night.
Here's how to build an actual Agente OS using Fable 5.
Start by configuring the Agente OS engine.
The model string should be set to Fable 5, and make sure to set max tokens to 64K, so
it caps the thinking plus and responses.
Effort is always set to load a max, but never extra high unless you're working with an
agente glue.
And here's the part most people will miss.
Refusals need to come back as HTTP200, in this way your scripts have to check for stop
reason.
Refusal and not just exit code.
Create a batch code called loop.sh and it runs on a cron drive every morning and runs
your entire system.
First use a cheap AI model like when to look at your project, and ask if anything
change or needs attention.
If yes, Fable accepts up as the manager.
It does not do the actual work, but writes down exactly what needs to be changed.
Then you saw it to do actual work, and a fresh copy of Fable that checks it like an
expector.
Next build up the report card.
Every task the system finishes gets logged as a pass or fail, and each type of task will
earn its own grade over time.
Once something passes 20 times to the 95% success rate, this system is then allowed to do
it completely on its own.
But if the grade ever drops below 90%, the system will lose that privilege, and you get
an email.
So this way you never have to guess whether to trust your AI, the numbers will speak
for you.
Build out a ghost directory.
The system writes down a simple test that proof it still works, and then it reruns one
of those tests every single day.
The moment something you build three weeks ago quietly breaks, it flags it.
It tells you exactly what changed since the last word.
This is how nothing you build will ever fall apart without you noticing.
I created a step by step guide to build out this agenda system with many more builds using
Fable 5.
Just like the video and comment agent and I'll send it to you.
So ball four part two with the rest of the builds for the gentigo system with Fable
5.
